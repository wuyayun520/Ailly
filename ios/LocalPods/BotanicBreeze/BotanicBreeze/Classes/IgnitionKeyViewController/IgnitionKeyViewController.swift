
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_makeName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let str_cameraValue:String = "Load...frame app block point scale"

/*: "Skip" :*/
fileprivate let str_picName:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let str_imageData:[UInt8] = [0x4c,0x70,0x70,0x74,0x50,0x6b,0x6b,0x68,0x2a,0x4a,0x65,0x70,0x6d,0x72,0x61,0x50,0x6b,0x4e,0x77,0x2c,0x23,0x76,0x61,0x67,0x6c,0x65,0x76,0x63,0x61,0x23,0x2d]

private func textTheory(value num: UInt8) -> UInt8 {
    return num ^ 4
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let str_showData:[UInt8] = [0xf3,0xed,0xea,0xe0,0xeb,0xf3,0xaa,0xeb,0xea,0xd4,0xe5,0xe3,0xe1,0xd7,0xec,0xeb,0xf3,0xa2,0xa2,0xeb,0xea,0xd4,0xe5,0xe3,0xe1,0xd7,0xec,0xeb,0xf3,0xac,0xad,0xbf]

private func dropTo(bubble num: UInt8) -> UInt8 {
    return num ^ 132
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let str_itemName:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e","S","h","o","w",")",":"," "]

/*: --- :*/
fileprivate let str_byText:String = "---"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let str_numberMText:[UInt8] = [0x3b,0x29,0x28,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageHide):  :*/
fileprivate let str_conTitle:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let str_textData:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let str_mainTitleName:[Character] = ["c","a","n","G","o","B","a","c"]
fileprivate let str_indicatorData:String = "K"

/*: "PHPSESSID= :*/
fileprivate let str_addValue:String = "ph"
fileprivate let str_labText:[Character] = ["P","S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let str_paperName:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let str_colorMaxLiveData:String = "return user self databaseCookie"

/*: "nav_back_black_nor" :*/
fileprivate let str_modelName:[Character] = ["n","a","v","_","b","a","c","k","_","b","l","a","c","k","_"]
fileprivate let str_cellData:String = "nfill"

/*: "ico_intimate_list_sm" :*/
fileprivate let str_buttonValue:[Character] = ["i","c","o","_","i","n","t","i","m","a"]
fileprivate let str_userActualContent:[Character] = ["t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let str_showToData:[UInt8] = [0x3b,0x30,0x3c,0x2a,0x32,0x3a,0x31,0x2b,0x71,0x38,0x3a,0x2b,0x1a,0x33,0x3a,0x32,0x3a,0x31,0x2b,0x2c,0x1d,0x26,0xb,0x3e,0x38,0x11,0x3e,0x32,0x3a,0x77,0x78,0x3d,0x30,0x3b,0x26,0x78,0x76,0x4,0x6f,0x2,0x71,0x2c,0x2b,0x26,0x33,0x3a,0x71,0x3d,0x3e,0x3c,0x34,0x38,0x2d,0x30,0x2a,0x31,0x3b,0x62,0x78,0x2d,0x38,0x3d,0x3e,0x77,0x6f,0x73,0x6f,0x73,0x6f,0x73,0x6f,0x76,0x78]

/*: "Alert" :*/
fileprivate let str_labName:String = "all else screen space downAlert"

/*: "OK" :*/
fileprivate let str_bubbleContent:[Character] = ["O","K"]

/*: "Cancle" :*/
fileprivate let str_dayTitle:String = "family correlation result staff completeCancle"

/*: "Done" :*/
fileprivate let str_sendValue:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let str_sexValue:String = "time picture textjs cal"
fileprivate let str_topText:String = "aspect name self letod n"
fileprivate let str_toName:[Character] = ["a","m","e"," ","="," "]

/*: , message =  :*/
fileprivate let str_priceData:[Character] = [","," ","m","e","s","s","a","g","e"," "]
fileprivate let str_clearData:String = "accept "

/*: "http://" :*/
fileprivate let str_modelArrayValue:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let str_backgroundTimeErrorContent:String = "https:/else menu type view"
fileprivate let str_pathColorText:[Character] = ["/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let str_miniValue:[UInt8] = [0x90,0x87,0x94,0xc6,0x85,0x89,0x89,0x8d,0x8f,0x83,0xa8,0x87,0x8b,0x83,0x95,0xc6,0xdb,0xc6,0x82,0x89,0x85,0x93,0x8b,0x83,0x88,0x92,0xc8,0x85,0x89,0x89,0x8d,0x8f,0x83,0xc8,0x95,0x96,0x8a,0x8f,0x92,0xce,0xc1,0xdd,0xc6,0xc1,0xcf,0xc8,0x8b,0x87,0x96,0xce,0x80,0x93,0x88,0x85,0x92,0x8f,0x89,0x88,0xce,0x85,0x89,0x89,0x8d,0x8f,0x83,0xcf,0xc6,0x9d,0xc6,0x94,0x83,0x92,0x93,0x94,0x88,0xc6,0x85,0x89,0x89,0x8d,0x8f,0x83,0xc8,0x95,0x96,0x8a,0x8f,0x92,0xce,0xc1,0xdb,0xc1,0xcf,0xbd,0xd6,0xbb,0xc6,0x9b,0xc6,0xcf,0xdd,0xec]

private func firstStatus(manager num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "uid= :*/
fileprivate let str_viewData:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let str_intimateValue:[Character] = [";","d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let str_cookieText:String = "end index let type;path="
fileprivate let str_fatalData:[Character] = ["/"]

/*: "document.cookie=' :*/
fileprivate let str_mpValue:String = "datacu"
fileprivate let str_managerTitle:String = "csectionsection"
fileprivate let str_toValue:[Character] = ["k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let str_fromName:[Character] = ["\'",";","\n"]

/*: ;domin= :*/
fileprivate let str_emptyName:String = ";domintype text with"
fileprivate let str_callName:String = "="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let str_alongName:[UInt8] = [0x27,0x3d,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x7b,0x20,0x29,0x31,0x2d,0x20,0x3d,0x3d,0x20,0x29,0x27,0x44,0x49,0x53,0x53,0x45,0x53,0x50,0x48,0x50,0x27,0x28,0x66,0x4f,0x78,0x65,0x64,0x6e,0x69,0x2e,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x20,0x66,0x69]

/*: '; };\n" :*/
fileprivate let str_finishName:[Character] = ["\'",";"," ","}",";"]
fileprivate let str_addData:String = "\n"

/*: '" :*/
fileprivate let str_viewName:String = "\'"

/*: "h5Domain =  :*/
fileprivate let str_modelTitle:[Character] = ["h","5","D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let str_countLayerValue:[Character] = ["g","e","t","A","l","l","C","o","o","k","i","e","s"," ","="]
fileprivate let str_turnText:String = "icon"

/*: "PHPSESSID" :*/
fileprivate let str_withValueText:String = "PcornerPS"

/*: "uid" :*/
fileprivate let str_colorMessageData:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let str_labContent:String = "row model imageappC"
fileprivate let str_intimateText:String = "modeki"

/*: "UID" :*/
fileprivate let str_userValue:String = "uid"

/*: "navigator.userAgent" :*/
fileprivate let str_postName:String = "naview"
fileprivate let str_indexText:[Character] = ["s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let str_handleTitle:String = "model message viewxunwu_i"
fileprivate let str_contentName:String = "peditone"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let str_normalName:String = "xunameu"
fileprivate let str_willValue:String = "tool to by namee xu"
fileprivate let str_genderText:[Character] = ["n","w","u","_","v","e","r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let str_playRawData:String = "model response xun"
fileprivate let str_listTitle:String = "ackage/self layer"

/*:  xunwu_bundleId/ :*/
fileprivate let str_executeName:String = " xunwin on application tab"
fileprivate let str_elementTitle:String = "view model let errorleId"
fileprivate let str_contextValue:[Character] = ["/"]

/*:   webkit/1" :*/
fileprivate let str_iconData:String = "  websure party restore"

/*: "  :*/
fileprivate let str_frameContent:String = " "

/*: " xunwu_device_id/ :*/
fileprivate let str_sizeTitle:String = "self name xun"
fileprivate let str_actionData:String = "bottomvicbottom"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  IgnitionKeyViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit

//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class IgnitionKeyViewController: LatterDayRecognizerDelegate {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: AddConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: TypeNameLiteral?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: TypeNameLiteral?, webConfig: AddConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_makeName.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        noneventAndShow()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        addHide()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(str_cameraValue.prefix(7)))
        //: addNotification()
        littleness()
        //: func__checkConfig()
        beforeConfigKey()
        //: func__setUpWebView()
        ballBuster()
        //: func__registerObserver()
        arrayData()
        //: func__beginStartRequest()
        pageHide()
        //: func_checkAppleIAP()
        randomBackground()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_maxContent, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            terms(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.plaster(), .font: UIFont.pingfang(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_picName)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension IgnitionKeyViewController {
    /// 添加通知
    //: private func addNotification() {
    private func littleness() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(makeupView),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: kLet_messageClickPublicData,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(noneventAndShow),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func makeupView() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_imageData.map{textTheory(value: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func noneventAndShow() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_showData.map{dropTo(bubble: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_itemName)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func addHide() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_numberMText.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_conTitle)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension IgnitionKeyViewController {
    //: func func__registerObserver() {
    func arrayData() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(str_textData)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(str_mainTitleName) + str_indicatorData.lowercased()))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(oakDown), name: kLet_postValue, object: nil)
    }

    //: func func__setUpWebView() {
    func ballBuster() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        nameTime()
        //: func__addBridgeMethod()
        addAcross()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.imageColor()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            ModelReactiveCompatible.sendImage(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(kLet_timeSenseValue)
            }
        }
    }

    //: func func__beginStartRequest() {
    func pageHide() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.showRequest()
            }
            //: } else {
        } else {
            //: func__startRequest()
            showRequest()
        }
    }

    //: func func__startRequest() {
    func showRequest() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = EqualThen.share.loginUid
        let uid = EqualThen.share.loginUid
        //: let ssid = EqualThen.share.loginSessionId
        let ssid = EqualThen.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (str_addValue.uppercased() + String(str_labText)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(str_paperName)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(str_colorMaxLiveData.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        exceptClear()
        //: func__initRightButton()
        orientationButton()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func randomBackground() {
        //: if webUrlType == .RechargeFullPage ||
        if webUrlType == .RechargeFullPage ||
            //: webUrlType == .RechargeHalfPage ||
            webUrlType == .RechargeHalfPage ||
            //: urlStr.contains(WebUrlType.RechargeAlert.rawValue) ||
            urlStr.contains(TypeNameLiteral.RechargeAlert.rawValue) ||
            //: urlStr.contains(WebUrlType.SubscribeAlert.rawValue) {
            urlStr.contains(TypeNameLiteral.SubscribeAlert.rawValue)
        {
            //: AppleIAPManager.shared.completionHandle = { [weak self] type, reportMoney in
            AtThen.shared.completionHandle = { [weak self] type, reportMoney in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if type == .veritySucceed {
                if type == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney)
                    self.addSize(price: reportMoney)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            AtThen.shared.response()
        }
    }

    //: func func__initLeftButton() {
    func likeButton() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.eponymUp(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.eponymUp(name: (String(str_modelName) + str_cellData.replacingOccurrences(of: "fill", with: "or"))), style: .plain, target: self, action: #selector(envelop(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func orientationButton() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(TypeNameLiteral.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.eponymUp(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.eponymUp(name: (String(str_buttonValue) + String(str_userActualContent))), style: .plain, target: self, action: #selector(currentBar))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func envelop(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        appToMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.infoController() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: IgnitionKeyViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! IgnitionKeyViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! IgnitionKeyViewController).noneventAndShow()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func beforeConfigKey() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func rowPath(sucess: Bool) {
        //: ProgressHUD.dismiss()
        ModelReactiveCompatible.dataConverterCan()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func exceptClear() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: str_showToData.map{$0^95}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func javelin(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: EditTextThen.share.func__pushToWebVC(urlStr: url)
            EditTextThen.share.bileOf(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func firstUrl(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func oakDown() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kLet_postValue, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func currentBar() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = OfKeyUpView(frame: CGRect(x: 0, y: 0, width: kLet_cameraValue, height: kLet_clickData))
        //: view.show()
        view.brandShow()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension IgnitionKeyViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            ModelReactiveCompatible.sendImage(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            rowPath(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        likeButton()
        //: func__clearJSBgColor()
        exceptClear()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            rowPath(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            rowPath(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(str_labName.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(str_dayTitle.suffix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(str_sendValue)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension IgnitionKeyViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(str_sexValue.suffix(6)) + "l meth" + String(str_topText.suffix(4)) + String(str_toName)) + "\(message.name)" + (String(str_priceData) + str_clearData.replacingOccurrences(of: "accept", with: "=")) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.panMessage(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func addAcross() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in TypeTargetMirrorPath.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func appToMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in TypeTargetMirrorPath.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension IgnitionKeyViewController {
    //: func func__addUserScript() {
    func utilizerPresentPath() {
        //: var htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(str_modelArrayValue))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_modelArrayValue)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(str_backgroundTimeErrorContent.prefix(7)) + String(str_pathColorText))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_backgroundTimeErrorContent.prefix(7)) + String(str_pathColorText)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = EqualThen.share.loginUserMode.userID
        let tempUid: String = EqualThen.share.loginUserMode.userID
        //: let tempSessionID: String = EqualThen.share.loginSessionId
        let tempSessionID: String = EqualThen.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: str_miniValue.map{firstStatus(manager: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_viewData)) + "\(tempUid)" + (String(str_intimateValue)) + "\(htmlDomain)" + (String(str_cookieText.suffix(6)) + String(str_fatalData))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((str_mpValue.replacingOccurrences(of: "data", with: "do") + "ment." + str_managerTitle.replacingOccurrences(of: "section", with: "o") + String(str_toValue)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (str_addValue.uppercased() + String(str_labText)) + "\(tempSessionID)" + (String(str_emptyName.prefix(6)) + str_callName.capitalized) + "\(htmlDomain)" + (String(str_cookieText.suffix(6)) + String(str_fatalData))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((str_mpValue.replacingOccurrences(of: "data", with: "do") + "ment." + str_managerTitle.replacingOccurrences(of: "section", with: "o") + String(str_toValue)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: str_alongName.reversed(), encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_viewData)) + "\(tempUid)" + (String(str_intimateValue)) + "\(htmlDomain)" + (String(str_cookieText.suffix(6)) + String(str_fatalData))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((str_mpValue.replacingOccurrences(of: "data", with: "do") + "ment." + str_managerTitle.replacingOccurrences(of: "section", with: "o") + String(str_toValue)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_viewData)) + "\(tempSessionID)" + (String(str_intimateValue)) + "\(htmlDomain)" + (String(str_cookieText.suffix(6)) + String(str_fatalData))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((str_mpValue.replacingOccurrences(of: "data", with: "do") + "ment." + str_managerTitle.replacingOccurrences(of: "section", with: "o") + String(str_toValue)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func terms(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
            let htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(str_modelTitle)) + "\(htmlDomain)")
            //: let userID: String = EqualThen.share.loginUserMode.userID
            let userID: String = EqualThen.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(str_countLayerValue) + str_turnText.replacingOccurrences(of: "icon", with: " ")) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (str_withValueText.replacingOccurrences(of: "corner", with: "H") + "ESSID") {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(str_colorMessageData)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.pageHide()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.infoAdd(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func infoAdd(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
                let htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(str_modelTitle)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: SendUpReactiveCompatible.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(str_labContent.suffix(4)) + str_intimateText.replacingOccurrences(of: "mode", with: "oo") + "es = ") + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (str_userValue.uppercased()), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(str_colorMessageData))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie = HTTPCookie(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (str_withValueText.replacingOccurrences(of: "corner", with: "H") + "ESSID"), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (str_withValueText.replacingOccurrences(of: "corner", with: "H") + "ESSID")
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie = HTTPCookie(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.pageHide()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func nameTime() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((str_postName.replacingOccurrences(of: "view", with: "vi") + "gator.u" + String(str_indexText))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (str_postName.replacingOccurrences(of: "view", with: "vi") + "gator.u" + String(str_indexText)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(str_handleTitle.suffix(7)) + str_contentName.replacingOccurrences(of: "edit", with: "h"))) {
                let str = "xunwu_iphone xunwu_version/\(kLet_descriptionData) xunwu_package/\(kLet_playPageValue) xunwu_bundleId/\(kLet_succeedLineViewData)  webkit/1"
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.textBlock { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(str_sizeTitle.suffix(4)) + "wu_d" + str_actionData.replacingOccurrences(of: "bottom", with: "e") + "_id/") + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
