
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_cellValue:[UInt8] = [0xf,0x14,0xf,0x1a,0xce,0x9,0x15,0xa,0xb,0x18,0xe0,0xcf,0xc6,0xe,0x7,0x19,0xc6,0x14,0x15,0x1a,0xc6,0x8,0xb,0xb,0x14,0xc6,0xf,0x13,0x16,0x12,0xb,0x13,0xb,0x14,0x1a,0xb,0xa]

fileprivate func insideFrom(model num: UInt8) -> UInt8 {
    let value = Int(num) + 90
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let str_cameraValue:[UInt8] = [0xca,0xf5,0xe0,0xeb,0xa5,0xf1,0xed,0xe0,0xeb,0xa5,0xa0,0xc5,0xa5,0xf2,0xec,0xe9,0xe9,0xa5,0xf7,0xe0,0xe6,0xea,0xe8,0xe8,0xe0,0xeb,0xe1,0xa5,0xf0,0xf6,0xe0,0xf7,0xf6,0xa5,0xf1,0xea,0xa5,0xfc,0xea,0xf0,0xab,0xa5,0xc4,0xa5,0xeb,0xe0,0xf2,0xa5,0xe6,0xed,0xe4,0xf1,0xa5,0xf2,0xec,0xe9,0xe9,0xa5,0xe7,0xe0,0xa5,0xe8,0xe4,0xe1,0xe0,0xa5,0xe4,0xe3,0xf1,0xe0,0xf7,0xa5,0xe4,0xa5,0xf6,0xf0,0xe6,0xe6,0xe0,0xf6,0xf6,0xe3,0xf0,0xe9,0xa5,0xf7,0xe0,0xe6,0xea,0xe8,0xe8,0xe0,0xeb,0xe1,0xe4,0xf1,0xec,0xea,0xeb,0xab]

private func viewBackgroundImage(extra num: UInt8) -> UInt8 {
    return num ^ 133
}

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let str_modelData:[UInt8] = [0x9f,0xc0,0xb5,0xbe,0x70,0xc4,0xb8,0xb5,0xbe,0x70,0x91,0xc0,0xc0,0xbc,0xc9,0xbe,0xb1,0xbd,0xb5,0x70,0xc7,0xb9,0xbc,0xbc,0x70,0xc2,0xb5,0xb3,0xbf,0xbd,0xbd,0xb5,0xbe,0xb4,0x70,0xc5,0xc3,0xb5,0xc2,0xc3,0x70,0xc4,0xbf,0x70,0xc9,0xbf,0xc5,0x7e,0x70,0xa5,0xc3,0xb5,0x70,0xc9,0xbf,0xc5,0xc2,0x70,0xb9,0xb4,0xb5,0xbe,0xc4,0xb9,0xc4,0xc9,0x70,0xc4,0xbf,0x70,0xb7,0xc2,0xb5,0xb5,0xc4,0x70,0xb5,0xb1,0xb3,0xb8,0x70,0xbf,0xc4,0xb8,0xb5,0xc2,0x7e]

fileprivate func schoolbagQuit(leading num: UInt8) -> UInt8 {
    let value = Int(num) - 80
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let str_conversationInviteWithText:[UInt8] = [0xc3,0xf5,0xef,0xe8,0xba,0xfb,0xec,0xfb,0xee,0xfb,0xe8,0xba,0xfb,0xf4,0xfe,0xba,0xf4,0xf3,0xf9,0xf1,0xf4,0xfb,0xf7,0xff,0xba,0xed,0xf3,0xf6,0xf6,0xba,0xf4,0xf5,0xee,0xba,0xf8,0xff,0xba,0xfe,0xf3,0xe9,0xea,0xf6,0xfb,0xe3,0xff,0xfe,0xba,0xf3,0xf4,0xba,0xee,0xf2,0xff,0xba,0xe8,0xfb,0xf4,0xf1,0xf3,0xf4,0xfd,0xb4]

private func targetView(sex num: UInt8) -> UInt8 {
    return num ^ 154
}

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let str_modelContent:[UInt8] = [0x1f,0x20,0x35,0x3e,0x70,0x24,0x38,0x35,0x3e,0x70,0x75,0x10,0x70,0x27,0x39,0x3c,0x3c,0x70,0x22,0x35,0x33,0x3f,0x3d,0x3d,0x35,0x3e,0x34,0x70,0x25,0x23,0x35,0x22,0x23,0x70,0x24,0x3f,0x70,0x29,0x3f,0x25,0x7e,0x70,0x5,0x23,0x35,0x70,0x29,0x3f,0x25,0x22,0x70,0x39,0x34,0x35,0x3e,0x24,0x39,0x24,0x29,0x70,0x24,0x3f,0x70,0x37,0x22,0x35,0x35,0x24,0x70,0x35,0x31,0x33,0x38,0x70,0x3f,0x24,0x38,0x35,0x22,0x7e]

private func believeFrame(array num: UInt8) -> UInt8 {
    return num ^ 80
}

/*: "CustomView" :*/
fileprivate let str_mediumValue:String = "Talkreturn if"
fileprivate let str_thenValue:[Character] = ["n","g","C","e","l","l"]

/*: "#FF2348" :*/
fileprivate let str_itemTitle:[Character] = ["#","F","F","2","3","4","8"]

/*: "ViewNotifCell" :*/
fileprivate let str_toValue:String = "Talkinreturn view"
fileprivate let str_managerName:String = "ingNself try info"
fileprivate let str_backgroundTopName:[Character] = ["o","t","i","f","C","e","l","l"]

/*: "To set up a password, you need to verify the account, choose your verification method" :*/
fileprivate let str_faceName:[UInt8] = [0x65,0x5e,0x11,0x42,0x54,0x45,0x11,0x44,0x41,0x11,0x50,0x11,0x41,0x50,0x42,0x42,0x46,0x5e,0x43,0x55,0x1d,0x11,0x48,0x5e,0x44,0x11,0x5f,0x54,0x54,0x55,0x11,0x45,0x5e,0x11,0x47,0x54,0x43,0x58,0x57,0x48,0x11,0x45,0x59,0x54,0x11,0x50,0x52,0x52,0x5e,0x44,0x5f,0x45,0x1d,0x11,0x52,0x59,0x5e,0x5e,0x42,0x54,0x11,0x48,0x5e,0x44,0x43,0x11,0x47,0x54,0x43,0x58,0x57,0x58,0x52,0x50,0x45,0x58,0x5e,0x5f,0x11,0x5c,0x54,0x45,0x59,0x5e,0x55]

private func equalQuality(map num: UInt8) -> UInt8 {
    return num ^ 49
}

/*: "Verified by Phone" :*/
fileprivate let str_viewValue:String = "Verifipath equal add"
fileprivate let str_cardData:[Character] = [" ","P","h","o","n","e"]

/*: "Verified by Email" :*/
fileprivate let str_backgroundData:[Character] = ["V","e","r","i","f","i","e"]
fileprivate let str_dropStackData:String = "user"
fileprivate let str_modelLoadValue:[Character] = [" ","b","y"," ","E","m","a","i","l"]

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let str_viewText:[UInt8] = []

/*: "UNRECOVERABLE" :*/
fileprivate let str_backgroundValue:String = "UNleading"
fileprivate let str_sizeData:String = "COhiRABLE"

/*: "Delete Account" :*/
fileprivate let str_addData:String = "Deleif view time return"
fileprivate let str_backTitle:String = "click"

/*: "Cancel" :*/
fileprivate let str_equalValue:String = "launch blockCancel"

/*: "Delete" :*/
fileprivate let str_randomName:String = "Deleteto lab group return"

/*: " ( :*/
fileprivate let str_imageValue:[Character] = [" ","("]

/*: s)" :*/
fileprivate let str_viewResultTitle:[Character] = ["s",")"]

/*: "Logout succeeded!" :*/
fileprivate let str_dataName:String = "Logoutself background"
fileprivate let str_imageName:String = " sucmake leading fatal return"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DeepWithViewDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum DataFileTextProtocol: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum TargetTableConvertible: String {
    //: case Phone_Number          = "Phone Number"
    case Phone_Number = "Phone Number"
    //: case Email_Address         = "Email Address"
    case Email_Address = "Email Address"
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case SetPassword           = "Set New Password"
    case SetPassword = "Set New Password"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class DeepWithViewDelegate: SeamViewController {
    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = ViewSettingModel()

    //: var  type = SettingsUniversal.Contact_information
    var type = DataFileTextProtocol.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_cellValue.map{insideFrom(model: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.imageColor()
        //: designView()
        classListView()
        //: getSettingData()
        environs()
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [TargetTableConvertible] = {
        //: var array = [SettingsUniversalType]()
        var array = [TargetTableConvertible]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_cameraValue, height: kLet_clickData - kLet_viewDepthText), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized, "Your avatar and nickname will not be displayed in the ranking.".localized]
        var array = ["", String(bytes: str_cameraValue.map{viewBackgroundImage(extra: $0)}, encoding: .utf8)!.localizedArguments(kLet_managerData), String(bytes: str_modelData.map{schoolbagQuit(leading: $0)}, encoding: .utf8)!.localized, String(bytes: str_conversationInviteWithText.map{targetView(sex: $0)}, encoding: .utf8)!.localized]
        //: if EqualThen.share.loginUserMode.sex == "2" {
        if EqualThen.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: str_modelContent.map{believeFrame(array: $0)}, encoding: .utf8)!.localizedArguments(kLet_managerData)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension DeepWithViewDelegate {
    //: func getSettingData() {
    func environs() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        CameraThen.enter(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<ViewSettingModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func clickType(type: DataFileTextProtocol) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: DataSource.append(.Phone_Number)
            DataSource.append(.Phone_Number)
            //: DataSource.append(.Email_Address)
            DataSource.append(.Email_Address)
            //: if EqualThen.share.loginUserMode.mobile?.count ?? 0 > 0 || EqualThen.share.loginUserMode.email.count > 0 {
            if EqualThen.share.loginUserMode.mobile?.count ?? 0 > 0 || EqualThen.share.loginUserMode.email.count > 0 {
                //: DataSource.append(.SetPassword)
                DataSource.append(.SetPassword)
            }
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    /// 获取修改【手机号】密码验证码
    //: private func req_sendModifyPwdSms() {
    private func nameBuffer() {
        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()
        //: TalkingLoginRequestTool.req_sendModifyPwdSms { succeed, result, errorModel in
        LoginRequestTool.build { succeed, _, _ in
            //: ProgressHUD.dismiss()
            ModelReactiveCompatible.dataConverterCan()
            //: guard succeed else { return }
            guard succeed else { return }
            //: EditTextThen.share.func__pushToSetNewPasswordVC(type: .PhoneLogin, content: EqualThen.share.loginUserMode.mobile ?? "", isNewPwd: false)
            EditTextThen.share.feelList(type: .PhoneLogin, content: EqualThen.share.loginUserMode.mobile ?? "", isNewPwd: false)
        }
    }

    /// 获取修改【邮箱】密码验证码
    //: private func req_sendModifyPwdEmail() {
    private func totalSum() {
        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()
        //: TalkingLoginRequestTool.req_sendModifyPwdEmail { succeed, result, errorModel in
        LoginRequestTool.change { succeed, _, _ in
            //: ProgressHUD.dismiss()
            ModelReactiveCompatible.dataConverterCan()
            //: guard succeed else { return }
            guard succeed else { return }
            //: EditTextThen.share.func__pushToSetNewPasswordVC(type: .EmailLogin, content: EqualThen.share.loginUserMode.email, isNewPwd: false)
            EditTextThen.share.feelList(type: .EmailLogin, content: EqualThen.share.loginUserMode.email, isNewPwd: false)
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension DeepWithViewDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return actualHeight(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if EqualThen.share.loginUserMode.sex == "2" {
                if EqualThen.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return actualHeight(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return actualHeight(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return actualHeight(h: 92)
            }
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let identifier = "TalkingSettingCell"
            let identifier = (String(str_mediumValue.prefix(4)) + "ingSetti" + String(str_thenValue))
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingSettingCell
            let cell: CustomView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! CustomView

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.designate(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)

            //: if DataSource[indexPath.row] == SettingsUniversalType.Phone_Number {
            if DataSource[indexPath.row] == TargetTableConvertible.Phone_Number {
                //: if EqualThen.share.loginUserMode.mobile?.count ?? 0 > 0 {
                if EqualThen.share.loginUserMode.mobile?.count ?? 0 > 0 {
                    //: cell.setCellDetails(detail: EqualThen.share.loginUserMode.mobile ?? "")
                    cell.sunParlour(detail: EqualThen.share.loginUserMode.mobile ?? "")
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: cell.titleBLB.textColor = .appTitleColor()
                cell.titleBLB.textColor = .executeTo()
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.Email_Address {
            } else if DataSource[indexPath.row] == TargetTableConvertible.Email_Address {
                //: if EqualThen.share.loginUserMode.email.count > 0 {
                if EqualThen.share.loginUserMode.email.count > 0 {
                    //: cell.setCellDetails(detail: EqualThen.share.loginUserMode.email )
                    cell.sunParlour(detail: EqualThen.share.loginUserMode.email)
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.SetPassword {
            } else if DataSource[indexPath.row] == TargetTableConvertible.SetPassword {
                //: cell.editImag.isHidden = false
                cell.editImag.isHidden = false
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            } else if DataSource[indexPath.row] == TargetTableConvertible.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(str_itemTitle)))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let identifier = "TalkingSettingNotifCell"
            let identifier = (String(str_toValue.prefix(6)) + "gSett" + String(str_managerName.prefix(4)) + String(str_backgroundTopName))
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingSettingNotifCell
            let cell: ViewNotifCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! ViewNotifCell

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.valueLast(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.details(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.outspoken(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.outspoken(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.outspoken(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.outspoken(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.outspoken(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.Phone_Number.rawValue {
            if DataSource[indexPath.row].rawValue == TargetTableConvertible.Phone_Number.rawValue {
                //: let mobile = EqualThen.share.loginUserMode.mobile ?? ""
                let mobile = EqualThen.share.loginUserMode.mobile ?? ""
                //: if mobile.isEmptyString {
                if mobile.isEmptyString {
                    //: let vc = TalkingBindPhoneVC()
                    let vc = CaptureViewController()
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.Email_Address.rawValue {
            } else if DataSource[indexPath.row].rawValue == TargetTableConvertible.Email_Address.rawValue {
                //: if EqualThen.share.loginUserMode.email.count <= 0 {
                if EqualThen.share.loginUserMode.email.count <= 0 {
                    //: let vc = TalkingLoginBindEmailVC()
                    let vc = BeautyThen()
                    //: vc.isBack = true
                    vc.isBack = true
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            } else if DataSource[indexPath.row].rawValue == TargetTableConvertible.DeleteA.rawValue {
                //: writeOff()
                sumerval()
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.SetPassword.rawValue {
            } else if DataSource[indexPath.row].rawValue == TargetTableConvertible.SetPassword.rawValue {
                //: let mobile = EqualThen.share.loginUserMode.mobile ?? ""
                let mobile = EqualThen.share.loginUserMode.mobile ?? ""
                //: let email = EqualThen.share.loginUserMode.email
                let email = EqualThen.share.loginUserMode.email
                //: if !mobile.isEmpty, !email.isEmpty {
                if !mobile.isEmpty, !email.isEmpty { // 手机号和邮箱都存在
                    //: let vc = TalkingMunuPopView(frame: self.view.frame)
                    let vc = OnViewDelegate(frame: self.view.frame)
                    //: vc.title = "To set up a password, you need to verify the account, choose your verification method".localized
                    vc.title = String(bytes: str_faceName.map{equalQuality(map: $0)}, encoding: .utf8)!.localized
                    //: vc.titleFont = UIFont.outsideAllSize(fontSize: 12)
                    vc.titleFont = UIFont.outsideAllSize(fontSize: 12)
                    //: vc.titleColor = UIColor.publishView()
                    vc.titleColor = UIColor.publishView()
                    //: vc.cellTitleFont = UIFont.pingfangWithDiameter(fontSize: 16)
                    vc.cellTitleFont = UIFont.pingfangWithDiameter(fontSize: 16)
                    //: vc.hasCancel = false
                    vc.hasCancel = false
                    //: vc.initwithList(cellTitleList: ["Verified by Phone".localized, "Verified by Email".localized])
                    vc.videoList(cellTitleList: [(String(str_viewValue.prefix(6)) + "ed by" + String(str_cardData)).localized, (String(str_backgroundData) + str_dropStackData.replacingOccurrences(of: "user", with: "d") + String(str_modelLoadValue)).localized])
                    //: vc.munuBlock = { [weak self] index, str in
                    vc.munuBlock = { [weak self] index, _ in
                        //: guard let self = self else { return }
                        guard let self = self else { return }
                        //: if index == 0 {
                        if index == 0 {
                            //: self.req_sendModifyPwdSms()
                            self.nameBuffer()
                            //: } else if index == 1 {
                        } else if index == 1 {
                            //: self.req_sendModifyPwdEmail()
                            self.totalSum()
                        }
                    }

                    //: } else if !mobile.isEmpty {
                } else if !mobile.isEmpty {
                    //: req_sendModifyPwdSms()
                    nameBuffer()

                    //: } else if !email.isEmpty {
                } else if !email.isEmpty {
                    //: req_sendModifyPwdEmail()
                    totalSum()
                }
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension DeepWithViewDelegate {
    /// 注销
    //: private func writeOff() {
    private func sumerval() {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard RubricThen.socialistPartyActive() == false else { return }
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard DeviceSocketDelegate.shared.isTalking == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.oversight(showMsg: kLet_replaceTitle)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: str_viewText.reversed(), encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.exMatchStrRange((str_backgroundValue.replacingOccurrences(of: "leading", with: "RE") + str_sizeData.replacingOccurrences(of: "hi", with: "VE")))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.trim(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        MotilityThen.displace(title: (String(str_addData.prefix(4)) + "te Accoun" + str_backTitle.replacingOccurrences(of: "click", with: "t")).localized, attributedMessage: string, leftBtnTitle: (String(str_equalValue.suffix(6))).localized, rightBtnTitle: (String(str_randomName.prefix(6))).localized) {
            //: TalkingAlertShow.hideAlert()
            MotilityThen.afterward()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            MotilityThen.afterward()

            //: self.LockAccount()
            self.bringEqual()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                MotilityThen.betweenCurrent(title: (String(str_randomName.prefix(6))).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                MotilityThen.betweenCurrent(title: (String(str_randomName.prefix(6))).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func bringEqual() {
        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        CameraThen.outLock(params: [:]) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            ModelReactiveCompatible.dataConverterCan()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.fireside()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.fileFirst(showMsg: (String(str_dataName.prefix(6)) + String(str_imageName.prefix(4)) + "ceeded!").localized)
            }
        }
    }

    //: private func logoutTool() {
    private func fireside() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard DeviceSocketDelegate.shared.isTalking == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.oversight(showMsg: kLet_replaceTitle)
            //: return
            return
        }

        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()
        //: TalkingLoginRequestTool.req_loginOut { t in
        LoginRequestTool.atCompletion { t in
            //: ProgressHUD.dismiss()
            ModelReactiveCompatible.dataConverterCan()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_useName, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension DeepWithViewDelegate {
    //: private func designView() {
    private func classListView() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: "TalkingSettingCell")
        MainTable.register(CustomView.self, forCellReuseIdentifier: (String(str_mediumValue.prefix(4)) + "ingSetti" + String(str_thenValue)))
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: "TalkingSettingNotifCell")
        MainTable.register(ViewNotifCell.self, forCellReuseIdentifier: (String(str_toValue.prefix(6)) + "gSett" + String(str_managerName.prefix(4)) + String(str_backgroundTopName)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
