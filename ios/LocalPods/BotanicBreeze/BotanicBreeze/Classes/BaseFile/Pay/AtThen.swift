
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let str_documentTitle:[UInt8] = [0xee,0xff,0xff,0xe3,0xea,0xf0,0xe6,0xee,0xff,0xf0,0xff,0xee,0xf6,0xf0,0xfc,0xfa,0xec,0xec,0xea,0xea,0xeb,0xf0,0xe1,0xe0,0xfb,0xe6,0xe9,0xe6,0xec,0xee,0xfb,0xe6,0xe0,0xe1]

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let str_sizeData:[UInt8] = [0x12,0x3,0x3,0x1f,0x16,0xc,0x1a,0x12,0x3,0xc,0x0,0x6,0x11,0x0,0x10,0x1,0x1a,0x11,0x16,0xc,0x0,0x6,0x10,0x10,0x16,0x16,0x17,0xc,0x1d,0x1c,0x7,0x1a,0x15,0x1a,0x10,0x12,0x7,0x1a,0x1c,0x1d]

/*: "mf/recharge/createApplePay" :*/
fileprivate let str_micName:String = "return leading scale returnmf/rec"
fileprivate let str_myCellValue:String = "/creatmessage model center gift"

/*: "productId" :*/
fileprivate let str_infoPriceData:String = "productIdmake false self title"

/*: "source" :*/
fileprivate let str_messageData:[Character] = ["s","o","u","r","c","e"]

/*: "IAP充值创建订单失败: :*/
fileprivate let str_managerData:String = "IAP充值right name let"
fileprivate let str_shareData:[Character] = ["创","建","订","单","失","败",":"]

/*: , 参数: :*/
fileprivate let str_layerValue:String = ", 参user:"

/*: ." :*/
fileprivate let str_makeHungTabTitle:[Character] = ["."]

/*: "orderNum" :*/
fileprivate let str_rangeContent:String = "okitde"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let str_responseValue:[Character] = ["m","f","/","r","e","c","h","a","r","g"]
fileprivate let str_styleText:String = "up data usere/appleP"
fileprivate let str_rangeData:[Character] = ["a","y","N","o","t","i","f","y"]

/*: "IAP充值校验失败: :*/
fileprivate let str_popInputValue:String = "dataP"
fileprivate let str_bottomData:String = "充值校验失败:"

/*: "transactionId" :*/
fileprivate let str_targetWithHeightValue:String = "colorans"
fileprivate let str_barName:String = "button"
fileprivate let str_giftTitle:String = "open else titlectionId"

/*: "reportMoney" :*/
fileprivate let str_tableValue:String = "cellpo"
fileprivate let str_keyData:[Character] = ["n","e","y"]

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let str_modeData:String = "mf/AutoStool size big value attribute"
fileprivate let str_tableName:String = "time model contentub/App"
fileprivate let str_fullContent:String = "teOrderbottom index leading type"

/*: "IAP订阅创建订单失败: :*/
fileprivate let str_labelText:[Character] = ["I","A","P","订","阅","创","建","订","单","失","败",":"]

/*: "orderId" :*/
fileprivate let str_stackValue:[UInt8] = [0x87,0x8a,0x7c,0x7d,0x8a,0x61,0x7c]

fileprivate func moreQuantityimate(head num: UInt8) -> UInt8 {
    let value = Int(num) - 24
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let str_viewData:String = "frame varmf/Au"
fileprivate let str_userMinuteText:String = "view roottoSub/"
fileprivate let str_writePageName:String = "user falseaySuccess"

/*: "IAP订阅校验失败: :*/
fileprivate let str_modelValue:String = "IAP订阅show view data send"

/*: "App" :*/
fileprivate let str_keyValue:String = "Apppage self var border view"

/*: "OrderTransactionInfo_ :*/
fileprivate let str_targetPlayerContent:[Character] = ["O","r","d","e","r","T","r","a","n","s","a","c","t","i","o"]
fileprivate let str_reportValue:String = "nInfo_title view size request"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let str_decisionValueViewContent:[UInt8] = [0x5f,0x65,0x62,0x69,0x72,0x63,0x73,0x62,0x75,0x53,0x5f,0x6f,0x66,0x6e,0x49,0x6e,0x6f,0x69,0x74,0x63,0x61,0x73,0x6e,0x61,0x72,0x54,0x72,0x65,0x64,0x72,0x4f]

/*: "verifyData" :*/
fileprivate let str_inputData:[UInt8] = [0xa7,0xb4,0xa3,0xb8,0xb7,0xa8,0x95,0xb0,0xa5,0xb0]

private func bottomUnder(background num: UInt8) -> UInt8 {
    return num ^ 209
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let str_fillLimitName:String = "IAP查询内购商indicator data"
fileprivate let str_videoName:String = "品失\u{8d25}, pr"
fileprivate let str_iconContent:[Character] = ["o","d","u","c","t","I","d",":"]

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let str_lineTitle:String = "IAP\u{67e5}询"
fileprivate let str_imageErrorValue:String = "path self left if失败, produ"
fileprivate let str_viewOfThanValue:String = "text make var lab tocts."

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let str_messageValue:String = "IAP查询sense height"
fileprivate let str_countryValue:[Character] = ["内","购","\u{5546}","品","\u{5931}","败",","," "]
fileprivate let str_finishValue:String = "Error:card view"

/*: "IAP-purchased: :*/
fileprivate let str_frameData:[Character] = ["I","A","P","-","p","u","r","c","h","a"]
fileprivate let str_leadingContent:[Character] = ["s","e","d",":"]

/*: , 订单号: :*/
fileprivate let str_viewValue:[Character] = [","," ","订","单","\u{53f7}",":"]

/*: , 商品Id: :*/
fileprivate let str_panTitle:String = ", 商品Id:name label"

/*: , 苹果transId: :*/
fileprivate let str_userText:String = ", \u{82f9}果tra"
fileprivate let str_postText:[Character] = ["n","s","I","d",":"]

/*: "IAP-failed: :*/
fileprivate let str_titleData:[Character] = ["I","A","P","-","f","a","i"]
fileprivate let str_sectionName:String = "to lab elseled:"

/*: "IAP-deferred: :*/
fileprivate let str_makeValue:String = "data showIAP-d"
fileprivate let str_giftCenterText:String = "efepath"

/*: "IAP未知交易类型: :*/
fileprivate let str_targetTitle:String = "IAP\u{672a}"

/*: " 未知的交易类型" :*/
fileprivate let str_toValue:String = " 未知的player"
fileprivate let str_militaryValue:String = "易类\u{578b}"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let str_clearValue:String = "return if upIAP获取本"
fileprivate let str_statusValue:String = "据失败,"
fileprivate let str_maleName:String = "plot let main in equalsactionI"
fileprivate let str_selectedValue:String = "d:array sex"

/*: , orderId: :*/
fileprivate let str_contentValue:String = ", ordinterval favorite path as back"
fileprivate let str_appData:String = "return equal aftererId:"

/*: , payType: :*/
fileprivate let str_sectionEqualData:String = "invite raw record, payT"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AtThen.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import UIKit
import UIKit

//: import StoreKit
import StoreKit

//: import SwiftyJSON
import SwiftyJSON

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let kLet_popData = NSNotification.Name(rawValue: String(bytes: str_documentTitle.map{$0^175}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let kLet_messageName = 9

/// 支付类型
//: enum ApplePayType {
enum CreateType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum PomeWithNameProtocol: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double) -> Void
typealias IAPcompletionHandle = (PomeWithNameProtocol, Double) -> Void

//: class AppleIAPManager: NSObject {
class AtThen: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: CreateType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = AtThen()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(giftName),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func giftName() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension AtThen {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func linguisticUnitHandle(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(str_micName.suffix(6)) + "harge" + String(str_myCellValue.prefix(6)) + "eApplePay")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(str_infoPriceData.prefix(9)))] = productId
        //: dict["source"] = source
        dict[(String(str_messageData))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_managerData.prefix(5)) + String(str_shareData)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(str_rangeContent.replacingOccurrences(of: "kit", with: "r") + "rNum")].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func atSpring(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(str_responseValue) + String(str_styleText.suffix(8)) + String(str_rangeData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    DeepKeyTamperReactiveCompatible.indexView(msg: (str_popInputValue.replacingOccurrences(of: "data", with: "IA") + str_bottomData.capitalized) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.sendSearch(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.pullIn()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_popData, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(str_tableValue.replacingOccurrences(of: "cell", with: "re") + "rtMo" + String(str_keyData))].doubleValue)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension AtThen {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func sacristy(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(str_modeData.prefix(8)) + String(str_tableName.suffix(6)) + "leCrea" + String(str_fullContent.prefix(7)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(str_infoPriceData.prefix(9)))] = productId
        //: dict["source"] = source
        dict[(String(str_messageData))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_labelText)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: str_stackValue.map{moreQuantityimate(head: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func laterClose(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(str_viewData.suffix(5)) + String(str_userMinuteText.suffix(6)) + "AppleP" + String(str_writePageName.suffix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_modelValue.prefix(5)) + "\u{6821}验失败:") + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.sendSearch(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.countimateResource()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(str_tableValue.replacingOccurrences(of: "cell", with: "re") + "rtMo" + String(str_keyData))].doubleValue)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension AtThen {
    /// 初始化数据
    //: private func iap_initData() {
    private func duringTimeRow() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = stateClick(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = stateClick(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func stateClick(payType: CreateType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = pullIn()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = countimateResource()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func pullIn() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(str_keyValue.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = kLet_turnPartyValue.string(forKey: kLet_bottomNextText) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(str_targetPlayerContent) + String(str_reportValue.prefix(6))) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func countimateResource() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(str_keyValue.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = kLet_turnPartyValue.string(forKey: kLet_bottomNextText) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: str_decisionValueViewContent.reversed(), encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func componentMethod(_ transactionId: String, _ payType: CreateType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: str_inputData.map{bottomUnder(background: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: str_inputData.map{bottomUnder(background: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension AtThen {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func response() {
        //: iap_initData()
        duringTimeRow()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            callFailed(dict[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            callFailed(dict[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func callFailed(_ transactionId: String?, _ payType: CreateType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        sendSearch(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension AtThen {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func snapPath(productId: String, payType: CreateType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        duringTimeRow()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            linguisticUnitHandle(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.auspicateTemp(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            sacristy(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.auspicateTemp(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func auspicateTemp(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP查询内购商品失败, productId:\(productId).")
            DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_fillLimitName.prefix(8)) + str_videoName + String(str_iconContent)) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0)
            self.completionHandle?(.notArrow, 0)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.viewProductRequest()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func viewProductRequest() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension AtThen: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP查询内购商品失败, products.count为0.")
            DeepKeyTamperReactiveCompatible.indexView(msg: (str_lineTitle + "内购商\u{54c1}" + String(str_imageErrorValue.suffix(9)) + String(str_viewOfThanValue.suffix(4)) + "count为0."))
            //: self.completionHandle?( .noProductId, 0)
            self.completionHandle?(.noProductId, 0)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP查询内购商品失败, Error:\(error).")
        DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_messageValue.prefix(5)) + String(str_countryValue) + String(str_finishValue.prefix(6))) + "\(error).")
        //: self.completionHandle?( .noProductId, 0)
        self.completionHandle?(.noProductId, 0)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension AtThen: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0)
                    self.completionHandle?(.renewSucceed, 0)
                    //: } else {
                } else { // 普通购买和订阅
                    //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_frameData) + String(str_leadingContent)) + "\(currentPayType)" + (String(str_viewValue)) + "\(String(describing: createOrderId))" + (String(str_panTitle.prefix(7))) + "\(transaction.payment.productIdentifier)" + (str_userText + String(str_postText)) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    sendSearch(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_titleData) + String(str_sectionName.suffix(4))) + "\(currentPayType)" + (String(str_viewValue)) + "\(String(describing: createOrderId))" + (String(str_panTitle.prefix(7))) + "\(transaction.payment.productIdentifier)" + (str_userText + String(str_postText)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0)
                self.completionHandle?(.failed, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0)
                self.completionHandle?(.restored, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_makeValue.suffix(5)) + str_giftCenterText.replacingOccurrences(of: "path", with: "r") + "red:") + "\(currentPayType)" + (String(str_viewValue)) + "\(String(describing: createOrderId))" + (String(str_panTitle.prefix(7))) + "\(transaction.payment.productIdentifier)" + (str_userText + String(str_postText)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0)
                self.completionHandle?(.deferred, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0)
                self.completionHandle?(.unknow, 0)
                //: createOrderId = nil
                createOrderId = nil
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                DeepKeyTamperReactiveCompatible.indexView(msg: (str_targetTitle + "知\u{4ea4}\u{6613}类型:") + "\(currentPayType)" + (String(str_viewValue)) + "\(String(describing: createOrderId))" + (String(str_panTitle.prefix(7))) + "\(transaction.payment.productIdentifier)" + (str_userText + String(str_postText)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((str_toValue.replacingOccurrences(of: "player", with: "交") + str_militaryValue))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func sendSearch(_ transactionId: String, _ payType: CreateType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = componentMethod(transactionId, payType) else {
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_clearValue.suffix(6)) + "地收据数" + str_statusValue.capitalized + " tran" + String(str_maleName.suffix(8)) + String(str_selectedValue.prefix(2))) + "\(transactionId)" + (String(str_contentValue.prefix(5)) + String(str_appData.suffix(5))) + "\(String(describing: createOrderId))" + (String(str_sectionEqualData.suffix(6)) + "ype:") + "\(payType).")
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))] == transactionId || $0[String(bytes: str_stackValue.map{moreQuantityimate(head: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: str_stackValue.map{moreQuantityimate(head: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: str_inputData.map{bottomUnder(background: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.pullIn()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))] == transactionId || $0[String(bytes: str_stackValue.map{moreQuantityimate(head: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: str_stackValue.map{moreQuantityimate(head: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: str_inputData.map{bottomUnder(background: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.countimateResource()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > kLet_messageName {
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            atSpring(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(str_targetWithHeightValue.replacingOccurrences(of: "color", with: "tr") + str_barName.replacingOccurrences(of: "button", with: "a") + String(str_giftTitle.suffix(7)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            laterClose(transactionId, params: paramsArr.first!)
        }
    }
}
