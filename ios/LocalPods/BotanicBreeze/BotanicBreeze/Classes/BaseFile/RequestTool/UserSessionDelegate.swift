
//: Declare String Begin

/*: / :*/
fileprivate let str_listFileValue:[Character] = ["/"]

/*:  ( :*/
fileprivate let str_computerAdminValue:[Character] = [" ","("]

/*: ; build: :*/
fileprivate let str_iconData:[Character] = [";"," ","b","u","i","l","d"]
fileprivate let str_groupValue:String = ":"

/*: ; iOS  :*/
fileprivate let str_makeValue:String = "play info model; iOS "

/*: ;  :*/
fileprivate let str_showValue:String = ";add"

/*: )" :*/
fileprivate let str_observeName:[Character] = [")"]

/*: "audio" :*/
fileprivate let str_centerName:String = "audionline"

/*: + :*/
fileprivate let str_currentTitle:[Character] = ["+"]

/*: .wav" :*/
fileprivate let str_dataTitle:String = "else show model.wav"

/*: "audio/wav" :*/
fileprivate let str_modelData:[Character] = ["a","u","d","i","o","/","w","a","v"]

/*: .jpg" :*/
fileprivate let str_sizeData:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let str_handleEqualTitle:String = "type tempimage/"

/*: "://app." :*/
fileprivate let str_viewName:String = "data view://ap"
fileprivate let str_playerTitle:[Character] = ["p","."]

/*: "\"data\":null" :*/
fileprivate let str_cleanValue:String = "\""
fileprivate let str_disappearGreenReturnName:String = "userata"

/*: "\"data\":{}" :*/
fileprivate let str_withAddContent:String = "\"dat"

/*: ":null" :*/
fileprivate let str_colorName:String = ":nullto cancel guard raw"

/*: ":\"\"" :*/
fileprivate let str_makeToData:String = ":\"\\"

/*: "Request failed, Try again later" :*/
fileprivate let str_topData:[UInt8] = [0x43,0x74,0x60,0x64,0x74,0x62,0x65,0x31,0x77,0x70,0x78,0x7d,0x74,0x75,0x3d,0x31,0x45,0x63,0x68,0x31,0x70,0x76,0x70,0x78,0x7f,0x31,0x7d,0x70,0x65,0x74,0x63]

/*: "json error" :*/
fileprivate let str_observerValue:String = "json bind fill"
fileprivate let str_frameData:[Character] = ["e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let str_managerValue:[Character] = ["p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e"]
fileprivate let str_ageValue:String = "style current view equalrsion="

/*: &packageId= :*/
fileprivate let str_cellValue:String = "user video scale hidden&pa"
fileprivate let str_viewContent:String = "main labckageId="

/*: &bundleId= :*/
fileprivate let str_titlePinData:String = "&bustyle finish"
fileprivate let str_errorValue:String = "ndleId=key icon array in share"

/*: &lang= :*/
fileprivate let str_userData:String = "&lang=transaction back the to star"

/*: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| " :*/
fileprivate let str_actionValue:String = "?!@#$"
fileprivate let str_errorData:String = ",top;="
fileprivate let str_endArrayData:[Character] = ["(",")","["]
fileprivate let str_atData:String = "]{}/\\| "

/*: "/route/slowindex" :*/
fileprivate let str_titleByName:String = "/roublock label no model self"
fileprivate let str_matchValue:[Character] = ["i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let str_listData:[Character] = ["/","r","o","u","t","e","/","i","n"]
fileprivate let str_buttonAllName:[Character] = ["d","e","x"]

/*: "?path= :*/
fileprivate let str_textContent:String = "?path=if share"

/*: "&GJNonceStr= :*/
fileprivate let str_addValue:[Character] = ["&","G","J","N","o","n","c","e","S","t","r"]
fileprivate let str_videoValue:String = "="

/*: "& :*/
fileprivate let str_bubbleValue:[Character] = ["&"]

/*: = :*/
fileprivate let str_removeData:String = "="

/*: "&params= :*/
fileprivate let str_colorAllowTitle:String = "&parvar response true sign view"

/*: "/ :*/
fileprivate let str_sizeValue:String = "/"

/*: "? :*/
fileprivate let str_meValue:[Character] = ["?"]

/*: "token" :*/
fileprivate let str_keyName:String = "toraten"

/*: "请求成功 :*/
fileprivate let str_alwaysValue:String = "请\u{6c42}成功"

/*: "AAAAAAAA" :*/
fileprivate let str_fieldData:String = "addaddaddadd"

/*: "AA" :*/
fileprivate let str_managerContent:[Character] = ["A","A"]

/*: "BB" :*/
fileprivate let str_voiceValue:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let str_roomRemarkCameraTitle:[Character] = ["下","载","完","成"," ","-"]
fileprivate let str_viewData:[Character] = [" "]

/*: "/Documents/" :*/
fileprivate let str_iconText:String = "view true import/Docu"

/*: ".mp4" :*/
fileprivate let str_sizePathContentValue:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let str_topName:[Character] = ["移","动","地","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let str_cellData:String = "\u{540e}台任务下载"
fileprivate let str_frameValue:String = "\u{56de}来"

/*: "下载成功 : :*/
fileprivate let str_endNameData:String = "\u{4e0b}载成功 :"

/*: "Net Error" :*/
fileprivate let str_actionData:[Character] = ["N","e","t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let str_infoTitleMakeContent:String = "下载失\u{8d25} :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UserSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import UIKit
import UIKit

//: import Alamofire
import Alamofire

//: import CoreMedia
import CoreMedia

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let kLet_likeContent = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let kLet_availableNetData = "\(kLet_managerData)/\(kLet_intimateValue) (\(kLet_succeedLineViewData)" + (String(str_iconData) + str_groupValue.capitalized) + "\(kLet_failTabText)" + (String(str_makeValue.suffix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: ErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let kLet_nextTitle = UserSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class UserSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func photoLive(model: WithTitleThen, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.binaryTo() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.tillStrikeOut(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.asWill(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func tillStrikeOut(model: WithTitleThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.liveIndex(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.ergodic(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    tipEventAccountingDataConsequenceModelSubstanceCompletionSumeraction(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ErrorResponse(errorCode: AftermathBinaryCount.NetError.rawValue, errorMsg: kLet_errorData))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        oversight(showMsg: kLet_errorData)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (str_centerName.replacingOccurrences(of: "online", with: "o")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.pushFinish())+\(kLet_playPageValue)" + (String(str_dataTitle.suffix(4))), mimeType: (String(str_modelData)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(str_sizeData)), mimeType: (String(str_handleEqualTitle.suffix(6)) + "jpeg"))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    tipEventAccountingDataConsequenceModelSubstanceCompletionSumeraction(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ErrorResponse(errorCode: AftermathBinaryCount.NetError.rawValue, errorMsg: kLet_errorData))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        oversight(showMsg: kLet_errorData)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func asWill(model: WithTitleThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.liveIndex(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.ergodic(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: kLet_senseName ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_likeContent }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    tipEventAccountingDataConsequenceModelSubstanceCompletionSumeraction(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ErrorResponse(errorCode: AftermathBinaryCount.NetError.rawValue, errorMsg: kLet_errorData))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        oversight(showMsg: kLet_errorData)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_likeContent }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    tipEventAccountingDataConsequenceModelSubstanceCompletionSumeraction(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ErrorResponse(errorCode: AftermathBinaryCount.NetError.rawValue, errorMsg: kLet_errorData))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        oversight(showMsg: kLet_errorData)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func tipEventAccountingDataConsequenceModelSubstanceCompletionSumeraction(model: WithTitleThen, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            SendUpReactiveCompatible.share.necessary(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if kLet_senseName, model.requestServer.contains((String(str_viewName.suffix(5)) + String(str_playerTitle))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.aes256Decrypt(key: SendUpReactiveCompatible.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: (str_cleanValue.capitalized + str_disappearGreenReturnName.replacingOccurrences(of: "user", with: "d") + "\":null"), with: (str_withAddContent + "a\":{}"))
        //: if !Encryption_Request {
        if !kLet_senseName {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(str_colorName.prefix(5))), with: ":\"\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<MakeTransformable>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == AftermathBinaryCount.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, ErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    oversight(showMsg: responseModel.msg ?? String(bytes: str_topData.map{$0^17}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case AftermathBinaryCount.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_useName, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, ErrorResponse(errorCode: AftermathBinaryCount.NetError.rawValue, errorMsg: (String(str_observerValue.prefix(5)) + String(str_frameData)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func liveIndex(model: WithTitleThen) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(str_managerValue) + String(str_ageValue.suffix(6))) + "\(kLet_descriptionData)" + (String(str_cellValue.suffix(3)) + String(str_viewContent.suffix(8))) + "\(kLet_playPageValue)" + (String(str_titlePinData.prefix(3)) + String(str_errorValue.prefix(7))) + "\(kLet_succeedLineViewData)" + (String(str_userData.prefix(6))) + "\(SendUpReactiveCompatible.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if kLet_senseName, serverUrl.contains((String(str_viewName.suffix(5)) + String(str_playerTitle))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if SendUpReactiveCompatible.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(str_titleByName.prefix(4)) + "te/slow" + String(str_matchValue)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(str_listData) + String(str_buttonAllName))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(str_textContent.prefix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(str_addValue) + str_videoValue.capitalized) + "\(SendUpReactiveCompatible.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.aes256Encrypt(key: SendUpReactiveCompatible.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(str_colorAllowTitle.prefix(4)) + "ams=") + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func ergodic(model: WithTitleThen) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(kLet_availableNetData)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (str_keyName.replacingOccurrences(of: "rate", with: "ke")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func generateDown(model: WithTitleThen) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.liveIndex(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (str_alwaysValue) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (str_fieldData.replacingOccurrences(of: "add", with: "AA"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (String(str_roomRemarkCameraTitle) + String(str_viewData)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(str_iconText.suffix(5)) + "ments/") + NSDate.pushFinish() + (String(str_sizePathContentValue))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(str_topName)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (str_cellData + str_frameValue))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = MakeDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func counteractionClick(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: kLet_deviceName)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (str_endNameData) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ErrorResponse(errorCode: AftermathBinaryCount.Normal.rawValue, errorMsg: (String(str_actionData)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ErrorResponse(errorCode: AftermathBinaryCount.NetError.rawValue, errorMsg: (String(str_actionData)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_infoTitleMakeContent) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func vox(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: kLet_deviceName)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (str_endNameData) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ErrorResponse(errorCode: AftermathBinaryCount.Normal.rawValue, errorMsg: (String(str_actionData)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ErrorResponse(errorCode: AftermathBinaryCount.NetError.rawValue, errorMsg: (String(str_actionData)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_infoTitleMakeContent) + "\(errMsg)")
            }
        }
    }
}
