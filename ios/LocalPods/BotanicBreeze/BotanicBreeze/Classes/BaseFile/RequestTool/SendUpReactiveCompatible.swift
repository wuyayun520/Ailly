
//: Declare String Begin

/*: "http://" :*/
fileprivate let str_dataName:String = "http://return call layer tool"

/*: "https://" :*/
fileprivate let str_goldValue:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let str_liveData:String = "http://lab make return"

/*: "serverSelctIndex =  :*/
fileprivate let str_levelData:String = "matchrv"
fileprivate let str_contentData:[Character] = ["l","c","t","I","n","d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let str_insideValue:[Character] = ["s","e","r","v","e","r","U","r","l","S","t","r"," ","="]
fileprivate let str_modelName:String = " "

/*: "app. :*/
fileprivate let str_backgroundValue:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let str_selectionName:String = "name lab result info frame.com"

/*: "IM/getMyBlackList" :*/
fileprivate let str_nextName:[Character] = ["I","M","/","g","e","t"]
fileprivate let str_tableText:String = "MyBlto make view if effect"

/*: "mf/business/sendMsg" :*/
fileprivate let str_cellText:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let str_norText:String = "mf/buarea import"
fileprivate let str_viewOpValue:String = "empty add image/s"
fileprivate let str_byTopTitle:String = "frame letendGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_allTitle:String = "mf/cadd if open frame"
fileprivate let str_buttonTitle:String = "to color self maximumsendMsg"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_shareData:String = "mf/cherror model at video model"
fileprivate let str_labData:String = "om/seuser false let input color"
fileprivate let str_voiceData:String = "insertt"

/*: "recharge/applePayNotify" :*/
fileprivate let str_bottomValue:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y"]
fileprivate let str_effectMessageData:[Character] = ["N","o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let str_labValue:String = "user/abag model app message add"
fileprivate let str_taskData:String = "N"

/*: "en" :*/
fileprivate let str_errorContent:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let str_kitData:String = "AR"

/*: "es" :*/
fileprivate let str_norTitle:String = "ES"

/*: "pt" :*/
fileprivate let str_loadData:String = "PT"

/*: "GJ21FB" :*/
fileprivate let str_upValue:String = "Gtext21FB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let str_resultData:String = "完成了检"

/*: "接口请求失败" :*/
fileprivate let str_compareName:String = "接口请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let str_backgroundToText:[Character] = ["\u{5b8c}","成","了","检","测",",","\u{9700}","要","更","换","s","e","r","v"]
fileprivate let str_cellName:String = "size"
fileprivate let str_cleanData:String = "r urlinside more"

/*: "HTTPProxy" :*/
fileprivate let str_resourceLabelToValue:String = "HeditPP"

/*: "__SCOPED__" :*/
fileprivate let str_closeData:String = "__SCOPimage transform self tab if"
fileprivate let str_collectionText:String = "row let title elseED__"

/*: "tap" :*/
fileprivate let str_callData:String = "contentp"

/*: "tun" :*/
fileprivate let str_styleData:String = "handleun"

/*: "ipsec" :*/
fileprivate let str_emptyRecordData:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let str_byData:String = "viewp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let str_layerTitle:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e"]
fileprivate let str_tableAspectTitle:String = "Talkiof view content first"
fileprivate let str_errorInnerValue:String = "add post labelokies = "

/*: "https:// :*/
fileprivate let str_nameContent:String = "hdatap"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let str_imageTitle:[UInt8] = [0x92,0x81,0x9a,0x97,0xab,0xab,0x90,0x91,0x98,0x91,0x80,0x91,0xa7,0x95,0x99,0x91,0xb7,0x9b,0x9b,0x9f,0x9d,0x91,0x87,0xa3,0x9d,0x80,0x9c,0xa1,0x86,0x98,0xd4,0xc9,0xd4]

/*: "UID" :*/
fileprivate let str_collectionValue:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let str_blockValue:String = "phpse"

/*: "func__saveAllCookies =  :*/
fileprivate let str_mediumName:String = "func__found path table request head"
fileprivate let str_upGiftName:[Character] = ["A","l","l","C","o"]
fileprivate let str_textDownData:String = "use if return button extensionokies = "

/*: "func__saveAllCookies  error" :*/
fileprivate let str_inputAttentionValue:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o","k","i","e","s"]
fileprivate let str_viewStackValue:[Character] = [" "," ","e","r","r","o","r"]

/*: "🍪 删除 :*/
fileprivate let str_targetValue:String = "🍪 \u{5220}\u{9664}"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SendUpReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit

//: import WebKit
import WebKit

//: enum LangType: String {
enum EditTermConvertible: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class SendUpReactiveCompatible: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = SendUpReactiveCompatible()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        analogDigitalConverterData()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(str_dataName.prefix(7)))) || newValue.hasPrefix((String(str_goldValue))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kLet_recordErrName && !kLet_senseName {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_liveData.prefix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_liveData.prefix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    necessary(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func analogDigitalConverterData() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = kLet_turnPartyValue.integer(forKey: kLet_useValue)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (str_levelData.replacingOccurrences(of: "match", with: "se") + "erSe" + String(str_contentData)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = toUrl()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_dataName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_recordErrName && !kLet_senseName {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_dataName.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(str_insideValue) + str_modelName.capitalized) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func toUrl() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func photoStr(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(str_backgroundValue)) + "\(kLet_deviceName)" + (String(str_selectionName.suffix(4))), (String(str_backgroundValue)) + "\(kLet_deviceName)" + (String(str_selectionName.suffix(4)))]
    //: }()

    lazy var slowPathArr: Array = {
        return ["IM/getMyBlackList",
                "mf/business/sendMsg",
                "mf/business/sendGift",
                "mf/chatRoom/sendMsg",
                "mf/chatRoom/sendGift",
                "recharge/applePayNotify",
                "user/appleLogin"]
    }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = MacroDefine.getSystemLangCode()
        let lang = UserMacroDefine.lang()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .randomString(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (str_upValue.replacingOccurrences(of: "text", with: "J")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension SendUpReactiveCompatible {
    //: func checkServerUrl() {
    func textQueryUrl() {
        //: if EqualThen.share.networkStatus != .Unavailable {
        if EqualThen.share.networkStatus != .Unavailable {
            //: func_requestHost()
            addPath()
            //: } else {
        } else {
            //: func_starCodeTime()
            sinceActivityTime()
        }
    }

    //: func func_starCodeTime() {
    func sinceActivityTime() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.addPath()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func orate() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func addPath() {
        //: if EqualThen.share.networkStatus == .Unavailable {
        if EqualThen.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        orate()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        GiftManagerRequest.recordHost { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (str_resultData.capitalized + "测,请求状态正常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (str_compareName.capitalized))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.sinceActivityTime()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(str_backgroundToText) + str_cellName.replacingOccurrences(of: "size", with: "e") + String(str_cleanData.prefix(5))))
                    //: self.func__changeHostUrl()
                    self.toEmpty()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func toEmpty() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        orate()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        kLet_turnPartyValue.set(serverSelctIndex, forKey: kLet_useValue)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_dataName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_recordErrName, !kLet_senseName {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_dataName.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension SendUpReactiveCompatible {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func frenchTelephoneProxy() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(str_resourceLabelToValue.replacingOccurrences(of: "edit", with: "TT") + "roxy")] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func startVpn() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(str_closeData.prefix(6)) + String(str_collectionText.suffix(4)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((str_callData.replacingOccurrences(of: "content", with: "ta"))) || keyStr.contains((str_styleData.replacingOccurrences(of: "handle", with: "t"))) || keyStr.contains((String(str_emptyRecordData))) || keyStr.contains((str_byData.replacingOccurrences(of: "view", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension SendUpReactiveCompatible {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func necessary(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(str_layerTitle) + String(str_tableAspectTitle.prefix(5)) + "ngCo" + String(str_errorInnerValue.suffix(8))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = positionHideUrl()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
        var urlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(str_dataName.prefix(7))), with: (String(str_goldValue)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(str_goldValue))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (str_nameContent.replacingOccurrences(of: "data", with: "tt") + "s://") + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            constraintBox(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        album()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func constraintBox(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: str_imageTitle.map{$0^244}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func positionHideUrl() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_dataName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_recordErrName, !kLet_senseName {
            //: serverPrefix = "https://"
            serverPrefix = (String(str_goldValue))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(str_backgroundValue)) + "\(kLet_deviceName)" + (String(str_selectionName.suffix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func viewDown(note _: Notification) {
        //: if EqualThen.share.loginUid.isEmptyString {
        if EqualThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            placeVersion()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            prefer()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func placeVersion() {
        //: var htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_collectionValue)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(str_collectionValue))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_blockValue.uppercased() + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_blockValue.uppercased() + "SSID")] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_collectionValue)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(str_collectionValue))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_blockValue.uppercased() + "SSID") {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(str_blockValue.uppercased() + "SSID")] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(str_collectionValue))), h5Cookie.keys.contains((String(str_collectionValue))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(str_collectionValue))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(str_collectionValue))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((str_blockValue.uppercased() + "SSID")), h5Cookie.keys.contains((str_blockValue.uppercased() + "SSID")) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(str_blockValue.uppercased() + "SSID")]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(str_blockValue.uppercased() + "SSID")]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func prefer() {
        //: let htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
        let htmlDomain: String = EqualThen.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == EqualThen.share.loginUserMode.userID {
            if cookie.name == (String(str_collectionValue)), cookie.value == EqualThen.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (str_blockValue.uppercased() + "SSID") {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_collectionValue)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(str_collectionValue))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_blockValue.uppercased() + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_blockValue.uppercased() + "SSID")] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(str_collectionValue))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(str_collectionValue))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((str_blockValue.uppercased() + "SSID")) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(str_blockValue.uppercased() + "SSID")] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func album() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(str_mediumName.prefix(6)) + "save" + String(str_upGiftName) + String(str_textDownData.suffix(8))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            kLet_turnPartyValue.set(cookieData, forKey: kLet_selectName)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(str_inputAttentionValue) + String(str_viewStackValue)))
        }
    }

    //: func func__setAppCookies() {
    func matter() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = kLet_turnPartyValue.object(forKey: kLet_selectName)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func byItem() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        kLet_turnPartyValue.removeObject(forKey: kLet_selectName)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
