
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let str_dataIncomeTitle:String = "sock"
fileprivate let str_statusName:[Character] = ["R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let str_toText:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4c,0x4c,0x41,0x43,0x54,0x53,0x45,0x55,0x51,0x45,0x52,0x5f,0x54,0x41,0x48,0x43,0x4f,0x45,0x44,0x49,0x56,0x5f,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let str_addValue:[UInt8] = [0xc4,0xd3,0xd5,0xdf,0xc0,0xd3,0xc9,0xd0,0xda,0xd9,0xd7,0xc2,0xc5,0xd5,0xc4,0xd3,0xd3,0xd8,0xc9,0xd8,0xd9,0xc2,0xdf,0xd0,0xdf,0xd5,0xd7,0xc2,0xdf,0xd9,0xd8]

private func imageBottom(sex num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "isCalling" :*/
fileprivate let str_voiceTitle:String = "isCafile normal make share true"

/*: "isTalking" :*/
fileprivate let str_giftUpData:[Character] = ["i"]
fileprivate let str_fillToValue:String = "sTalkingequal space height current"

/*: "SOCKET 连接成功." :*/
fileprivate let str_addResultData:String = "gift true user or alongSOCKET "
fileprivate let str_allData:String = "连\u{63a5}成\u{529f}."

/*: "SOCKET 连接失败, error: :*/
fileprivate let str_mValue:[Character] = ["S","O","C","K","E","T"," ","连","\u{63a5}","失"]
fileprivate let str_lastData:[Character] = ["败",","," ","e","r","r","o","r",":"]

/*: ." :*/
fileprivate let str_momentValue:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let str_dismissPathData:String = "SOpageE"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let str_observerName:String = "\u{6b21}, \u{5207}换"
fileprivate let str_photoSumText:[Character] = ["重"]
fileprivate let str_videoData:[Character] = ["\u{65b0}","\u{5c1d}","\u{8bd5}","\u{ff0c}","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let str_equalContent:String = "SOwindowE"
fileprivate let str_lastName:String = ", code:image model view"

/*: , reason: :*/
fileprivate let str_videoValue:[Character] = [","," ","r","e","a","s","o","n",":"]

/*: "ws:// :*/
fileprivate let str_playerTitleContent:String = "style object kit right disappearws://"

/*: /?sid= :*/
fileprivate let str_imageValue:[Character] = ["/"]
fileprivate let str_appDirectionName:String = "content share at?sid="

/*: &uid= :*/
fileprivate let str_callValue:String = "&uid=view image table let cookie"

/*: &platform=iphone&version= :*/
fileprivate let str_titleData:[Character] = ["&","p","l","a","t","f","o"]
fileprivate let str_tableModelName:String = "layer position info sexrm=ip"
fileprivate let str_listName:String = "self model size share&version="

/*: &packageId= :*/
fileprivate let str_nameData:String = "content add self&p"
fileprivate let str_viewRecordValue:[Character] = ["a","c","k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let str_systemValue:[Character] = ["1","v","1","s","e"]
fileprivate let str_equalValue:[Character] = ["r","v","e","r","."]

/*: .com:7766" :*/
fileprivate let str_colorText:String = ".comake sh true view"
fileprivate let str_byValue:String = "m:7766gender add status"

/*: "43.129.185.194:7767" :*/
fileprivate let str_valueText:String = "43.1"
fileprivate let str_labelTitle:String = "4:addadd6add"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let str_cameraValue:[UInt8] = [0xbb,0xa7,0xab,0xa3,0xad,0xbc,0xc8,0xf,0x55,0x79,0xf,0x53,0x74,0x9a,0x8d,0x89,0x8b,0x80,0x89,0x8a,0x81,0x84,0x81,0x9c,0x91,0xab,0x80,0x89,0x86,0x8f,0x8d,0x8c,0xf,0x73,0x79,0xd,0x78,0x44,0x7,0x54,0x64,0xe,0x7e,0x45,0xf,0x55,0x79,0xd,0x6d,0x5b,0x1,0x7f,0x45,0xc6]

private func userModel(value num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: "cmd" :*/
fileprivate let str_popValue:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let str_buttonName:String = "countng"

/*: "callHeartbeat" :*/
fileprivate let str_youCancelWithName:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let str_faceName:String = "rtitlecititlevtitle"
fileprivate let str_executeLengthName:[Character] = ["d","D","a","t","e"]

/*: "liveHeartBeat" :*/
fileprivate let str_makeViewTimeData:[Character] = ["l","i","v","e","H","e","a","r","t","B"]
fileprivate let str_centerData:String = "epaget"

/*: "🔗 关闭 socket" :*/
fileprivate let str_modelShareModeValue:String = "\u{0001f517} 关\u{95ed} "

/*: "The user logs out" :*/
fileprivate let str_rawValue:[Character] = ["T","h","e"," ","u","s","e","r"," ","l"]
fileprivate let str_managerName:String = "ogs outinfo action outside"

/*: "SOCKET  :*/
fileprivate let str_conversationData:String = "sexO"
fileprivate let str_errorValue:[Character] = ["C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let str_replyValue:[Character] = ["重","连","."," ","重","试"]
fileprivate let str_onlineData:String = "gold数："

/*: , 地址： :*/
fileprivate let str_userNowValue:String = ", 地址\u{ff1a}"

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let str_removeViewTitle:String = "SOCKaddT"
fileprivate let str_styleContent:[Character] = [" ","连","接","\u{6b63}","常"," ","\u{4e0d}","需","要","重","\u{65b0}","\u{8fde}","\u{63a5}","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let str_indexText:String = "do top attach text responseSOCKET 正"
fileprivate let str_centerValue:String = "在连接 不需"

/*: "&resume=1" :*/
fileprivate let str_buttonData:[Character] = ["&","r","e","s","u","m","e","="]
fileprivate let str_equalDataName:String = "show"

/*: "&lang= :*/
fileprivate let str_makeText:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let str_appModeValue:[Character] = ["S","O","C","K","E","T"," ","\u{5f00}"]
fileprivate let str_objectValue:String = "始\u{8fde}接, 地\u{5740}："

/*: "onNetDisabled" :*/
fileprivate let str_headDataName:String = "view greet succeedonNetDi"
fileprivate let str_indexTitle:String = "sableto"

/*: "errno" :*/
fileprivate let str_inviteName:String = "errerror"

/*: "data" :*/
fileprivate let str_replyData:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let str_callContent:[Character] = ["S","O","C","K","E","T"," ","连","接","\u{4fdd}","活"]
fileprivate let str_textValue:String = "cmd: pof response cover"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let str_centerTitle:[UInt8] = [0x46,0x5a,0x56,0x5e,0x50,0x41,0x35,0x61,0x67,0x61,0x76,0xf1,0xaa,0x88,0xf3,0xa1,0xae,0x76,0x78,0x71,0x2f,0x35,0x76,0x74,0x79,0x79,0x5d,0x70,0x74,0x67,0x61,0x77,0x70,0x74,0x61,0x3b]

private func cloudTo(actual num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let str_resultData:[UInt8] = [0x80,0x9c,0x90,0x98,0x96,0x87,0xf3,0xbf,0xba,0xa5,0xb6,0x37,0x6c,0x4e,0x35,0x67,0x68,0xb0,0xbe,0xb7,0xe9,0xf3,0xbf,0xba,0xa5,0xb6,0x9b,0xb6,0xb2,0xa1,0xa7,0x91,0xb6,0xb2,0xa7,0xfd]

private func dataOf(add num: UInt8) -> UInt8 {
    return num ^ 211
}

/*: "onP" :*/
fileprivate let str_atName:String = "onPtext if"

/*: "msg" :*/
fileprivate let str_tableValue:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let str_toName:[Character] = ["🔗"," ","处","理","消","息","：","c","m","d","N","a"]
fileprivate let str_resignTitle:String = "return main popularme:"

/*: , errNo: :*/
fileprivate let str_byText:String = ", erbottom make leading view"

/*: , msg: :*/
fileprivate let str_pathValue:String = "guidance self, msg:"

/*: , data: :*/
fileprivate let str_viewTitle:[Character] = [","," ","d","a","t","a"]
fileprivate let str_addRequestContent:[Character] = [":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let str_birthName:[Character] = ["S","O","C","K","E","T"," ","接","收","\u{5f02}","\u{5e38}","消","息","：","c","m","d","N","a","m","e"]
fileprivate let str_beginData:[Character] = [":"]

/*: "onReceivedGift" :*/
fileprivate let str_viewData:[Character] = ["o","n","R","e","c","e","i"]
fileprivate let str_fromGiftLetData:[Character] = ["v","e"]
fileprivate let str_managerTitle:[Character] = ["d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let str_shareName:String = "min notonVi"

/*: "onRequestCall" :*/
fileprivate let str_modelGiftName:[Character] = ["o","n","R","e","q","u"]
fileprivate let str_drawValue:[Character] = ["e","s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let str_intimateValue:[Character] = ["o","n","C","a","n","c","e","l","C","a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let str_dataValue:String = "onRefudraft self start"
fileprivate let str_enableData:String = "self send button block requestseCall"

/*: "onAcceptCall" :*/
fileprivate let str_backToReturnContent:[Character] = ["o"]
fileprivate let str_modelData:[Character] = ["n","A","c","c","e","p","t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let str_infoVideoValue:String = "onStopCscale manager to super"
fileprivate let str_leadingSkipValue:[Character] = ["a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let str_viewContent:String = "view lab viewfreeCa"
fileprivate let str_selectedVideoData:String = "let label giftllTimes"

/*: "mfCoin" :*/
fileprivate let str_makeValue:String = "mfCoinfor self render and"

/*: "onMissCoin" :*/
fileprivate let str_viewValue:String = "onMiplay value background if container"
fileprivate let str_dismissShareTableText:[Character] = ["s","s","C","o","i","n"]

/*: "acceptCall" :*/
fileprivate let str_containerData:String = "orientationce"
fileprivate let str_showName:[Character] = ["l","l"]

/*: "stopCall" :*/
fileprivate let str_scaleData:String = "stopCto catch try in"
fileprivate let str_contentData:[Character] = ["a","l","l"]

/*: "requestCall" :*/
fileprivate let str_viewTapData:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let str_recordName:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let str_arrayData:String = "TYPE"

/*: "[Invite you to a voice call...]" :*/
fileprivate let str_modelImageColorData:[UInt8] = [0xdb,0xc9,0xee,0xf6,0xe9,0xf4,0xe5,0xa0,0xf9,0xef,0xf5,0xa0,0xf4,0xef,0xa0,0xe1,0xa0,0xf6,0xef,0xe9,0xe3,0xe5,0xa0,0xe3,0xe1,0xec,0xec,0xae,0xae,0xae,0xdd]

private func temporaryWorker(value num: UInt8) -> UInt8 {
    return num ^ 128
}

/*: "uid" :*/
fileprivate let str_addName:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let str_labConstraintData:[Character] = ["n"]
fileprivate let str_keyTitle:[Character] = ["i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let str_limitText:String = "status position stringheadPi"
fileprivate let str_videoSmallElseValue:[Character] = ["c"]

/*: "onCallResume" :*/
fileprivate let str_frameSizeTitle:String = "addn"
fileprivate let str_selectedValue:String = "Callvar from self"

/*: "cancelCall" :*/
fileprivate let str_currentText:String = "cavailablece"
fileprivate let str_kitTitle:String = "lCallmake let"

/*: "refuseCall" :*/
fileprivate let str_resultCrushName:String = "REFUS"

/*: "onCallMotivation" :*/
fileprivate let str_withValue:String = "onCaldate bind color change"
fileprivate let str_localTitle:String = "ivaend"
fileprivate let str_messageText:String = "ijump"

/*: "onLiveWarning" :*/
fileprivate let str_interestText:[Character] = ["o","n","L","i","v","e"]
fileprivate let str_rangeValue:[Character] = ["W","a","r","n","i","n","g"]

/*: "reason" :*/
fileprivate let str_databaseName:String = "managereason"

/*: "time" :*/
fileprivate let str_languageDetailValue:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let str_conditionContent:String = "empty finish viewonSto"
fileprivate let str_rankTitle:[Character] = ["p","L","i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let str_pageName:[Character] = ["o","n","S","e","n","d","P","r","o","g","r","a","m"]
fileprivate let str_meName:String = "red column fillGift"

/*: "onGameFs" :*/
fileprivate let str_modelValue:String = "kit else official detailonGame"
fileprivate let str_shareData:String = "Fsmutual page var succeed"

/*: "broadStatus" :*/
fileprivate let str_outputName:String = "BROA"
fileprivate let str_upData:String = "US"

/*: "onCallChatMsg" :*/
fileprivate let str_hostName:[Character] = ["o","n","C","a","l","l","C","h"]
fileprivate let str_colorData:String = "atMsgtext required tool type"

/*: "onNotificationPopupWin" :*/
fileprivate let str_labValue:String = "onNofatal super"
fileprivate let str_imageMaxValue:String = "cadetailio"

/*: "onUserBlock" :*/
fileprivate let str_appToValue:[Character] = ["o","n","U","s","e","r","B","l","o","c"]
fileprivate let str_timeData:[Character] = ["k"]

/*: "freeCallWin" :*/
fileprivate let str_bubbleData:String = "freeCwill make than"
fileprivate let str_backgroundValue:String = "actual"
fileprivate let str_arrayName:String = "llWinvalue if"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DeviceSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import UIKit
import UIKit

//: import SocketRocket
import SocketRocket

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let kLet_maxData = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let kLet_messageListContent = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let kLet_imageName = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let kLet_domainTitle = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kLet_tableTitle = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let kLet_barValue = (str_dataIncomeTitle.uppercased() + "ET_ADD" + String(str_statusName))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let kLet_fileTitle = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kLet_buttonValue = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let kLet_closeName = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let kLet_hiValue = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let kLet_packageErrorMarginData = NSNotification.Name(rawValue: String(bytes: str_toText.reversed(), encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let kLet_timeData = NSNotification.Name(rawValue: String(bytes: str_addValue.map{imageBottom(sex: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol FileCurrentErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func constraintInsert(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol ModelObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func finishData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func makeList(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol GrantCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func leadIn(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func centerData(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func loungeTouch(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func signature(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol HeadingPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func sectionSocket(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func outsideSize(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func coinLocal(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class DeviceSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum IndexEqual: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: FileReactiveCompatible.shared.direction == .rightToLeft ? 0 : kLet_cameraValue,
        //: y: StatusBarHeight+190
        y: kLet_timeSenseValue + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: FileReactiveCompatible.shared.direction == .rightToLeft ? 0 : kLet_cameraValue,
        //: y: StatusBarHeight+190
        y: kLet_timeSenseValue + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: FileReactiveCompatible.shared.direction == .rightToLeft ? 0 : kLet_cameraValue,
        //: y: StatusBarHeight+500
        y: kLet_timeSenseValue + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: FileCurrentErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: ModelObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: GrantCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: HeadingPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTalking = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = DeviceSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(pushView(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(titleWebsocket),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(str_voiceTitle.prefix(4)) + "lling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (CellTalkingApplication.shared as! CellTalkingApplication).destroyUponTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, (String(str_giftUpData) + String(str_fillToValue.prefix(8))))
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (CellTalkingApplication.shared as! CellTalkingApplication).destroyUponTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension DeviceSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 连接成功.")
        DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_addResultData.suffix(7)) + str_allData))
        //: sendPing()
        collideWithName()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_deviceContent,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: kLet_barValue)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        mini()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_mValue) + String(str_lastData)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = EqualThen.share.reachability?.connection, status != .unavailable else {
        guard let status = EqualThen.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == kLet_domainTitle {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = managerRetry(isRetry: true)
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            DeepKeyTamperReactiveCompatible.indexView(msg: (str_dismissPathData.replacingOccurrences(of: "page", with: "CK") + "T 失败") + "\(kLet_domainTitle)" + (str_observerName + "过Socket域名" + String(str_photoSumText) + String(str_videoData)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.plume(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.targetShared(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        DeepKeyTamperReactiveCompatible.indexView(msg: (str_equalContent.replacingOccurrences(of: "window", with: "CK") + "T 已关闭" + String(str_lastName.prefix(7))) + "\(code)" + (String(str_videoValue)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.circumferentOut(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == kLet_hiValue {
            //: return
            return
        }
        //: if let status = EqualThen.share.reachability?.connection, status != .unavailable {
        if let status = EqualThen.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.plume(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func managerRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = EqualThen.share.appConfigMode.ws
        self.linkAddrArr = EqualThen.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = inputMessage()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: kLet_barValue)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = EqualThen.share.loginUid
        let uid = EqualThen.share.loginUid
        //: let sid = EqualThen.share.loginSessionId
        let sid = EqualThen.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(str_imageValue) + String(str_appDirectionName.suffix(5))) + "\(sid)" + (String(str_callValue.prefix(5))) + "\(uid)" + (String(str_titleData) + String(str_tableModelName.suffix(5)) + "hone" + String(str_listName.suffix(9))) + "\(kLet_descriptionData)" + (String(str_nameData.suffix(2)) + String(str_viewRecordValue)) + "\(kLet_playPageValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func inputMessage() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return kLet_recordErrName ? [(String(str_systemValue) + String(str_equalValue)) + "\(kLet_deviceName)" + (String(str_colorText.prefix(3)) + String(str_byValue.prefix(6)))] : [(str_valueText.capitalized + "29.185.19" + str_labelTitle.replacingOccurrences(of: "add", with: "7"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension DeviceSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func pushView(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            DeepKeyTamperReactiveCompatible.indexView(msg: String(bytes: str_cameraValue.map{userModel(value: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.circumferentOut(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.plume(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func titleWebsocket() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.plume(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.enableData()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension DeviceSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func collideWithName() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: kLet_maxData, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.noCurrent(info: [(String(str_popValue)): (str_buttonName.replacingOccurrences(of: "count", with: "pi"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func clickAt() {
        //: endTalkingPing()
        panelWith()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: kLet_messageListContent, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTalking || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTalking || (self.infoController()?.isKind(of: MakeCallerDelegate.self) == false && self.infoController()?.isKind(of: PtolemaicSystemViewController.self) == false) && (self.currTalkingVC?.isKind(of: MakeCallerDelegate.self) == false && self.currTalkingVC?.isKind(of: PtolemaicSystemViewController.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.noCurrent(info: [(String(str_popValue)): (String(str_youCancelWithName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func panelWith() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func enableData() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(str_faceName.replacingOccurrences(of: "title", with: "e") + String(str_executeLengthName))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < kLet_errorValue {
                //: EqualThen.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                EqualThen.share.sportsSection(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension DeviceSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func runNumbero() {
        //: endLivePing()
        listLive()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: kLet_imageName, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.noCurrent(info: [(String(str_popValue)): (String(str_makeViewTimeData) + str_centerData.replacingOccurrences(of: "page", with: "a"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func listLive() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension DeviceSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func mini() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = GiftReactiveCompatible()
        //: let keyWindow = MacroDefine.getWindow()
        let keyWindow = UserMacroDefine.toUp()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTalking
        self.containerView?.isHidden = !self.isTalking
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func equalData() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard UserPushListener.sexApp().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard ViewRoomManager.astatine().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension DeviceSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func circumferentOut(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (str_modelShareModeValue + "socket"))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: kLet_hiValue, reason: (String(str_rawValue) + String(str_managerName.prefix(7))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        panelWith()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            equalData()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func modelSize() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = DeviceSocketDelegate.shared.managerRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.plume(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func plume(timeInterval: TimeInterval, type: IndexEqual) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            DeepKeyTamperReactiveCompatible.indexView(msg: (str_conversationData.replacingOccurrences(of: "sex", with: "S") + String(str_errorValue)) + "\(type.rawValue)" + (String(str_replyValue) + str_onlineData.replacingOccurrences(of: "gold", with: "次")) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.socket(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func socket(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 连接正常 不需要重新连接.")
                DeepKeyTamperReactiveCompatible.indexView(msg: (str_removeViewTitle.replacingOccurrences(of: "add", with: "E") + String(str_styleContent)))
                //: } else {
            } else {
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 正在连接 不需要重新连接.")
                DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_indexText.suffix(8)) + str_centerValue.capitalized + "要重新\u{8fde}\u{63a5}."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTalking {
        if self.isTalking {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(str_buttonData) + str_equalDataName.replacingOccurrences(of: "show", with: "1")))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(str_makeText)) + "\(SendUpReactiveCompatible.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        circumferentOut(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kLet_tableTitle
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 开始连接, 地址：\(newStr).")
        DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_appModeValue) + str_objectValue) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func noCurrent(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if EqualThen.share.reachability?.connection == nil || EqualThen.share.reachability?.connection == .unavailable {
        if EqualThen.share.reachability?.connection == nil || EqualThen.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(str_popValue)): (String(str_headDataName.suffix(7)) + str_indexTitle.replacingOccurrences(of: "to", with: "d")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (str_inviteName.replacingOccurrences(of: "error", with: "no")): "\(kLet_closeName)",
                                       //: "data": [:]]
                                       (String(str_replyData)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            targetShared(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(str_popValue))] as? String == (str_buttonName.replacingOccurrences(of: "count", with: "pi")) {
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 连接保活cmd: ping.")
                DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_callContent) + String(str_textValue.prefix(6)) + "ing."))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(str_popValue))] as? String == (String(str_youCancelWithName)) {
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                DeepKeyTamperReactiveCompatible.indexView(msg: String(bytes: str_centerTitle.map{cloudTo(actual: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(str_popValue))] as? String == (String(str_makeViewTimeData) + str_centerData.replacingOccurrences(of: "page", with: "a")) {
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET live保活cmd: liveHeartBeat.")
                DeepKeyTamperReactiveCompatible.indexView(msg: String(bytes: str_resultData.map{dataOf(add: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(str_popValue))] as? String == (str_buttonName.replacingOccurrences(of: "count", with: "pi")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(str_popValue)): (String(str_atName.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (str_inviteName.replacingOccurrences(of: "error", with: "no")): "\(kLet_fileTitle)",
                                           //: "data": [:]]
                                           (String(str_replyData)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                targetShared(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.plume(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func targetShared(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(str_popValue))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(str_inviteName.replacingOccurrences(of: "error", with: "no"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(str_tableValue))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(str_replyData))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(str_toName) + String(str_resignTitle.suffix(3))) + "\(cmdName)" + (String(str_byText.prefix(4)) + "rNo:") + "\(errorNo)" + (String(str_pathValue.suffix(6))) + "\(msg)" + (String(str_viewTitle) + String(str_addRequestContent)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        equalLive(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_birthName) + String(str_beginData)) + "\(cmdName)" + (String(str_byText.prefix(4)) + "rNo:") + "\(errorNo)" + (String(str_pathValue.suffix(6))) + "\(msg)" + (String(str_viewTitle) + String(str_addRequestContent)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: EqualThen.share.loginUserMode.freeCallTimes = 0
                EqualThen.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !EqualThen.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !EqualThen.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                PlayerWindowManager.shared.visageMakeUpwardChangeFormWindow()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.constraintInsert(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(str_viewData) + String(str_fromGiftLetData) + String(str_managerTitle)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kLet_marginValue, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(str_shareName.suffix(4)) + "deoMatch") { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.finishData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(str_modelGiftName) + String(str_drawValue)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.makeList(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(str_intimateValue)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(str_dataValue.prefix(6)) + String(str_enableData.suffix(6))) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(str_backToReturnContent) + String(str_modelData)) { // 接收方发送acceptCall回执
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            clickAt()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.outsideSize(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(str_infoVideoValue.prefix(7)) + String(str_leadingSkipValue)) { // 接收方发送stopCall回执
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            panelWith()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(str_viewContent.suffix(6)) + String(str_selectedVideoData.suffix(7)))] as? Int {
                //: EqualThen.share.loginUserMode.freeCallTimes = freeCallTimes
                EqualThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: EqualThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            EqualThen.share.loginUserMode.mf_coin = json[(String(str_replyData))][(String(str_makeValue.prefix(6)))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_postValue, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (CellTalkingApplication.shared as! CellTalkingApplication).femaleAfterDescribe()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(str_viewValue.prefix(4)) + String(str_dismissShareTableText)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.signature(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (str_containerData.replacingOccurrences(of: "orientation", with: "ac") + "ptCa" + String(str_showName)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            clickAt()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.leadIn(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(str_scaleData.prefix(5)) + String(str_contentData)) { // 接收方/拨打方 挂断了通话
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            panelWith()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.centerData(data: data)
            // 更新余额
            //: EqualThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            EqualThen.share.loginUserMode.mf_coin = json[(String(str_replyData))][(String(str_makeValue.prefix(6)))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            indexRender(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_postValue, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (CellTalkingApplication.shared as! CellTalkingApplication).femaleAfterDescribe()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(str_viewTapData)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_packageErrorMarginData, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: EqualThen.share.recieve1v1TalkRequest(info: data)
                EqualThen.share.sportsSection(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(str_faceName.replacingOccurrences(of: "title", with: "e") + String(str_executeLengthName))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: str_recordName.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(str_replyData))][(str_arrayData.lowercased())].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: str_modelImageColorData.map{temporaryWorker(value: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                MakeDelegateHelper.magnitudeeractionUrl(uid: String(json[(String(str_replyData))][(String(str_addName))].intValue),
                                                       //: title: data["nickname"] as? String,
                                                       title: data[(String(str_labConstraintData) + String(str_keyTitle))] as? String,
                                                       //: body: body,
                                                       body: body,
                                                       //: imageUrl: data["headPic"] as? String)
                                                       imageUrl: data[(String(str_limitText.suffix(6)) + String(str_videoSmallElseValue))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                MakeDelegateHelper.shared.launchBackPlay()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (str_frameSizeTitle.replacingOccurrences(of: "add", with: "o") + String(str_selectedValue.prefix(4)) + "Resume") { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            clickAt()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(str_frameSizeTitle.replacingOccurrences(of: "add", with: "o") + String(str_selectedValue.prefix(4)) + "Resume")] = "1"
            //: EqualThen.share.recieve1v1TalkResumeRequest(info: resumeData)
            EqualThen.share.failure(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (CellTalkingApplication.shared as! CellTalkingApplication).destroyUponTimer()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (str_currentText.replacingOccurrences(of: "available", with: "an") + String(str_kitTitle.prefix(5))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            panelWith()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.loungeTouch(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                MakeDelegateHelper.addLab(identifier: String(json[(String(str_replyData))][(String(str_addName))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                MakeDelegateHelper.shared.conk()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (str_resultCrushName.lowercased() + "eCall") { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            panelWith()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.sectionSocket(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(str_withValue.prefix(5)) + "lMot" + str_localTitle.replacingOccurrences(of: "end", with: "t") + str_messageText.replacingOccurrences(of: "jump", with: "on")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if EqualThen.share.loginUserMode.sex == Gender.female.rawValue {
            if EqualThen.share.loginUserMode.sex == RunReplaceableCollection.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.coinLocal?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(str_interestText) + String(str_rangeValue)) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            UserPushListener.scoreEnable(content: msg, reason: json[(String(str_replyData))][(str_databaseName.replacingOccurrences(of: "manager", with: "r"))].stringValue, time: json[(String(str_replyData))][(String(str_languageDetailValue))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(str_conditionContent.suffix(5)) + String(str_rankTitle)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            UserPushListener.sexApp().viewLodge()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(str_pageName) + String(str_meName.suffix(4))) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.fileFirst(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(str_modelValue.suffix(6)) + String(str_shareData.prefix(2))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(str_outputName.lowercased() + "dStat" + str_upData.lowercased())] as? Int ?? -1
            //: if EqualThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_timeData, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kLet_marginValue, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_timeData, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(str_hostName) + String(str_colorData.prefix(5))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            TextDanmuManager.averageCost().radical(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(str_labValue.prefix(4)) + "tifi" + str_imageMaxValue.replacingOccurrences(of: "detail", with: "t") + "nPopupWin") { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            PlayerWindowManager.shared.noPop(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(str_appToValue) + String(str_timeData)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            UserPushListener.sexApp().viewLodge()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ViewRoomManager.astatine().fastening()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: kLet_useName, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension DeviceSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func equalLive(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(str_viewTapData)) || cmdName == (String(str_modelGiftName) + String(str_drawValue)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if UserPushListener.sexApp().isLive == true {
                //: NEED_RESTORE_LIVE = true
                kLet_soundInformationContent = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                UserPushListener.variantSize(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                UserPushListener.sexApp().viewLodge()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard kLet_soundInformationContent == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (str_currentText.replacingOccurrences(of: "available", with: "an") + String(str_kitTitle.prefix(5))) || cmdName == (String(str_intimateValue)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (str_resultCrushName.lowercased() + "eCall") || cmdName == (String(str_dataValue.prefix(6)) + String(str_enableData.suffix(6))) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(str_scaleData.prefix(5)) + String(str_contentData)) || cmdName == (String(str_infoVideoValue.prefix(7)) + String(str_leadingSkipValue))
        {
            //: NEED_RESTORE_LIVE = false
            kLet_soundInformationContent = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = RudeView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                UserPushListener.sexApp().when()
            }
            //: alert.show()
            alert.subjectMatterSchedule()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func indexRender(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(str_viewContent.suffix(6)) + String(str_selectedVideoData.suffix(7)))] as? Int {
            //: EqualThen.share.loginUserMode.freeCallTimes = freeCallTimes
            EqualThen.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(str_bubbleData.prefix(5)) + str_backgroundValue.replacingOccurrences(of: "actual", with: "a") + String(str_arrayName.prefix(5)))] as? Bool
        //: if freeCallWin == true && EqualThen.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, EqualThen.share.loginUserMode.sex == RunReplaceableCollection.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = FreeView()
            //: view.show()
            view.eachValue()
            //: view.setShowData(data: data)
            view.currentData(data: data)
        }
    }
}
