
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let str_actualValue:String = "segment if string self letmf/bu"
fileprivate let str_fromName:String = "ftListphoto net"

/*: "type" :*/
fileprivate let str_toContent:[UInt8] = [0x11,0x1c,0x15,0x0]

private func tableDescribe(white num: UInt8) -> UInt8 {
    return num ^ 101
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let str_freeTitle:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/"]
fileprivate let str_nameLeadingData:[Character] = ["g","i","f","t","L","i","s","t"]

/*: "live/giftList" :*/
fileprivate let str_attachData:String = "live/gifhidden remove equal size add"
fileprivate let str_buttonValue:String = "layer if return cantList"

/*: "party/giftList" :*/
fileprivate let str_quoteValue:String = "party/size color render"

/*: "IM/getMessageList" :*/
fileprivate let str_fatalData:[Character] = ["I","M","/","g","e"]
fileprivate let str_modelText:[Character] = ["t","M","e","s","s","a","g","e","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let str_frameCollectionName:String = "height gift hidden to casemf/bus"
fileprivate let str_showEqualToValue:String = "equal extension accounting leading selfsendM"
fileprivate let str_titleData:String = "stouch"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_positionInviteViewName:String = "mf/chaview height interaction"
fileprivate let str_ofEffectValue:String = "/sendMhidden cover equal with add"
fileprivate let str_scaleAddTitle:String = "estimated"

/*: "mf/business/sendGift" :*/
fileprivate let str_managerName:String = "mf/bcall cell"
fileprivate let str_deviceBeginValue:String = "native if pan finishss/se"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_ofData:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let str_toData:String = "om/sempty photo"

/*: "live/sendGift" :*/
fileprivate let str_sizeName:String = "live/text return return"
fileprivate let str_actionName:String = "digiten"
fileprivate let str_viewData:[Character] = ["d","G","i","f","t"]

/*: "party/sendGift" :*/
fileprivate let str_colorValue:[Character] = ["p","a","r","t","y","/"]
fileprivate let str_appTitle:String = "manager"
fileprivate let str_clearData:[Character] = ["e","n","d","G","i","f","t"]

/*: "mf/user/getChatInfo" :*/
fileprivate let str_cropValue:String = "in if entermf/use"
fileprivate let str_selectedAllName:String = "ChatInfoview type fatal up"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let str_sexRefData:[Character] = ["I","M","/","g","e","t","M"]
fileprivate let str_topServerData:String = "ESSAG"
fileprivate let str_signName:[Character] = ["e","U","s","e","r","I","n","f","o","L","i","s","t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let str_kitName:String = "mf/cruback kit"
fileprivate let str_equalValue:String = "LikeMeLiimage value left manager make"
fileprivate let str_runData:[Character] = ["s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let str_requestName:String = "view returnmf/crush"
fileprivate let str_resultValue:[Character] = ["/","i","g","n","o","r","e"]

/*: "mf/crush/like" :*/
fileprivate let str_directionRawData:[Character] = ["m","f","/","c","r","u","s","h","/","l","i","k","e"]

/*: "mf/user/getRelation" :*/
fileprivate let str_imageContent:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","R","e","l"]
fileprivate let str_makeValueData:[Character] = ["a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let str_colorPushViewData:[UInt8] = [0x22,0x24,0x32,0x25,0x78,0x36,0x23,0x23,0x32,0x39,0x23,0x3e,0x38,0x39]

private func goOn(video num: UInt8) -> UInt8 {
    return num ^ 87
}

/*: "user/removeAttention" :*/
fileprivate let str_addData:String = "user/collection name var"
fileprivate let str_scaleName:String = "normal in limit up militaryAtte"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let str_managerText:[UInt8] = [0x6,0x24,0x2b,0x62,0x31,0x65,0x20,0x24,0x37,0x2b,0x65,0x35,0x2a,0x2c,0x2b,0x31,0x36,0x65,0x2c,0x23,0x65,0x3c,0x2a,0x30,0x65,0x23,0x2a,0x29,0x29,0x2a,0x32,0x65,0x20,0x24,0x26,0x2d,0x65,0x2a,0x31,0x2d,0x20,0x37,0x3b]

/*: "follow Success" :*/
fileprivate let str_requestLabelValue:String = "liveollow"
fileprivate let str_insertData:String = " Sucmodel time var"

/*: "unfollow Success" :*/
fileprivate let str_placeName:String = "unfocell"
fileprivate let str_limitValue:String = "text"
fileprivate let str_selectedValue:String = "CCESS"

/*: "translate/msg" :*/
fileprivate let str_whiteData:[Character] = ["t","r","a","n","s","l","a","t","e","/","m","s"]
fileprivate let str_appearTopText:[Character] = ["g"]

/*: "msgId" :*/
fileprivate let str_textData:[UInt8] = [0xaf,0xb1,0xa5,0x8b,0xa6]

private func modelAt(bottom num: UInt8) -> UInt8 {
    return num ^ 194
}

/*: "targetLng" :*/
fileprivate let str_userText:[UInt8] = [0x2e,0x3b,0x28,0x3d,0x3f,0x2e,0x16,0x34,0x3d]

private func blockArray(mini num: UInt8) -> UInt8 {
    return num ^ 90
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let str_textName:String = "if self elsemf/g"
fileprivate let str_arrayName:String = "etAutoshared he"
fileprivate let str_pathEffectValue:String = "progress others forCont"
fileprivate let str_atCameraName:String = "epict"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let str_rowValue:String = "mf/grpath raw"
fileprivate let str_tagPathViewData:String = "etInticontent left read mode for"
fileprivate let str_backgroundValue:String = "tselected"

/*: "toUid" :*/
fileprivate let str_makeData:[UInt8] = [0x36,0x2d,0x17,0x2b,0x26]

private func targetWithSucceed(app num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "mf/chatRoom/info" :*/
fileprivate let str_listLimitName:String = "image let make target sizemf/cha"
fileprivate let str_labelName:[Character] = ["f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let str_eventValue:String = "source player withmf/ch"
fileprivate let str_appName:String = "m/listroom to gift"

/*: "roomId" :*/
fileprivate let str_momentContent:[UInt8] = [0x79,0x64,0x64,0x66,0x42,0x6f]

private func fileName(line num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "needExtraInfo" :*/
fileprivate let str_liveValue:[UInt8] = [0x2e,0x25,0x25,0x24,0x5,0x38,0x34,0x32,0x21,0x9,0x2e,0x26,0x2f]

/*: "mf/chatRoom/check" :*/
fileprivate let str_playerData:String = "mf/cin cell bean leading with"
fileprivate let str_createTextData:String = "oom/self model list control data"
fileprivate let str_itemData:String = "tipeck"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let str_managerRequestData:String = "target button succeed the iconmf/c"
fileprivate let str_noValue:[Character] = ["/","j","o","i","n","O","r","Q","u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let str_objectName:String = "value self videomf/ch"
fileprivate let str_pushModelData:String = "to gift elseom/m"
fileprivate let str_toLevelValue:[Character] = ["e","m","b","e","r","s"]

/*: "translate/text" :*/
fileprivate let str_sizeValue:[Character] = ["t","r","a","n","s","l","a","t","e"]
fileprivate let str_viewValue:String = "last view/text"

/*: "text" :*/
fileprivate let str_sectionPackageMakeValue:[UInt8] = [0x22,0x33,0x2e,0x22]

private func imageAccept(connection num: UInt8) -> UInt8 {
    return num ^ 86
}

/*: "mf/user/queryStatus" :*/
fileprivate let str_modeTitle:String = "current text aspect self sizemf/us"
fileprivate let str_styleValue:String = "video value coloreryStatus"

/*: "toUids" :*/
fileprivate let str_managerData:[UInt8] = [0x5a,0x41,0x7b,0x47,0x4a,0x5d]

private func atLarge(limit num: UInt8) -> UInt8 {
    return num ^ 46
}

/*: "call/getCallTab" :*/
fileprivate let str_socialBroadcastContent:String = "need label showcall/ge"
fileprivate let str_popData:String = "target list self in valuetCal"

/*: "mf/user/remark" :*/
fileprivate let str_userTitle:[Character] = ["m","f","/","u","s","e"]
fileprivate let str_homeData:[Character] = ["r","/","r","e","m","a","r","k"]

/*: "content" :*/
fileprivate let str_scaleValue:[UInt8] = [0x94,0xa0,0x9f,0xa5,0x96,0x9f,0xa5]

fileprivate func labInputInfo(view num: UInt8) -> UInt8 {
    let value = Int(num) - 49
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "top" :*/
fileprivate let str_atName:[UInt8] = [0xd2,0xc9,0xd6]

private func knockOffEffect(push num: UInt8) -> UInt8 {
    return num ^ 166
}

/*: "mf/newFeature/likeList" :*/
fileprivate let str_chemicalValue:[Character] = ["m","f","/","n","e","w","F","e","a","t","u","r","e","/","l","i","k","e","L","i"]
fileprivate let str_collectData:String = "sframe"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PostulationReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class PostulationReactiveCompatible: NSObject {
public class PostulationReactiveCompatible: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [SoulCacheModel]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [SoulCacheModel]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func transactionReload(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(str_actualValue.suffix(5)) + "siness/gi" + String(str_fromName.prefix(6)))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: str_toContent.map{tableDescribe(white: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func tableCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(str_freeTitle) + String(str_nameLeadingData))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func joinCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(str_attachData.prefix(8)) + String(str_buttonValue.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func wrap(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (String(str_quoteValue.prefix(6)) + "giftList")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func less(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(str_fatalData) + String(str_modelText))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func bindCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(str_frameCollectionName.suffix(6)) + "iness/" + String(str_showEqualToValue.suffix(5)) + str_titleData.replacingOccurrences(of: "touch", with: "g"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func livingRoom(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(str_positionInviteViewName.prefix(6)) + "tRoom" + String(str_ofEffectValue.prefix(6)) + str_scaleAddTitle.replacingOccurrences(of: "estimated", with: "sg"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func windowTo(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(str_managerName.prefix(4)) + "usine" + String(str_deviceBeginValue.suffix(5)) + "ndGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func bottomModel(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(str_ofData) + String(str_toData.prefix(4)) + "endGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func addMake(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(str_sizeName.prefix(5)) + str_actionName.replacingOccurrences(of: "digit", with: "s") + String(str_viewData))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func clickFrom(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(str_colorValue) + str_appTitle.replacingOccurrences(of: "manager", with: "s") + String(str_clearData))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func doUp(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(str_cropValue.suffix(6)) + "r/get" + String(str_selectedAllName.prefix(8)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func memberImage(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(str_sexRefData) + str_topServerData.lowercased() + String(str_signName))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<SoulCacheModel>.deserialize(from: result as? Array) {
            if let datas = Array<SoulCacheModel>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [SoulCacheModel])
                completion(succeed, datas as! [SoulCacheModel])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func whenCompletion(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        kLet_nextTitle.counteractionClick(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func targetCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(str_kitName.prefix(6)) + "sh/who" + String(str_equalValue.prefix(8)) + String(str_runData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func unitedNationsAgencyWithoutOverdo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(str_requestName.suffix(8)) + String(str_resultValue))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func changeFormLog(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(str_directionRawData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func searchEffect(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(str_imageContent) + String(str_makeValueData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func addParams(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: str_colorPushViewData.map{goOn(video: $0)}, encoding: .utf8)! : (String(str_addData.prefix(5)) + "remove" + String(str_scaleName.suffix(4)) + "ntion")
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if EqualThen.share.loginUserMode.sex == Gender.female.rawValue {
                    if EqualThen.share.loginUserMode.sex == RunReplaceableCollection.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        kLet_inputName.fileFirst(showMsg: String(bytes: str_managerText.map{$0^69}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        kLet_inputName.fileFirst(showMsg: (str_requestLabelValue.replacingOccurrences(of: "live", with: "f") + String(str_insertData.prefix(4)) + "cess").localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    kLet_inputName.fileFirst(showMsg: (str_placeName.replacingOccurrences(of: "cell", with: "l") + "low S" + str_limitValue.replacingOccurrences(of: "text", with: "u") + str_selectedValue.lowercased()).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                kLet_inputName.oversight(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func dismissCompletion(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (String(str_whiteData) + String(str_appearTopText))
        //: reqModel.params = ["msgId": msgId, "targetLng": MacroDefine.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: str_textData.map{modelAt(bottom: $0)}, encoding: .utf8)!: msgId, String(bytes: str_userText.map{blockArray(mini: $0)}, encoding: .utf8)!: UserMacroDefine.lang(), String(bytes: str_toContent.map{tableDescribe(white: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func upEvent(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(str_textName.suffix(4)) + "reet/g" + String(str_arrayName.prefix(6)) + "Greet" + String(str_pathEffectValue.suffix(4)) + str_atCameraName.replacingOccurrences(of: "pic", with: "n"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func modelLine(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(str_rowValue.prefix(5)) + "eet/g" + String(str_tagPathViewData.prefix(6)) + "matePho" + str_backgroundValue.replacingOccurrences(of: "selected", with: "o"))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: str_makeData.map{targetWithSucceed(app: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func chatInfoGetCompletionRoomReply(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(str_listLimitName.suffix(6)) + "tRoom/in" + String(str_labelName))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func listing(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(str_eventValue.suffix(5)) + "atRoo" + String(str_appName.prefix(6)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func borderCollection(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: str_momentContent.map{fileName(line: $0)}, encoding: .utf8)!: roomId, String(bytes: str_liveValue.map{$0^64}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(str_playerData.prefix(4)) + "hatR" + String(str_createTextData.prefix(4)) + str_itemData.replacingOccurrences(of: "tip", with: "ch"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func joinBasicCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(str_managerRequestData.suffix(4)) + "hatRoom" + String(str_noValue))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func statusMagnitudeCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(str_objectName.suffix(5)) + "atRo" + String(str_pushModelData.suffix(4)) + String(str_toLevelValue))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func metricate(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (String(str_sizeValue) + String(str_viewValue.suffix(5)))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: str_sectionPackageMakeValue.map{imageAccept(connection: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func tillMessage(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WithTitleThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(str_modeTitle.suffix(5)) + "er/qu" + String(str_styleValue.suffix(9)))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: str_managerData.map{atLarge(limit: $0)}, encoding: .utf8)!: toUids, String(bytes: str_toContent.map{tableDescribe(white: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func approximationResign(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(str_socialBroadcastContent.suffix(7)) + String(str_popData.suffix(4)) + "lTab")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func informationView(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(str_userTitle) + String(str_homeData))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: str_scaleValue.map{labInputInfo(view: $0)}, encoding: .utf8)!: content, String(bytes: str_atName.map{knockOffEffect(push: $0)}, encoding: .utf8)!: isTop, String(bytes: str_makeData.map{targetWithSucceed(app: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// I Like列表数据
    /// - Parameter completion: 回调
    //: class func req_getILikeList(completion: @escaping FinishBlock) {
    class func titleCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WithTitleThen()
        //: reqModel.requestPath = "mf/newFeature/likeList"
        reqModel.requestPath = (String(str_chemicalValue) + str_collectData.replacingOccurrences(of: "frame", with: "t"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_nextTitle.photoLive(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
