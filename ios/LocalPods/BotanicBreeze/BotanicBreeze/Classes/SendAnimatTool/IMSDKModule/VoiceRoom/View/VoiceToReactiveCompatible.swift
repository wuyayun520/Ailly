
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_maleToValue:[UInt8] = [0x63,0x64,0x63,0x7e,0x22,0x69,0x65,0x6e,0x6f,0x78,0x30,0x23,0x2a,0x62,0x6b,0x79,0x2a,0x64,0x65,0x7e,0x2a,0x68,0x6f,0x6f,0x64,0x2a,0x63,0x67,0x7a,0x66,0x6f,0x67,0x6f,0x64,0x7e,0x6f,0x6e]

private func selfWinding(window num: UInt8) -> UInt8 {
    return num ^ 10
}

/*: "Followers" :*/
fileprivate let str_cornerLineData:[Character] = ["F","o","l","l","o"]
fileprivate let str_valueTitle:[Character] = ["w","e","r","s"]

/*: "Following" :*/
fileprivate let str_tableTitleText:[Character] = ["F","o","l","l","o","w","i","n","g"]

/*: "#E6E6E6" :*/
fileprivate let str_rawTitle:[Character] = ["#","E","6","E","6"]
fileprivate let str_centerValue:[Character] = ["E","6"]

/*: "btn_message_more" :*/
fileprivate let str_nameContent:String = "btn_mshare lab should min"
fileprivate let str_equalData:String = "value new color lefte_more"

/*: "UID:  :*/
fileprivate let str_intervalTextHeightName:String = "let list free text handleUID: "

/*: "level_ :*/
fileprivate let str_videoMakeName:String = "deadline lab viewlevel_"

/*: "Gift" :*/
fileprivate let str_dismissName:[UInt8] = [0x89,0xab,0xa8,0xb6]

fileprivate func contentPlayer(leading num: UInt8) -> UInt8 {
    let value = Int(num) + 190
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Mike" :*/
fileprivate let str_contentValue:[UInt8] = [0x99,0xbd,0xbf,0xb1]

private func fatalRange(make num: UInt8) -> UInt8 {
    return num ^ 212
}

/*: "Follow" :*/
fileprivate let str_mainName:[UInt8] = [0x29,0x0,0x3,0x3,0x0,0x18]

/*: "gift_voice_card" :*/
fileprivate let str_touchData:String = "gifinput"
fileprivate let str_leadingData:[Character] = ["c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let str_makeText:[Character] = ["b","t","n","_","p","a"]
fileprivate let str_moreValue:String = "rty_voequal let app"

/*: "btn_party_voice_pre" :*/
fileprivate let str_currentValue:[Character] = ["b","t","n","_","p","a","r","t","y","_","v","o","i","c","e","_","p"]
fileprivate let str_statusMakeValue:String = "rparty"

/*: "attentionUid" :*/
fileprivate let str_errorPhotoData:[Character] = ["a","t","t","e","n","t","i","o"]
fileprivate let str_keyName:String = "nUidwhite center not cell"

/*: "source" :*/
fileprivate let str_tableTitle:String = "SOURCE"

/*: "Kick out" :*/
fileprivate let str_matchValue:[Character] = ["K","i","c","k"," ","o","u","t"]

/*: "Unmute" :*/
fileprivate let str_titleValueName:[Character] = ["U","n","m","u","t","e"]

/*: "Mute" :*/
fileprivate let str_appearData:[Character] = ["M","u","t","e"]

/*: "Kick off" :*/
fileprivate let str_tempData:[Character] = ["K","i","c","k"," ","o"]
fileprivate let str_layerData:String = "camera"

/*: "Block" :*/
fileprivate let str_userTitle:String = "Blocktype title if self"

/*: "Report" :*/
fileprivate let str_priceText:String = "frame"
fileprivate let str_topValue:[Character] = ["e","p","o","r","t"]

/*: "#F6F6F6" :*/
fileprivate let str_mLogText:String = "#F6F6Fif var auto"
fileprivate let str_ofTempValue:String = "6"

/*: "Confirm to kick out the room?" :*/
fileprivate let str_touchRequestTitle:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," ","k","i","c","k"," ","o","u","t"]
fileprivate let str_messageData:[Character] = [" ","t"]
fileprivate let str_shareTitle:String = "he room?image if equal"

/*: "Cancel" :*/
fileprivate let str_userValue:String = "Canceerror start"
fileprivate let str_imageName:String = "tip"

/*: "Sure" :*/
fileprivate let str_liveData:String = "if comment start main breakSure"

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let str_hitValue:[UInt8] = [0x3f,0x65,0x74,0x75,0x6d,0x20,0x65,0x68,0x74,0x20,0x6c,0x65,0x63,0x6e,0x61,0x63,0x20,0x6f,0x74,0x20,0x74,0x6e,0x61,0x77,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x75,0x73,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x41]

/*: "Confirm" :*/
fileprivate let str_infoName:String = "photo"
fileprivate let str_tempValue:String = "ONFIRM"

/*: "The mute has been lifted~" :*/
fileprivate let str_collectionName:String = "The from my self with sex"
fileprivate let str_withValue:String = "app selected makehas be"
fileprivate let str_clickViewName:[Character] = ["~"]

/*: "Confirm to kick off the seat?" :*/
fileprivate let str_sexValue:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," "]
fileprivate let str_modelTitle:String = "kiappk"
fileprivate let str_numberText:String = "to make view let fatalhe seat?"

/*: "OK" :*/
fileprivate let str_scriptValue:[Character] = ["O","K"]

/*: "Shielding Success" :*/
fileprivate let str_modeImageData:String = "color"
fileprivate let str_iconName:String = "hielmain"
fileprivate let str_nameValue:String = "Successmake country push return model"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VoiceToReactiveCompatible.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol ShadowObjectProtocol: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func changeAtNickname(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func userUid(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func scaleValueTab(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func startUid(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class VoiceToReactiveCompatible: UIView {
    //: var popView: TalkingPopView?
    var popView: NameThen?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: PlayingCardOfMagnitude?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(ViewRoomManager.astatine().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = OccurrentInfoModel()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: ShadowObjectProtocol?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == EqualThen.share.loginUserMode.userID {
        if uid == EqualThen.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        headView()
        //: setupSubViewsConstraint()
        imageJoin()
        //: reqUserCardInfo()
        utilizer()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_maleToValue.map{selfWinding(window: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .pingfang(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.publishView()
        label.textColor = UIColor.publishView()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(stemPop), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .pingfang(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.executeTo()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .pingfang(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.publishView()
        label.textColor = UIColor.publishView()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .publishView()
        lab.textColor = .publishView()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(str_cornerLineData) + String(str_valueTitle)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.pingfang(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .executeTo()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.pingfang(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .publishView()
        lab.textColor = .publishView()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(str_tableTitleText)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.pingfang(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .executeTo()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.pingfang(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(str_rawTitle) + String(str_centerValue)))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.eponymUp(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.eponymUp(name: (String(str_nameContent.prefix(5)) + "essag" + String(str_equalData.suffix(6)))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(shooter), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: UserThen = {
        //: let v = TalkingLiveRoomMuteView()
        let v = UserThen()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension VoiceToReactiveCompatible {
    //: func reqUserCardInfo() {
    func utilizer() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        UserPushListener.viewStack(uid: self.uid, streamerUid: liveUserId, roomId: ViewRoomManager.astatine().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<OccurrentInfoModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.bindWill()
            }
        }
    }

    //: func setUserCardData() {
    func bindWill() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(str_intervalTextHeightName.suffix(5))) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.dataBack(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.eponymUp(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.eponymUp(name: (String(str_videoMakeName.suffix(6))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            beyondSexualLove(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func beyondSexualLove(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: str_dismissName.map{contentPlayer(leading: $0)}, encoding: .utf8)!)
            //: if liveUserId == EqualThen.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == EqualThen.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: str_contentValue.map{fatalRange(make: $0)}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: str_mainName.map{$0^111}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.plaster(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: str_dismissName.map{contentPlayer(leading: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.eponymUp(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.eponymUp(name: (str_touchData.replacingOccurrences(of: "input", with: "t") + "_voice_" + String(str_leadingData))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: str_contentValue.map{fatalRange(make: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.eponymUp(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.eponymUp(name: (String(str_makeText) + String(str_moreValue.prefix(6)) + "ice_nor")), for: .normal)
                //: btn.setImage(UIImage.eponymUp(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.eponymUp(name: (String(str_currentValue) + str_statusMakeValue.replacingOccurrences(of: "party", with: "e"))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.pingfangWithDiameter(fontSize: 15)
                btn.titleLabel?.font = UIFont.pingfangWithDiameter(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(dismissLocation(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(str_rawTitle) + String(str_centerValue)))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func dismissLocation(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: str_dismissName.map{contentPlayer(leading: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.userUid(uid: self.userModel.uid)
            //: self.dismiss()
            self.pinpoint()
        //: case "Mike":
        case String(bytes: str_contentValue.map{fatalRange(make: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.scaleValueTab(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.pinpoint()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.changeAtNickname(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.pinpoint()
        //: case "Follow":
        case String(bytes: str_mainName.map{$0^111}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            untilMedium(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func untilMedium(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        overdo(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.startUid(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func overdo(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(String(str_errorPhotoData) + String(str_keyName.prefix(4)))] = uid
        //: dict["source"] = "2"
        dict[(str_tableTitle.lowercased())] = "2"
        //: PostulationReactiveCompatible.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        PostulationReactiveCompatible.addParams(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func stemPop() {
        //: EditTextThen.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        EditTextThen.share.soulAge(uid: self.userModel.uid)
        //: self.dismiss()
        self.pinpoint()
    }
}

//: extension TalkingVoiceRoomCardView {
extension VoiceToReactiveCompatible {
    //: @objc func moreBtnClick() {
    @objc func shooter() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == EqualThen.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == EqualThen.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(str_matchValue)).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(str_titleValueName)).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(str_appearData)).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(str_tempData) + str_layerData.replacingOccurrences(of: "camera", with: "ff")).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(str_userTitle.prefix(5))).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((str_priceText.replacingOccurrences(of: "frame", with: "R") + String(str_topValue)).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.CustomFile.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = BeautyReactiveCompatible.spellAdd(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(str_mLogText.prefix(6)) + str_ofTempValue.capitalized))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func equidistant() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = NameThen(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: kLet_cameraValue, height: kLet_clickData)
        //: popView?.initWithView(view: self)
        popView?.observe(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? MacroDefine.getWindow())
        popView?.shackUp(view: self.infoController()?.view ?? UserMacroDefine.toUp())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func pinpoint() {
        //: popView?.dismissView()
        popView?.big()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension VoiceToReactiveCompatible: TitleViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func appText(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(str_matchValue)).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            MotilityThen.viewConfig(title: nil, message: (String(str_touchRequestTitle) + String(str_messageData) + String(str_shareTitle.prefix(8))).localized, leftBtnTitle: (String(str_userValue.prefix(5)) + str_imageName.replacingOccurrences(of: "tip", with: "l")).localized, rightBtnTitle: (String(str_liveData.suffix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                ModelReactiveCompatible.fromTarget()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                SurgeryReqTool.imageCompletion(targetUid: self.userModel.uid, roomId: ViewRoomManager.astatine().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    ModelReactiveCompatible.dataConverterCan()
                    //: self.dismiss()
                    self.pinpoint()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(str_titleValueName)).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            MotilityThen.viewConfig(title: nil, message: String(bytes: str_hitValue.reversed(), encoding: .utf8)!.localized, leftBtnTitle: (String(str_userValue.prefix(5)) + str_imageName.replacingOccurrences(of: "tip", with: "l")).localized, rightBtnTitle: (str_infoName.replacingOccurrences(of: "photo", with: "C") + str_tempValue.lowercased()).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                ModelReactiveCompatible.fromTarget()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                SurgeryReqTool.diningRoom(targetUid: self.userModel.uid, roomID: ViewRoomManager.astatine().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    ModelReactiveCompatible.dataConverterCan()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.fileFirst(showMsg: (String(str_collectionName.prefix(4)) + "mute " + String(str_withValue.suffix(6)) + "en lifted" + String(str_clickViewName)).localized)
                    }
                    //: self.dismiss()
                    self.pinpoint()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(str_appearData)).localized: // 设置禁言
            //: muteView.showView()
            muteView.largess()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.pinpoint()

        //: case "Kick off".localized: /// 踢下麦
        case (String(str_tempData) + str_layerData.replacingOccurrences(of: "camera", with: "ff")).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            MotilityThen.viewConfig(title: nil, message: (String(str_sexValue) + str_modelTitle.replacingOccurrences(of: "app", with: "c") + " off t" + String(str_numberText.suffix(8))).localized, leftBtnTitle: (String(str_userValue.prefix(5)) + str_imageName.replacingOccurrences(of: "tip", with: "l")).localized, rightBtnTitle: (String(str_liveData.suffix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                ViewRoomManager.astatine().destroyAction(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.pinpoint()
            }

        //: case "Report".localized:
        case (str_priceText.replacingOccurrences(of: "frame", with: "R") + String(str_topValue)).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = WithSkinViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.withPair(view: nil)
        //: case "Block".localized:
        case (String(str_userTitle.prefix(5))).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            MotilityThen.viewConfig(title: nil, message: kLet_viewNetValue, leftBtnTitle: (String(str_userValue.prefix(5)) + str_imageName.replacingOccurrences(of: "tip", with: "l")).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                MotilityThen.afterward()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                MotilityThen.afterward()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                TitleEqualRequestManager.betweenValue(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.fileFirst(showMsg: (str_modeImageData.replacingOccurrences(of: "color", with: "S") + str_iconName.replacingOccurrences(of: "main", with: "d") + "ing " + String(str_nameValue.prefix(7))).localized)
                        //: self.dismiss()
                        self.pinpoint()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension VoiceToReactiveCompatible {
    // 添加视图
    //: private func setupSubviews() {
    private func headView() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func imageJoin() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(kLet_cameraValue / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-kLet_cameraValue / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(kLet_cameraValue / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.infoController()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.infoController()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.pinpoint()
        }
    }
}
