
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_viewText:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "F5F5F5" :*/
fileprivate let str_messageTitle:String = "Fmapmap5"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let str_sizeValue:String = "style to file list makeTalkin"
fileprivate let str_topData:String = "EX"
fileprivate let str_userSessionContent:String = "player party self title requesttMsgC"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_toolToTitle:[Character] = ["T","a","l","k","i","n"]
fileprivate let str_atColorValue:[Character] = ["g","C","h","a","t","I","m","a","g","e","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let str_insertText:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","V","i","d","e","o","M","s","g","C","e","l","l"]
fileprivate let str_tableData:[Character] = ["I","D"]

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let str_gestureSocialValue:String = "component location in selfTalki"
fileprivate let str_managerValue:[Character] = ["a","t","G","i","f","t","M","s","g","C","e","l","l","I","D"]

/*: "ViewMsgCell" :*/
fileprivate let str_tingMakeCellData:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","s","k","F","o","r","G","i","f","t","M","s","g","C","e","l","l"]

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_picValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i","p","s","M","s","g","C","e"]
fileprivate let str_textName:String = "llIDin send title model of"

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let str_labValue:String = "content to heightTalk"
fileprivate let str_dataValue:String = "like render gift selfhatMat"
fileprivate let str_observerText:String = "view as for lab infogCel"
fileprivate let str_addContent:[Character] = ["l","I","D"]

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let str_managerViewValue:String = "Talkenable if else for"
fileprivate let str_moreName:String = "item viewatAud"
fileprivate let str_modeValue:String = "CellIDcount style image extension color"

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let str_controlName:[UInt8] = [0xa8,0x9d,0x90,0x97,0x95,0x92,0x9b,0xbf,0x94,0x9d,0x88,0xaf,0x85,0x8f,0x88,0x99,0x91,0xb2,0x93,0x88,0x95,0x9a,0xb6,0x89,0x91,0x8c,0xb1,0x8f,0x9b,0xbf,0x99,0x90,0x90,0xb5,0xb8]

private func caseData(progress num: UInt8) -> UInt8 {
    return num ^ 252
}

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_upReplyTitle:String = "TSysteguard top block error"
fileprivate let str_userTitle:String = "scale name userCell_"

/*: "TalkingChatMapMsgCellID" :*/
fileprivate let str_faceInputName:String = "Talkingname center to self"
fileprivate let str_imageData:String = "comment import image leftChat"
fileprivate let str_mapValue:String = "model indexMapMs"

/*: "#128CFF" :*/
fileprivate let str_enterData:[Character] = ["#","1","2","8","C","F"]
fileprivate let str_jumpContent:[Character] = ["F"]

/*: "Feedback" :*/
fileprivate let str_mainValue:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let str_pathValue:String = "value view add temp#8370FF"

/*: "Quick Greeting" :*/
fileprivate let str_whiteData:String = "Quick Greuser user"
fileprivate let str_countItemName:[Character] = ["e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let str_userData:[Character] = ["B","e","a","u","t","y"]
fileprivate let str_nextValue:String = " "
fileprivate let str_videoAddData:[Character] = ["P","h","o","t","o","s"]

/*: "url" :*/
fileprivate let str_quoteValue:String = "URL"

/*: "unlockGift" :*/
fileprivate let str_moreTitle:String = "let conversationunlockGif"
fileprivate let str_pathViewBreakText:String = "T"

/*: "%d" :*/
fileprivate let str_toMoonData:[Character] = ["%","d"]

/*: "isBurn" :*/
fileprivate let str_regularValue:[UInt8] = [0x6e,0x72,0x75,0x42,0x73,0x69]

/*: "giftId" :*/
fileprivate let str_popularTitle:String = "giftIdlabel end"

/*: "rYMsgType" :*/
fileprivate let str_sessionContent:String = "area heightrYMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let str_atData:String = "name scaleGJ:Ca"

/*: "changeDirection" :*/
fileprivate let str_genderTitle:[Character] = ["c","h","a","n","g","e","D","i"]
fileprivate let str_callData:[Character] = ["r","e","c","t","i","o","n"]

/*: "extra" :*/
fileprivate let str_playerContent:String = "planxtra"

/*: "msgInfo" :*/
fileprivate let str_arrayTitle:[Character] = ["m","s","g","I","n","f","o"]

/*: "toUid" :*/
fileprivate let str_locationData:String = "toUiddefine layer top to"

/*: "wantGift" :*/
fileprivate let str_showGiftText:[Character] = ["w","a","n","t","G"]
fileprivate let str_frameData:String = "IFT"

/*: "jumpKey" :*/
fileprivate let str_infoEffectTitle:[Character] = ["j","u"]
fileprivate let str_voiceTitle:[Character] = ["m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let str_makeWithName:String = "truePeinterest let model value"
fileprivate let str_pathTitle:String = "Authsuccess up let group pic"

/*: "logType" :*/
fileprivate let str_directionValue:String = "self block top false buttonlogType"

/*: "msgType" :*/
fileprivate let str_sectionName:[UInt8] = [0xff,0x5,0xf9,0xe6,0xb,0x2,0xf7]

fileprivate func leadingTo(card num: UInt8) -> UInt8 {
    let value = Int(num) - 146
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "txt" :*/
fileprivate let str_makeData:String = "infoxinfo"

/*: "type" :*/
fileprivate let str_constraintData:[Character] = ["t","y","p","e"]

/*: "QuickMsg" :*/
fileprivate let str_moreValue:String = "Quicself user to equal content"

/*: "params" :*/
fileprivate let str_liveValue:[UInt8] = [0x82,0x93,0x80,0x93,0x9f,0x81]

private func centerEnd(value num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "content" :*/
fileprivate let str_topValue:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "id" :*/
fileprivate let str_imageName:String = "ikey"

/*: "txtId" :*/
fileprivate let str_modeFadeData:String = "var scaletxtId"

/*: "message" :*/
fileprivate let str_lastName:[UInt8] = [0xa7,0xaf,0xb9,0xb9,0xab,0xad,0xaf]

/*: "TxtMsg" :*/
fileprivate let str_priceText:[Character] = ["T","x","t","M","s","g"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let str_kindContainerValue:[UInt8] = [0x65,0x72,0x75,0x64,0x65,0x63,0x6f,0x72,0x50,0x20,0x2e,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x64,0x61,0x6f,0x6c,0x6e,0x77,0x6f,0x64,0x20,0x6f,0x6c,0x6c,0x65,0x68,0x20,0x65,0x63,0x69,0x6f,0x56]

/*: "length" :*/
fileprivate let str_addUserName:String = "letopth"

/*: "audio" :*/
fileprivate let str_cellMakeName:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let str_timeVoiceText:String = "audioIdsection target res self"

/*: "audioLength" :*/
fileprivate let str_clearSectionTitle:[UInt8] = [0x68,0x74,0x67,0x6e,0x65,0x4c,0x6f,0x69,0x64,0x75,0x61]

/*: "audioSandbox" :*/
fileprivate let str_playerFillValue:String = "atimedio"
fileprivate let str_lastReplyData:[Character] = ["S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let str_clickName:[UInt8] = [0x7f,0x71,0x7a,0x70,0x71,0x7e,0x55,0x70]

fileprivate func mixUp(camera num: UInt8) -> UInt8 {
    let value = Int(num) - 12
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "AudioMsg" :*/
fileprivate let str_succeedContent:[Character] = ["A","u","d","i","o","M","s"]
fileprivate let str_toName:String = "m"

/*: "Can't send blank message" :*/
fileprivate let str_sizeName:String = "Can\'t "
fileprivate let str_dataContent:String = "true app dismiss icon managersend "
fileprivate let str_rowData:[Character] = [" ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let str_galleryCanValue:[Character] = ["q","u","o","t","e","M"]
fileprivate let str_willValue:String = "sgIdlet kit to let kit"

/*: "quoteMsgInfo" :*/
fileprivate let str_commitValue:String = "quotview"

/*: "contentType" :*/
fileprivate let str_toTouchData:[UInt8] = [0xe5,0xf1,0xf0,0xf6,0xe7,0xf0,0xf6,0xd6,0xfb,0xf2,0xe7]

fileprivate func fullPhaseOfTheMoon(time num: UInt8) -> UInt8 {
    let value = Int(num) + 126
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let str_cellValue:String = "ImgMsgchange size let"

/*: "img" :*/
fileprivate let str_liveTitle:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let str_listData:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "customSend" :*/
fileprivate let str_allText:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let str_equalData:String = "pactionc"

/*: "VideoMsg" :*/
fileprivate let str_pointData:[Character] = ["V","i","d"]
fileprivate let str_labName:[Character] = ["e","o","M","s","g"]

/*: "video" :*/
fileprivate let str_attachContent:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let str_playerName:[UInt8] = [0x6f,0x63,0x7a,0x69,0x7e,0x45,0x61,0x6b]

private func playCellAt(key num: UInt8) -> UInt8 {
    return num ^ 12
}

/*: "videoUrl" :*/
fileprivate let str_renderName:[UInt8] = [0x26,0x19,0x14,0x15,0x1f,0x5,0x22,0x1c]

fileprivate func viewData(next num: UInt8) -> UInt8 {
    let value = Int(num) - 176
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "duration" :*/
fileprivate let str_liveData:[UInt8] = [0xb,0x1c,0x19,0x8,0x1b,0x10,0x16,0x15]

fileprivate func headIndex(item num: UInt8) -> UInt8 {
    let value = Int(num) - 167
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "len" :*/
fileprivate let str_withName:[UInt8] = [0xd8,0xd1,0xda]

private func importSucceed(array num: UInt8) -> UInt8 {
    return num ^ 180
}

/*: "map" :*/
fileprivate let str_sectionValue:String = "maappearance"

/*: "latlon" :*/
fileprivate let str_addBottomBubbleText:[UInt8] = [0x12,0x7,0x1a,0x12,0x15,0x14]

fileprivate func sortDon(center num: UInt8) -> UInt8 {
    let value = Int(num) + 90
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "messageType" :*/
fileprivate let str_tableTitle:[Character] = ["m","e","s","s","a","g","e","T","y"]
fileprivate let str_mediumText:String = "pmodel"

/*: "MapMsg" :*/
fileprivate let str_contentName:String = "to make buttonMapMs"
fileprivate let str_toData:String = "view"

/*: "Documents/User/voice/" :*/
fileprivate let str_userValue:String = "live directionDocu"
fileprivate let str_insideData:String = "return model locations/User"
fileprivate let str_nameToNowValue:String = "/voice/online var self"

/*: "Documents/User/record/" :*/
fileprivate let str_equalPushStackTitle:String = "Docuapp show model show"
fileprivate let str_fileData:String = "hidden makeUser/rec"
fileprivate let str_emptyTitle:[Character] = ["o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let str_labelText:String = "photos"
fileprivate let str_equalMakeData:String = "gIncomeguard next share"

/*: "mfBean" :*/
fileprivate let str_taskToValue:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "WantGift" :*/
fileprivate let str_giftName:String = "self frameWantGift"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let str_videoData:String = "true if color else no_Pri"
fileprivate let str_lowTitle:String = "share more view shouldhatTi"
fileprivate let str_sendName:String = "psTisize self"

/*: "msgId" :*/
fileprivate let str_textPlayerAddName:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_groupValue:String = "redta"
fileprivate let str_viewData:String = "normaln"

/*: "attachSysMsg" :*/
fileprivate let str_transactionValue:String = "firstttfirstch"
fileprivate let str_clearValue:String = "SysMsgtarget var guard"

/*: "intimatePhoto" :*/
fileprivate let str_titleValue:[Character] = ["i","n","t","i","m","a"]
fileprivate let str_rowCameraName:[Character] = ["t","e","P","h","o","t","o"]

/*: "&type= :*/
fileprivate let str_centerData:String = "&type=model image send"

/*: _ :*/
fileprivate let str_bottomName:String = "_"

/*: "totalIntimate" :*/
fileprivate let str_listName:[Character] = ["t","o","t","a","l","I","n","t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let str_photoValue:[UInt8] = [0xba,0xae,0xa9]

fileprivate func centerTime(mode num: UInt8) -> UInt8 {
    let value = Int(num) - 69
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "intimate" :*/
fileprivate let str_viewTitle:[UInt8] = [0xb5,0xb2,0xa8,0xb5,0xb1,0xbd,0xa8,0xb9]

private func streetSmart(m num: UInt8) -> UInt8 {
    return num ^ 220
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let str_actualErrorData:[UInt8] = [0x2e,0x6c,0x65,0x76,0x65,0x6c,0x20,0x79,0x63,0x61,0x6d,0x69,0x74,0x6e,0x69,0x20,0x66,0x6f,0x20,0x6b,0x63,0x61,0x6c,0x20,0x72,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x65,0x75,0x64,0x20,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x74,0x27,0x6e,0x61,0x43]

/*: "giftNum" :*/
fileprivate let str_indexData:[Character] = ["g"]
fileprivate let str_selectedData:String = "empty sound hide var trueiftNum"

/*: "pkgItemsetId" :*/
fileprivate let str_cellData:String = "pkgItemsimage push data self view"
fileprivate let str_viewContent:String = "size mediumetId"

/*: "from" :*/
fileprivate let str_dataToValue:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let str_itemName:String = "W"
fileprivate let str_plusToolValue:[Character] = ["a","n","t","G","i","f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let str_requestValue:String = "array viewWantG"

/*: "gift" :*/
fileprivate let str_inputValue:String = "gifrefuse"

/*: "View info" :*/
fileprivate let str_textServiceTitle:String = "View ish head hidden up"
fileprivate let str_arrayScaleContent:String = "labfo"

/*: "Notes" :*/
fileprivate let str_modelName:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let str_countName:[Character] = ["R","e","p","o","r"]
fileprivate let str_pathName:[Character] = ["t"]

/*: "View read receipts?" :*/
fileprivate let str_toSucceedText:[Character] = ["V","i","e","w"," ","r","e","a","d"]
fileprivate let str_leadingName:[Character] = [" ","r","e","c","e","i","p","t","s","?"]

/*: "He" :*/
fileprivate let str_numberLineText:String = "Heindex else record active model"

/*: "She" :*/
fileprivate let str_eventName:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let str_fullValue:String = "%@ ivalue enable let constraint"
fileprivate let str_bottomData:String = "g too lowreturn back equal gift view"
fileprivate let str_iconValue:String = " a vetype title label"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let str_readContent:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e"]
fileprivate let str_turnName:String = "plain textast "
fileprivate let str_cornerFromInData:String = "context"

/*: "Upload failed" :*/
fileprivate let str_titleName:[Character] = ["U","p","l","o","a","d"," ","f","a","i"]
fileprivate let str_labelVideoContent:String = "lvoice"

/*: "播放状态: :*/
fileprivate let str_lastValue:[Character] = ["播","放","状","态",":"]

/*: "c2c_" :*/
fileprivate let str_errData:[Character] = ["c","2","c","_"]

/*: "msg" :*/
fileprivate let str_giftValue:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let str_addValue:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let str_fileValue:[UInt8] = [0xe7,0xd1,0xcb,0x9e,0xdd,0xdf,0xd0,0x9e,0xcb,0xcd,0xdb,0x9e,0xca,0xd6,0xdb,0x9e,0xc8,0xd7,0xda,0xdb,0xd1,0x9e,0xdd,0xd6,0xdf,0xca,0x9e,0xd8,0xdb,0xdf,0xca,0xcb,0xcc,0xdb,0x9e,0xca,0xd1,0x9e,0xd9,0xd7,0xc8,0xdb,0x9e,0x9b,0xfe,0x9e,0xdf,0x9e,0xdd,0xdf,0xd2,0xd2]

private func cellPic(date num: UInt8) -> UInt8 {
    return num ^ 190
}

/*: "cellData" :*/
fileprivate let str_voiceTempTitle:String = "cellDatgift else"
fileprivate let str_sizeData:String = "line"

/*: "System notification" :*/
fileprivate let str_indexContent:[Character] = ["S","y","s","t","e","m"," ","n","o","t","i","f","i","c","a"]
fileprivate let str_inputTitle:[Character] = ["t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SendReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import UIKit
import UIKit

//: import AVFAudio
import AVFAudio

//: import Photos
import Photos

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ToFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class SendReactiveCompatible: SeamViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = SoulCacheModel()
    private var userWrap = SoulCacheModel() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ToFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: SoulCacheModel = SoulCacheModel()) {
    init(chatID: String, isFrom: ToFromEnum = .Normal, userWrap: SoulCacheModel = SoulCacheModel()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        SendReactiveCompatible.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        FreeReactiveCompatible.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == MacroDefine.getXiaoMiID() {
        if targetId == UserMacroDefine.underGet() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == MacroDefine.getCustomerServiceID() {
        } else if targetId == UserMacroDefine.addTap() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_viewText.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if EqualThen.share.appConfigMode.disableShootScreen {
            if EqualThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CellDataReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_cameraValue, height: kLet_clickData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        weltanschauung()
        //: createUI()
        searchRoom()
        //: createUIConstraints()
        clearUp()
        //: bindInteraction()
        sortCounteraction()
        //: addTapGestureRecognizer()
        compound()
        //: func__requestChatInfo()
        pastPath()
        //: func__getFillDraftText()
        nameTitle()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        ControlRawManagerDelegate.shared.parameter()
        //: func__addVoiceNotification()
        enrich()
        //: addReadPeceiptNotification()
        screen()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            effectBind()
            //: func__TipsNotification()
            ofNotification()
            //: if EqualThen.share.loginUserMode.sex == Gender.male.rawValue {
            if EqualThen.share.loginUserMode.sex == RunReplaceableCollection.male.rawValue {
                //: func__setReplyTipsView()
                replyAppear()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        exaggerateAdd(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        exaggerateAdd(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            freePhone()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (str_messageTitle.replacingOccurrences(of: "map", with: "5F")))
        }
        msgVC.tableView.register(ChatAddView.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(TitleMsgCell.self, forCellReuseIdentifier: (String(str_toolToTitle) + String(str_atColorValue)))
        msgVC.tableView.register(ViewReactiveCompatible.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(QuantityeractionReactiveCompatible.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(ViewMsgCell.self, forCellReuseIdentifier: (String(str_tingMakeCellData)))
        msgVC.tableView.register(BottomThen.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(SaveTalkingThen.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(UserMessageCell.self, forCellReuseIdentifier: (String(str_managerViewValue.prefix(4)) + "ingCh" + String(str_moreName.suffix(5)) + "ioMsg" + String(str_modeValue.prefix(6))))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(ViewRunMsgCell.self, forCellReuseIdentifier: String(bytes: str_controlName.map{caseData(progress: $0)}, encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_upReplyTitle.prefix(6)) + "mMessage" + String(str_userTitle.suffix(5)) + "ReuseId"))

        msgVC.tableView.register(SkinStatusMsgCell.self, forCellReuseIdentifier: "TalkingChatMapMsgCellID")

        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: NameMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = NameMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: DoInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: RunViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = DoInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(str_enterData) + String(str_jumpContent)))!, .font: UIFont.pingfang(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_mainValue)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(derail), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: OfReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = OfReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: PositionReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = PositionReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.pingfang(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_pathValue.suffix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(str_whiteData.prefix(9)) + String(str_countItemName)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.plaster().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(bombUp), for: .touchUpInside)
        //: btn.isHidden = !EqualThen.share.appUserConfigMode.showDirectGreet || EqualThen.share.loginUserMode.sex == Gender.male.rawValue || EqualThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !EqualThen.share.appUserConfigMode.showDirectGreet || EqualThen.share.loginUserMode.sex == RunReplaceableCollection.male.rawValue || EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: AddView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = AddView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: GestureThen = {
        //: let v = TalkingSelectTranslationView()
        let v = GestureThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.bottomView(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: NameView = {
        //: let v = TalkingTranslationResultView()
        let v = NameView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.people(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.pingfang(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_pathValue.suffix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(str_userData) + str_nextValue.capitalized + String(str_videoAddData)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.plaster().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(belowView), for: .touchUpInside)
        //: btn.isHidden = EqualThen.share.loginUserMode.sex == Gender.male.rawValue || EqualThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = EqualThen.share.loginUserMode.sex == RunReplaceableCollection.male.rawValue || EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: TargetTitleGamesView = {
        //: let v = TalkingLiveRoomGamesView()
        let v = TargetTitleGamesView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: SightDataSource = {
        //: let view = TalkingPrivateBannerView.init()
        let view = SightDataSource()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: FileBeautyScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = FileBeautyScreenView()
        //: v.isHidden = (EqualThen.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: UpReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = UpReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.equalStop()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension SendReactiveCompatible: MakeBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func willView(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if EqualThen.share.loginUserMode.sex == Gender.male.rawValue {
                if EqualThen.share.loginUserMode.sex == RunReplaceableCollection.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if EqualThen.share.loginUserMode.sex == Gender.male.rawValue {
                if EqualThen.share.loginUserMode.sex == RunReplaceableCollection.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.mutualView(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension SendReactiveCompatible: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func bombUp() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        FreeReactiveCompatible.tap(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.oversight(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.brandTitle(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func belowView() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        FreeReactiveCompatible.doingArray(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = AddressUpPhotoVc()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().lineBorder(urlStr: dict?[(str_quoteValue.lowercased())] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(str_moreTitle.suffix(9)) + str_pathViewBreakText.lowercased())] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = FromJsonModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: str_regularValue.reversed(), encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(str_popularTitle.prefix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.incrementalCost(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: ShowCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ShowCellData.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(str_sessionContent.suffix(9)))].stringValue == (String(str_atData.suffix(5)) + "llCustom") { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(str_genderTitle) + String(str_callData))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic[(str_playerContent.replacingOccurrences(of: "plan", with: "e"))][(String(str_arrayTitle))][(String(str_locationData.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = SendCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = SendCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: AtHeadCellData.self) == true || cellData.isKind(of: DataReactiveCompatible.self) == true || cellData.isKind(of: SendCellData.self) == true || cellData.isKind(of: ManageCellData.self) == true || cellData.isKind(of: CompartmentSourceCellData.self) == true {
        if cellData.isKind(of: AtHeadCellData.self) == true || cellData.isKind(of: DataReactiveCompatible.self) == true || cellData.isKind(of: SendCellData.self) == true || cellData.isKind(of: ManageCellData.self) == true || cellData.isKind(of: CompartmentSourceCellData.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        FreeReactiveCompatible.accountingData(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        FreeReactiveCompatible.latestReplyFlushAndTimeApp(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = createBar(data: data)

        //: if data.isKind(of: SendCellData.self) {
        if data.isKind(of: SendCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = ChatAddView(style: .default, reuseIdentifier: (String(str_sizeValue.suffix(6)) + "gChatT" + str_topData.lowercased() + String(str_userSessionContent.suffix(5)) + "ellID"))
            //: let cellData = data as! SendCellData
            let cellData = data as! SendCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.coverMsg() {
                //: if cellData.msgModel.quoteMsgInfo.uid == EqualThen.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == EqualThen.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = EqualThen.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = EqualThen.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: DataReactiveCompatible.self) {
        if data.isKind(of: DataReactiveCompatible.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = TitleMsgCell(style: .default, reuseIdentifier: (String(str_toolToTitle) + String(str_atColorValue)))
            //: let cellData = data as! DataReactiveCompatible
            let cellData = data as! DataReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ManageCellData.self) {
        if data.isKind(of: ManageCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = ViewReactiveCompatible(style: .default, reuseIdentifier: (String(str_insertText) + String(str_tableData)))
            //: let cellData = data as! ManageCellData
            let cellData = data as! ManageCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SoulCellData.self) {
        if data.isKind(of: SoulCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = QuantityeractionReactiveCompatible(style: .default, reuseIdentifier: (String(str_gestureSocialValue.suffix(5)) + "ngCh" + String(str_managerValue)))
            //: let cellData = data as! SoulCellData
            let cellData = data as! SoulCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: PlayReactiveCompatible.self) {
        if data.isKind(of: PlayReactiveCompatible.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = ViewMsgCell(style: .default, reuseIdentifier: (String(str_tingMakeCellData)))
            //: let cellData = data as! PlayReactiveCompatible
            let cellData = data as! PlayReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = VaporBathHandyJSON()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.alongStr(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(str_showGiftText) + str_frameData.lowercased()))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: AtHeadCellData.self) {
        if data.isKind(of: AtHeadCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = UserMessageCell(style: .default, reuseIdentifier: (String(str_managerViewValue.prefix(4)) + "ingCh" + String(str_moreName.suffix(5)) + "ioMsg" + String(str_modeValue.prefix(6))))
            //: let cellData = data as! AtHeadCellData
            let cellData = data as! AtHeadCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ShowCellData.self) {
        if data.isKind(of: ShowCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = SaveTalkingThen(style: .default, reuseIdentifier: (String(str_labValue.suffix(4)) + "ingC" + String(str_dataValue.suffix(6)) + "chMs" + String(str_observerText.suffix(4)) + String(str_addContent)))
            //: let cellData = data as! ShowCellData
            let cellData = data as! ShowCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: EponymCellData.self) {
        if data.isKind(of: EponymCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = BottomThen(style: .default, reuseIdentifier: (String(str_picValue) + String(str_textName.prefix(4))))
            //: let cellData = data as! EponymCellData
            let cellData = data as! EponymCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: LengthSolarThermalSystemThen.self) {
        if data.isKind(of: LengthSolarThermalSystemThen.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = ViewRunMsgCell(style: .default, reuseIdentifier: String(bytes: str_controlName.map{caseData(progress: $0)}, encoding: .utf8)!)
            //: let cellData = data as! LengthSolarThermalSystemThen
            let cellData = data as! LengthSolarThermalSystemThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CompartmentSourceCellData.self) {
        if data.isKind(of: CompartmentSourceCellData.self) {
            //: let cell = TalkingChatMapMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMapMsgCellID")
            let cell = SkinStatusMsgCell(style: .default, reuseIdentifier: (String(str_faceInputName.prefix(7)) + String(str_imageData.suffix(4)) + String(str_mapValue.suffix(5)) + "gCellID"))
            //: let cellData = data as! CompartmentSourceCellData
            let cellData = data as! CompartmentSourceCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != MacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != UserMacroDefine.addTap() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: EditTextThen.share.func__pushToUserDetailVC(uid: String(uid))
            EditTextThen.share.soulAge(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TitleMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! DataReactiveCompatible
            let imageCellData = cell.messageData as! DataReactiveCompatible
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                //: let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                //: let num = TalkingPrivateChatManager.checkCanPreview(isImg: true, needAlert: needAlert)
                let num = FreeReactiveCompatible.toVideo(isImg: true, needAlert: needAlert)
                /// 需要解锁的照片
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                let vc = NamePreviewVc(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 1)
                        self.atMode(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_collectionSystemMarginValue, type: 1)
                        //: return
                        return
                    }
                    //: EqualThen.share.loginUserMode.updateResidualNum(type: 1, num: max(0, num - 1))
                    EqualThen.share.loginUserMode.turnTo(type: 1, num: max(0, num - 1))
//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                getApp()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kLet_coverValue)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = ZapModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = WithMomentModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [ZapModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = TargetPhotosVc(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                getApp()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ViewReactiveCompatible.self) { // 视频
            //: let videoCellData = cell.messageData as! ManageCellData
            let videoCellData = cell.messageData as! ManageCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = AtViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                getApp()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                //: let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                //: let num = TalkingPrivateChatManager.checkCanPreview(isImg: false, needAlert: needAlert)
                let num = FreeReactiveCompatible.toVideo(isImg: false, needAlert: needAlert)
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                let vc = WellspringViewController(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 2)
                        self.atMode(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_collectionSystemMarginValue, type: 2)
                        //: return
                        return
                    }
                    //: EqualThen.share.loginUserMode.updateResidualNum(type: 2, num: max(0, num - 1))
                    EqualThen.share.loginUserMode.turnTo(type: 2, num: max(0, num - 1))

//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                getApp()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: UserMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! UserMessageCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! AtHeadCellData, cellView: audioCell)
            via(cellData: audioCell.cellData as! AtHeadCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ChatAddView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ChatAddView
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_playerContent.replacingOccurrences(of: "plan", with: "e"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_infoEffectTitle) + String(str_voiceTitle))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(str_infoEffectTitle) + String(str_voiceTitle))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(str_makeWithName.prefix(6)) + "rson" + String(str_pathTitle.prefix(4))) {
                    //: if EqualThen.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if EqualThen.share.loginUserMode.isTPAuth == SkinComparable.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if EqualThen.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if EqualThen.share.loginUserMode.isTPAuth == SkinComparable.unknown.rawValue {
                        //: EditTextThen.share.func__pushUserVerifyController(toast: nil)
                        EditTextThen.share.alongTarget(toast: nil)
                        //: } else if EqualThen.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if EqualThen.share.loginUserMode.isTPAuth == SkinComparable.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = StatusThen()
                        //: EditTextThen.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        EditTextThen.share.addGet()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: EditTextThen.share.func__pushUserVerifyController(toast: nil)
                        EditTextThen.share.alongTarget(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<UpwardModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EditTextThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EditTextThen.share.makeEnableAdd(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(str_sessionContent.suffix(9)))] as? String == (String(str_atData.suffix(5)) + "llCustom") { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(str_arrayTitle))][(String(str_directionValue.suffix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    enableVocalisation()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    listener()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: BottomThen.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! BottomThen
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_playerContent.replacingOccurrences(of: "plan", with: "e"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_infoEffectTitle) + String(str_voiceTitle))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<UpwardModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EditTextThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EditTextThen.share.makeEnableAdd(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: SaveTalkingThen.self) {
            //: EditTextThen.share.func__pushToUserDetailVC(uid: targetId)
            EditTextThen.share.soulAge(uid: targetId)
            //: }else if cell.isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: SkinStatusMsgCell.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! SkinStatusMsgCell
            //: let mapCellData = mapCell.cellData as! CompartmentSourceCellData
            let mapCellData = mapCell.cellData as! CompartmentSourceCellData
            //: self.func__showLocationView(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: (mapCellData.direction == .MsgDirectionOutgoing))
            self.minimize(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: mapCellData.direction == .MsgDirectionOutgoing)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: NameCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TitleMsgCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! TitleMsgCell
            //: cellData = imageCell.cellData as! DataReactiveCompatible
            cellData = imageCell.cellData as! DataReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ViewReactiveCompatible.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! ViewReactiveCompatible
            //: cellData = videoCell.cellData as! ManageCellData
            cellData = videoCell.cellData as! ManageCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: UserMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! UserMessageCell
            //: cellData = audioCell.cellData as! AtHeadCellData
            cellData = audioCell.cellData as! AtHeadCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ChatAddView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ChatAddView
            //: cellData = textCell.cellData as! SendCellData
            cellData = textCell.cellData as! SendCellData
            //: } else if cell .isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: SkinStatusMsgCell.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! SkinStatusMsgCell
            //: cellData = mapCell.cellData as! CompartmentSourceCellData
            cellData = mapCell.cellData as! CompartmentSourceCellData
        }

        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: ManageCellData.self) {
                if reCellData!.isKind(of: ManageCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    voiceOrTextTheory(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    old(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = FreeReactiveCompatible.upJump(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        FreeReactiveCompatible.dataId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func createBar(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(EqualThen.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(EqualThen.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if EqualThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if EqualThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = EqualThen.share.loginUserMode.headPic!
                data.msgModel.user.headPic = EqualThen.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = EqualThen.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = EqualThen.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func sort() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (str_makeData.replacingOccurrences(of: "info", with: "t"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(str_constraintData)): (String(str_moreValue.prefix(4)) + "kMsg")]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_arrayTitle)): msgInfo, String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.old(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func brandTitle(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: str_topValue.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: str_topValue.reversed(), encoding: .utf8)!: content, (String(str_modeFadeData.suffix(5))): greetId, String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (str_makeData.replacingOccurrences(of: "info", with: "t"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: str_lastName.map{$0^202}, encoding: .utf8)!: content, (String(str_modeFadeData.suffix(5))): greetId, (String(str_constraintData)): (String(str_priceText))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(str_arrayTitle)): msgInfo, String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!: paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.old(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            HeadWithDataDelegate.shared.accumulation(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.oversight(showMsg: String(bytes: str_kindContainerValue.reversed(), encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        FreeReactiveCompatible.metadata(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(str_addUserName.replacingOccurrences(of: "top", with: "ng"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (String(str_cellMakeName)),
                               //: "audioId": greetId,
                               (String(str_timeVoiceText.prefix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: str_clearSectionTitle.reversed(), encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (str_playerFillValue.replacingOccurrences(of: "time", with: "u") + String(str_lastReplyData)): model.unZipFilePath,
                               //: "senderId": EqualThen.share.loginUserMode.userID]
                               String(bytes: str_clickName.map{mixUp(camera: $0)}, encoding: .utf8)!: EqualThen.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(str_timeVoiceText.prefix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: str_clearSectionTitle.reversed(), encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (str_playerFillValue.replacingOccurrences(of: "time", with: "u") + String(str_lastReplyData)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(str_constraintData)): (String(str_succeedContent) + str_toName.replacingOccurrences(of: "m", with: "g"))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(str_arrayTitle)): msgInfo, String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!: paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.old(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func people(msgStr: String, quoteModel: StorageMediumModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            oversight(showMsg: (str_sizeName + String(str_dataContent.suffix(5)) + "blank" + String(str_rowData)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: str_topValue.reversed(), encoding: .utf8)!: temStr, String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (str_makeData.replacingOccurrences(of: "info", with: "t"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: str_lastName.map{$0^202}, encoding: .utf8)!: temStr, (String(str_constraintData)): (String(str_priceText))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(str_arrayTitle)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(str_galleryCanValue) + String(str_willValue.prefix(4)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(str_commitValue.replacingOccurrences(of: "view", with: "e") + "MsgInfo")] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? StorageMediumModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        old(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func incrementalCost(img: UIImage, sendModel: FromJsonModel) {
        //: let imagePath = DataReactiveCompatible.getChatImagePath(image: img)
        let imagePath = DataReactiveCompatible.observerOf(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: str_toTouchData.map{fullPhaseOfTheMoon(time: $0)}, encoding: .utf8)!: (String(str_cellValue.prefix(6))), String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (String(str_liveTitle)), String(bytes: str_listData.reversed(), encoding: .utf8)!: imagePath, String(bytes: str_allText.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(str_equalData.replacingOccurrences(of: "action", with: "i")): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(str_constraintData)): (String(str_cellValue.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_regularValue.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_popularTitle.prefix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_arrayTitle)): msgInfo, String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        old(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func voiceOrTextTheory(msgData: TUIMessageCellData?, sendModel: FromJsonModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: str_toTouchData.map{fullPhaseOfTheMoon(time: $0)}, encoding: .utf8)!: (String(str_pointData) + String(str_labName)), String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (String(str_attachContent)), String(bytes: str_allText.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(str_constraintData)): (String(str_pointData) + String(str_labName)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: str_playerName.map{playCellAt(key: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: str_renderName.map{viewData(next: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: str_liveData.map{headIndex(item: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_regularValue.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_popularTitle.prefix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(str_arrayTitle)): msgInfo, String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.pour(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.pour(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        old(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func forefront(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (String(str_cellMakeName)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: str_clearSectionTitle.reversed(), encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (str_playerFillValue.replacingOccurrences(of: "time", with: "u") + String(str_lastReplyData)): voicePath,
                       //: "senderId": EqualThen.share.loginUserMode.userID]
                       String(bytes: str_clickName.map{mixUp(camera: $0)}, encoding: .utf8)!: EqualThen.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(str_playerFillValue.replacingOccurrences(of: "time", with: "u") + String(str_lastReplyData)): voicePath,
                         //: "type": "AudioMsg",
                         (String(str_constraintData)): (String(str_succeedContent) + str_toName.replacingOccurrences(of: "m", with: "g")),
                         //: "len": "\(voiceTime)"]
                         String(bytes: str_withName.map{importSucceed(array: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_arrayTitle)): msgInfo, String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        old(msgData: cellData, reSend: false)
    }

    //: func func__sendLocationMsg(latlon: String) {
    func stockMarketIndex(latlon: String) {
        //: let msgInfo = ["msgType": "map",
        let msgInfo = [String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (str_sectionValue.replacingOccurrences(of: "appearance", with: "p")),
                       //: "latlon": latlon,
                       String(bytes: str_addBottomBubbleText.map{sortDon(center: $0)}, encoding: .utf8)!: latlon,
                       //: "messageType": 28,
                       (String(str_tableTitle) + str_mediumText.replacingOccurrences(of: "model", with: "e")): 28,
                       //: "senderId": EqualThen.share.loginUserMode.userID] as [String : Any]
                       String(bytes: str_clickName.map{mixUp(camera: $0)}, encoding: .utf8)!: EqualThen.share.loginUserMode.userID] as [String: Any]

        //: let paramsDic = ["latlon": latlon,
        let paramsDic = [String(bytes: str_addBottomBubbleText.map{sortDon(center: $0)}, encoding: .utf8)!: latlon,
                         //: "type": "MapMsg"] as [String : Any]
                         (String(str_constraintData)): (String(str_contentName.suffix(5)) + str_toData.replacingOccurrences(of: "view", with: "g"))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_arrayTitle)): msgInfo, String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        old(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func old(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            oversight(showMsg: kLet_errorData)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(str_locationData.prefix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: ManageCellData.self) == false && msgData.isKind(of: PlayReactiveCompatible.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: ManageCellData.self) == false, msgData.isKind(of: PlayReactiveCompatible.self) == false, params[(String(str_constraintData))] as? String != (String(str_moreValue.prefix(4)) + "kMsg") {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((str_equalData.replacingOccurrences(of: "action", with: "i"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(str_equalData.replacingOccurrences(of: "action", with: "i"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(str_equalData.replacingOccurrences(of: "action", with: "i"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(String(str_constraintData))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(str_succeedContent) + str_toName.replacingOccurrences(of: "m", with: "g")) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((str_playerFillValue.replacingOccurrences(of: "time", with: "u") + String(str_lastReplyData))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(str_playerFillValue.replacingOccurrences(of: "time", with: "u") + String(str_lastReplyData))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (str_playerFillValue.replacingOccurrences(of: "time", with: "u") + String(str_lastReplyData)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(str_userValue.suffix(4)) + "ment" + String(str_insideData.suffix(6)) + String(str_nameToNowValue.prefix(7)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kLet_nextName + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(str_equalPushStackTitle.prefix(4)) + "ments/" + String(str_fileData.suffix(8)) + String(str_emptyTitle))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kLet_selectContent + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(str_timeVoiceText.prefix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(str_cellMakeName))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: PostulationReactiveCompatible.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        PostulationReactiveCompatible.bindCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(str_moreValue.prefix(4)) + "kMsg") {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.atMode(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_numberRequestTitle)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(str_modeFadeData.suffix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    FreeReactiveCompatible.metadata(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(str_arrayTitle))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome = NSNumber(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((str_labelText.replacingOccurrences(of: "photo", with: "m") + String(str_equalMakeData.prefix(7)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(str_labelText.replacingOccurrences(of: "photo", with: "m") + String(str_equalMakeData.prefix(7)))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if UserPushListener.sexApp().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kLet_statusText, object: nil, userInfo: [String(bytes: str_taskToValue.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.blind(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(str_tableTitle) + str_mediumText.replacingOccurrences(of: "model", with: "e"))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(str_moreValue.prefix(4)) + "kMsg") {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!] = (String(str_giftName.suffix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kLet_textData.historicalPaper(eventID: kLet_successText)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(str_moreValue.prefix(4)) + "kMsg") {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!] = (str_makeData.replacingOccurrences(of: "info", with: "t"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(str_arrayTitle)): msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(EqualThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(EqualThen.share.loginUserMode.userID)" + (String(str_videoData.suffix(4)) + "vateC" + String(str_lowTitle.suffix(5)) + String(str_sendName.prefix(4)) + "tle_") + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if EqualThen.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if EqualThen.share.loginUserMode.sex == RunReplaceableCollection.male.rawValue, type != (String(str_moreValue.prefix(4)) + "kMsg") {
                //: self.removeReplyTipsView()
                self.viewContentSize()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(str_textPlayerAddName))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            FreeReactiveCompatible.latestReplyFlushAndTimeApp(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((str_groupValue.replacingOccurrences(of: "red", with: "to") + "lMfCo" + str_viewData.replacingOccurrences(of: "normal", with: "i"))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(str_groupValue.replacingOccurrences(of: "red", with: "to") + "lMfCo" + str_viewData.replacingOccurrences(of: "normal", with: "i"))] as! NSNumber
                //: EqualThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                EqualThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((str_transactionValue.replacingOccurrences(of: "first", with: "a") + String(str_clearValue.prefix(6)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(str_transactionValue.replacingOccurrences(of: "first", with: "a") + String(str_clearValue.prefix(6)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.addQuery(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(str_titleValue) + String(str_rowCameraName))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(str_titleValue) + String(str_rowCameraName))] as! [String: Any]
                //: let model = ImageArrayModel(dic: intimatePhotoDict)
                let model = ImageArrayModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(str_attachContent))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(str_attachContent))] as! [String: Any]
                //: let model = PlayCollectionModel(dic: intimateVideoDict)
                let model = PlayCollectionModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        chat()

        //: updateSession()
        sum()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func sum() {
        //: PermitReactiveCompatible.cache_updateSessionAndPostNotif(with: targetId)
        PermitReactiveCompatible.outsideUserWith(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String, type: Int = 0) {
    private func atMode(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent _: String, type: Int = 0) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard EqualThen.share.loginUserMode.status != 1 else {
            guard EqualThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    oversight(showMsg: errorStr)
                }
                //: return
                return
            }
            //: EditTextThen.share.func__pushToRechargeAlert(appendParams: "&type=\(type)")
            EditTextThen.share.pause(appendParams: (String(str_centerData.prefix(6))) + "\(type)")
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.matchArray()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: EditTextThen.share.func__pushToSubscribeAlert()
            EditTextThen.share.addShowParams()

            //: } else if errorCode == 60003, !EqualThen.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !EqualThen.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            PlayerWindowManager.shared.visageMakeUpwardChangeFormWindow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                oversight(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func videoStatus(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(str_arrayTitle))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.blind(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func colorTo() {
        //: if self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.appellation(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(EqualThen.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(EqualThen.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (kLet_turnPartyValue.array(forKey: kLet_keyErrorData)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.dataTranslation()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                kLet_turnPartyValue.set(intimateCacheData, forKey: kLet_keyErrorData)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func wend(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(str_playerContent.replacingOccurrences(of: "plan", with: "e"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(str_arrayTitle))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(str_listName))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.atTime(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func blind(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(str_listName))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(str_listName))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.atTime(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func atTime(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = PermitReactiveCompatible.cache_getCachedUserInfo(targetId: self.targetId)
        let model = PermitReactiveCompatible.targetAppear(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= EqualThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= EqualThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.appellation(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.nearMessageSub(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.scutWork(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.scutWork(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: PermitReactiveCompatible.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        PermitReactiveCompatible.closePlayer(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: str_photoValue.map{centerTime(mode: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: str_viewTitle.map{streetSmart(m: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_minText,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension SendReactiveCompatible: DelegateReactiveCompatible {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func receive() {
        //: guard EqualThen.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard EqualThen.share.loginUserMode.sex == RunReplaceableCollection.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.labLanguage(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func utiliserRange(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func coloured(actionType: IndexGiftNumeric) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    behindCurrentEqual()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    botch()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                behindCurrentEqual()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if EqualThen.share.loginUserMode.sex == Gender.female.rawValue {
            if EqualThen.share.loginUserMode.sex == RunReplaceableCollection.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if EqualThen.share.loginUserMode.loungePlus {
                if EqualThen.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.upHandle(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                botch()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    sizeEqual()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    botch()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                sizeEqual()
            }

        //: case .Location:
        case .Location:
            //: TalkingPermissionTool.func__openLocationServiceWithBlock(true) { [weak self] isOpen in
            RubricThen.golfer(true) { [weak self] isOpen in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if isOpen {
                if isOpen {
                    //: self.func__showLocationView(latlon: nil, lookSelf: false)
                    self.minimize(latlon: nil, lookSelf: false)
                }
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.colorKind(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func botch() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.oversight(showMsg: String(bytes: str_actualErrorData.reversed(), encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func toError(giftModel _: VaporBathHandyJSON, giftNum _: String, model _: GrantUserMeasurable) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func funcInsideGift(giftModel: VaporBathHandyJSON, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        alongStr(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: PlayReactiveCompatible?, fromStr: String?) {
    func alongStr(giftModel: VaporBathHandyJSON, giftNum: String, cellData: PlayReactiveCompatible?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            oversight(showMsg: kLet_errorData)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(str_locationData.prefix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(str_popularTitle.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_indexData) + String(str_selectedData.suffix(6)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_cellData.prefix(8)) + String(str_viewContent.suffix(4)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(str_dataToValue))] = fromStr
        }
        // 送礼接口
        //: PostulationReactiveCompatible.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        PostulationReactiveCompatible.windowTo(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.atMode(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_statusData)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.blockPop(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.packetView(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.sum()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(str_arrayTitle))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(str_listName))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(str_listName))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.atTime(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kLet_textData.historicalPaper(eventID: kLet_sessionValue)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func commentHide(giftModel: VaporBathHandyJSON, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kLet_textData.historicalPaper(eventID: kLet_barData)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            oversight(showMsg: kLet_errorData)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= EqualThen.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            botch()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(str_itemName.lowercased() + String(str_plusToolValue))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(str_constraintData))] = (String(str_requestValue.suffix(5)) + "iftMsg")

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (String(str_giftName.suffix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(str_arrayTitle)): msgInfo, String(bytes: str_liveValue.map{centerEnd(value: $0)}, encoding: .utf8)!: params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        old(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func awake(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kLet_profileTitle - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func imageExit(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func showRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.showRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                positionHalfDismissAnimation(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func blockGroup() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if ControlRawManagerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            ControlRawManagerDelegate.shared.viewPlayer()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func packetView(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_arrayTitle))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!] = (str_inputValue.replacingOccurrences(of: "refuse", with: "t"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_arrayTitle))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((str_labelText.replacingOccurrences(of: "photo", with: "m") + String(str_equalMakeData.prefix(7)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(str_labelText.replacingOccurrences(of: "photo", with: "m") + String(str_equalMakeData.prefix(7)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_textPlayerAddName))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        routeModelDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((str_groupValue.replacingOccurrences(of: "red", with: "to") + "lMfCo" + str_viewData.replacingOccurrences(of: "normal", with: "i"))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(str_groupValue.replacingOccurrences(of: "red", with: "to") + "lMfCo" + str_viewData.replacingOccurrences(of: "normal", with: "i"))] as! NSNumber
            //: EqualThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            EqualThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: EqualThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.refresh(needReload: false, mf_coin: EqualThen.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func effectBind() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.vocalismCellGroup(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func routeModelDic(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.toDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func withArr(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.doingGoingKey(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func smallness() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func enableVocalisation() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.bodilyFunctionText { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.sum()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func listener() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.modelComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.sum()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    //: @objc private func feedbackBtnClick() {
    @objc private func derail() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = CounteractionAtFieldDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        getApp()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func printMedia() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = OnViewDelegate(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.videoList(cellTitleList: [(String(str_textServiceTitle.prefix(6)) + str_arrayScaleContent.replacingOccurrences(of: "lab", with: "n")).localized, (String(str_modelName)).localized, (String(str_countName) + String(str_pathName)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .executeTo()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: EditTextThen.share.func__pushToUserDetailVC(uid: self.targetId)
                EditTextThen.share.soulAge(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                equalStop()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = WithSkinViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.withPair(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func equalStop() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = SourceReactiveCompatible()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.colorPop(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.beginRestore(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func thing() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            positionHalfDismissAnimation(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            getApp()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func fileWill(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ViewScreenModel.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.database(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func screen() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(pastEqual(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kLet_senseValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(eraseList),
                                               //: name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION,
                                               name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func pastEqual(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard EqualThen.share.loginUserMode.loungePlus == true else { return }
        guard EqualThen.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func eraseList() {
        //: EqualThen.share.loginUserMode.loungePlus = true
        EqualThen.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func createSaveCollection() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if FreeReactiveCompatible.makeUp(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.beak(tipStr: (String(str_toSucceedText) + String(str_leadingName)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension SendReactiveCompatible: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func behindCurrentEqual() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        RubricThen.onError(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = GestureDataReactiveCompatible.extra(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.contentDevice(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func calendarYear() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        RubricThen.toBackMap(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.contentDevice(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func contentDevice(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: EqualThen.share.appStatus == AppSkinStatus.normal.rawValue else {
            EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = FromJsonModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.incrementalCost(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = TargetPhotosVc(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = FromJsonModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.incrementalCost(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        getApp()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func sizeEqual() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard FreeReactiveCompatible.latentPeriod(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == RunReplaceableCollection.male.rawValue ? "He".localized : (String(str_eventName)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.oversight(showMsg: (String(str_fullValue.prefix(4)) + "s usin" + String(str_bottomData.prefix(9)) + String(str_iconValue.prefix(5)) + "rsion").localizedArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.upwardlyBind()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func upwardlyBind() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = GestureDataReactiveCompatible.extra(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            GestureDataReactiveCompatible.completionEqual(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.postOf(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.oversight(showMsg: (String(str_readContent) + String(str_turnName.suffix(4)) + "5 se" + str_cornerFromInData.replacingOccurrences(of: "text", with: "ds")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func postOf(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: SoundEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = EditVisualCommunicationViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = FromJsonModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.voiceOrTextTheory(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        ModelReactiveCompatible.dataConverterCan()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        getApp()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func pour(msgData: TUIMessageCellData, sendModel: FromJsonModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ListReactiveCompatible.shared.nextIndex(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.oversight(showMsg: (String(str_titleName) + str_labelVideoContent.replacingOccurrences(of: "voice", with: "ed")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.voiceOrTextTheory(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    //: func func__addVoiceNotification() {
    func enrich() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(half),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(greetInstance),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(positionTable(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(titleNotification(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kLet_timeValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(wend(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kLet_domainEnvironmentTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func exaggerateAdd(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            ControlRawManagerDelegate.shared.parameter()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            ControlRawManagerDelegate.shared.listApp()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func half() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        exaggerateAdd(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func greetInstance() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        exaggerateAdd(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func positionTable(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            exaggerateAdd(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            exaggerateAdd(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: AtHeadCellData, cellView: TalkingChatAudioMsgCell) {
    func via(cellData: AtHeadCellData, cellView: UserMessageCell) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard DeviceSocketDelegate.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.oversight(showMsg: kLet_replaceTitle)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: AtHeadCellData.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: AtHeadCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! AtHeadCellData
            let audioModel = indexModel as! AtHeadCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = PermitReactiveCompatible.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = PermitReactiveCompatible.currency(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        ControlRawManagerDelegate.shared.viewPlayer()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if ControlRawManagerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(str_lastValue)) + "\(ControlRawManagerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if ControlRawManagerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                ControlRawManagerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        ControlRawManagerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        ControlRawManagerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: AtHeadCellData.self) {
                if subModel.isKind(of: AtHeadCellData.self) {
                    //: let audioModel = subModel as! AtHeadCellData
                    let audioModel = subModel as! AtHeadCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = PermitReactiveCompatible.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = PermitReactiveCompatible.currency(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: VoxReactiveCompatible.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    VoxReactiveCompatible.pastTarget(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        ControlRawManagerDelegate.shared.lineItem(msgArr: waitArray)
    }
}

// MARK: - 地图相关

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 发送地图
    //: private func func__showLocationView(latlon: String?, lookSelf: Bool) {
    private func minimize(latlon: String?, lookSelf: Bool) {
        //: let location = TalkingMapView(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        let location = GiftAtViewDelegate(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        //: location.show()
        location.textSub()
        //: location.sendBlcok = { [weak self] latlon in
        location.sendBlcok = { [weak self] latlon in
            //: self?.func__sendLocationMsg(latlon: latlon)
            self?.stockMarketIndex(latlon: latlon)
        }
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func pastPath() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.voiceWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.brandTitle(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: AtHeadCellData.self) == true || indexModel?.isKind(of: DataReactiveCompatible.self) == true || indexModel?.isKind(of: SendCellData.self) == true || indexModel?.isKind(of: ManageCellData.self) == true {
                if indexModel?.isKind(of: AtHeadCellData.self) == true || indexModel?.isKind(of: DataReactiveCompatible.self) == true || indexModel?.isKind(of: SendCellData.self) == true || indexModel?.isKind(of: ManageCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.chat()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.customer()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.createSaveCollection()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func voiceWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.direct(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.cleave(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.colorTo()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == EqualThen.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == EqualThen.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == EqualThen.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == EqualThen.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.beginRestore(userWrap)
    }

    //: func func__getFillDraftText() {
    func nameTitle() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_errData)) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.receive()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func freePhone() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_errData)) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    //: func func__TipsNotification() {
    func ofNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(atTo(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_changeValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func atTo(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: EponymCellData  = userinfo["msg"] as! EponymCellData
        let celldata: EponymCellData = userinfo[(String(str_giftValue))] as! EponymCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func beak(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: str_topValue.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: str_sectionName.map{leadingTo(card: $0)}, encoding: .utf8)!: (String(str_addValue))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func customer() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        FreeReactiveCompatible.instanceHandler { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.beak(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func addQuery(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.beak(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func chat() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        FreeReactiveCompatible.emptyHanded(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.beak(tipStr: String(bytes: str_fileValue.map{cellPic(date: $0)}, encoding: .utf8)!.localizedArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func titleNotification(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(String(str_voiceTempTitle.prefix(7)) + str_sizeData.replacingOccurrences(of: "line", with: "a"))] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension SendReactiveCompatible: SaveNameTipsProtocol {
    //: func func__setReplyTipsView() {
    func replyAppear() {
        //: let key =  "\(EqualThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(EqualThen.share.loginUserMode.userID)" + (String(str_videoData.suffix(4)) + "vateC" + String(str_lowTitle.suffix(5)) + String(str_sendName.prefix(4)) + "tle_") + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && EqualThen.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.price()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            willView(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            viewContentSize()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func pull(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        people(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        viewContentSize()
    }

    //: func removeReplyTipsView() {
    func viewContentSize() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            willView(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func weltanschauung() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kLet_requestValue, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kLet_clickData - kLet_profileTitle, width: kLet_cameraValue, height: kLet_clickData)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kLet_cameraValue,
                                     //: height: ScreenHeight)
                                     height: kLet_clickData)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func positionHalfDismissAnimation(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.infoController() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if ViewRoomManager.astatine().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_contentData, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: PopViewDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kLet_clickData - kLet_profileTitle,
                                     //: width: ScreenWidth,
                                     width: kLet_cameraValue,
                                     //: height: ScreenHeight)
                                     height: kLet_clickData)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if ViewRoomManager.astatine().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_contentData, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension SendReactiveCompatible {
    /// 创建UI
    //: private func createUI() {
    private func searchRoom() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(str_indexContent) + String(str_inputTitle)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func clearUp() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kLet_profileTitle : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.showEqual(width: contentView.width,
                                  //: height: contentView.height,
                                  height: contentView.height,
                                  //: corners: [.topLeft, .topRight],
                                  corners: [.topLeft, .topRight],
                                  //: cornerRadii: .init(width: 8, height: 8))
                                  cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_deviceRecordValue : kLet_detailNameText)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_nextData)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_deviceRecordValue : kLet_detailNameText)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_nextData)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + kLet_nextData)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func sortCounteraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.thing()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.printMedia()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.coloured(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kLet_textData.historicalPaper(eventID: kLet_micText, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
