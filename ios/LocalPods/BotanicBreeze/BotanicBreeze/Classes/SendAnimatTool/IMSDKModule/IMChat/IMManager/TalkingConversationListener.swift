
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let str_colorData:String = "IM\u{9000}\u{51fa}"
fileprivate let str_indexText:[Character] = ["d","e",":"]

/*: , desc: :*/
fileprivate let str_dateTextData:String = ", desleading true var"
fileprivate let str_contentValue:String = "render universal bottomc:"

/*: ." :*/
fileprivate let str_viewContent:[Character] = ["."]

/*: "IM登录失败，code: :*/
fileprivate let str_cellSingleValue:String = "IM登录失head next view array invite"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let str_toText:String = "IM\u{83b7}\u{53d6}未"
fileprivate let str_cameraValue:String = "\u{5931}败，c"
fileprivate let str_goTitle:String = "ode:false full coin frame item"

/*: "crushBadge" :*/
fileprivate let str_toData:String = "publishru"
fileprivate let str_effectContent:String = "shBadgeblock manager"

/*: "badNumber" :*/
fileprivate let str_makeTitle:[UInt8] = [0xf8,0xfb,0xfe,0xd4,0xef,0xf7,0xf8,0xff,0xe8]

/*: "plaza" :*/
fileprivate let str_areaData:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let str_frameContentValue:String = "background"
fileprivate let str_herTitle:String = "oomNumreflection main if model"

/*: "num" :*/
fileprivate let str_countText:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let str_norValue:String = "oedity"
fileprivate let str_backgroundData:[Character] = ["d"]

/*: "LIVE-CHAT" :*/
fileprivate let str_infoContent:[Character] = ["L","I","V","E","-","C","H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let str_femaleTitle:String = "PARTY-Chidden data model online for"
fileprivate let str_labTitle:[Character] = ["H","A","T"]

/*: "extra" :*/
fileprivate let str_normalValue:String = "clickxtra"

/*: "msgType" :*/
fileprivate let str_pathToData:String = "text inmsgTy"
fileprivate let str_giveLocationValue:String = "pcontext"

/*: "newPkgGift" :*/
fileprivate let str_gestureText:[Character] = ["n","e","w","P"]
fileprivate let str_labName:[Character] = ["k","g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let str_makeAddDeviceName:String = "remove effect elseMF:Ch"
fileprivate let str_packageData:String = "ionMtype temp false to"
fileprivate let str_wrapValue:String = "message"

/*: "uid" :*/
fileprivate let str_requestValue:[UInt8] = [0xa6,0xba,0xb7]

private func matchCenter(scale num: UInt8) -> UInt8 {
    return num ^ 211
}

/*: "onlineStatus" :*/
fileprivate let str_toName:[UInt8] = [0x9f,0x9e,0x9c,0x99,0x9e,0x95,0xa3,0x84,0x91,0x84,0x85,0x83]

private func errorLet(on num: UInt8) -> UInt8 {
    return num ^ 240
}

/*: "isNewUser" :*/
fileprivate let str_frameData:[UInt8] = [0x35,0x2f,0x12,0x39,0x2b,0x9,0x2f,0x39,0x2e]

/*: "c2c_ :*/
fileprivate let str_sizeViewTitle:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let str_tableText:[Character] = ["I","M","获","取","会","话","失","败","，","c","o","n","v","I","d",":"]

/*: , code: :*/
fileprivate let str_sexBlockTitle:String = "manager all start if, code"
fileprivate let str_textName:String = "by"

/*: "IM插入消息失败，targetId: :*/
fileprivate let str_managerData:String = "IM插入消\u{606f}"
fileprivate let str_senseValue:[Character] = ["r","g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let str_gestureData:String = "CHAN"
fileprivate let str_refuseTitle:[Character] = ["c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let str_portData:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let str_mediumText:String = "GJ:Calltouch status"
fileprivate let str_currentLimitTopText:[Character] = ["C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let str_willName:[Character] = ["I","M","\u{83b7}","取","会","话","\u{5217}"]
fileprivate let str_distributionManagerVarName:String = "render"
fileprivate let str_listStatusValue:String = "失\u{8d25}\u{ff0c}c"

/*: "msgInfo" :*/
fileprivate let str_modelScaleData:[Character] = ["m","s","g","I","n","f","o"]

/*: "needFold" :*/
fileprivate let str_arrayName:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let str_maleData:String = "ggiftup"
fileprivate let str_valueName:[Character] = ["_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let str_shareViewName:[Character] = ["I","M","删","除","会"]
fileprivate let str_itemTouchData:String = "话\u{5931}败，c"

/*: "IM置顶会话失败，convId: :*/
fileprivate let str_femaleData:[Character] = ["I","M","置","顶","会","话","失","败","，","c","o"]
fileprivate let str_numberKeyValue:String = "error cookie input startnvId:"

/*: "SDK 正在连接到服务器" :*/
fileprivate let str_lineName:String = "SDK \u{6b63}\u{5728}连接\u{5230}"
fileprivate let str_videoName:String = "服务\u{5668}"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let str_namePageData:String = "SDK make interaction self model center"
fileprivate let str_makeContent:String = "已经成功"

/*: "IM连接服务器失败，code: :*/
fileprivate let str_leadingName:String = "IM连接frame free birth class"
fileprivate let str_popMessageData:String = "败\u{ff0c}code:"

/*: , error: :*/
fileprivate let str_removeTitle:String = ","
fileprivate let str_modelTitle:String = "value live make detail error:"

/*: "IM账号被挤下线。" :*/
fileprivate let str_rowUserInputName:[Character] = ["I","M","账","号","被","挤","下","线","。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let str_scaleValueData:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let str_voiceModelInText:[Character] = ["O","f","f","l","i","n","e"," ","N","o","t","i","f","i","c","a","t"]
fileprivate let str_detailName:[Character] = ["i","o","n"]

/*: "Cancel" :*/
fileprivate let str_imageData:String = "Cancelphone make sum in"

/*: "Re Login" :*/
fileprivate let str_statusTitle:[Character] = ["R","e"," ","L","o","g","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TalkingConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol ManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func requestData(data: [LengthReactiveCompatible])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func tally(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func count(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum WithDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class TalkingConversationListener: NSObject {
@objcMembers public class TalkingConversationListener: NSObject {
    //: public static let shared = TalkingConversationListener()
    public static let shared = TalkingConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [LengthReactiveCompatible] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [LengthReactiveCompatible] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [LengthReactiveCompatible] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [LengthReactiveCompatible] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [LengthReactiveCompatible] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: LengthReactiveCompatible]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.toEvent()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func northeast() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(kLet_blockName, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func toEvent() {
        //: func__removeAllDelegate()
        bot()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.contentList()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func contentList() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = LengthReactiveCompatible.userType(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
//        let serviceModel = LengthReactiveCompatible.userType(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel/*, serviceModel*/]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func search() -> [LengthReactiveCompatible] {
        // 审核模式，在消息列表移除官方客服
        //: if EqualThen.share.appStatus == AppSkinStatus.special.rawValue {
        if EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == MacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == UserMacroDefine.addTap() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension TalkingConversationListener {
extension TalkingConversationListener {
    //: func func__removeAllDelegate() {
    func bot() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func handle(_ delegate: ManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func noFunc(_ delegate: ManagerDelegate) {
        //: self.func__invokeDelegate {
        self.task {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func task(_ invocation: (ManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! ManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension TalkingConversationListener {
extension TalkingConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func playApp() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            DeepKeyTamperReactiveCompatible.indexView(msg: (str_colorData + "登录失\u{8d25}\u{ff0c}co" + String(str_indexText)) + "\(code)" + (String(str_dateTextData.prefix(5)) + String(str_contentValue.suffix(2))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.toEvent()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func giveAndTakeCompletion(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = EqualThen.share.appUserConfigMode.uSig
        let uSigStr = EqualThen.share.appUserConfigMode.uSig
        //: let uidStr = EqualThen.share.loginUserMode.userID
        let uidStr = EqualThen.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.migrationTitle()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_cellSingleValue.prefix(5)) + "\u{8d25}，code:") + "\(code)" + (String(str_dateTextData.prefix(5)) + String(str_contentValue.suffix(2))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func dataImage() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || EqualThen.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || EqualThen.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.oversight(showMsg: kLet_errorData)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension TalkingConversationListener {
extension TalkingConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func migrationTitle() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            DeepKeyTamperReactiveCompatible.indexView(msg: (str_toText + "读消息\u{603b}数" + str_cameraValue + String(str_goTitle.prefix(4))) + "\(code)" + (String(str_dateTextData.prefix(5)) + String(str_contentValue.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func addMsg() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: PostulationReactiveCompatible.req_sysMessageList { _, result in
        PostulationReactiveCompatible.less { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(str_toData.replacingOccurrences(of: "publish", with: "c") + String(str_effectContent.prefix(7)))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: kLet_pathSenseData, object: self, userInfo: [String(bytes: str_makeTitle.map{$0^154}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func accountingDataApp(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: PostulationReactiveCompatible.req_getChatRoomInfo { succeed, result, _ in
        PostulationReactiveCompatible.chatInfoGetCompletionRoomReply { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(str_areaData))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(str_frameContentValue.replacingOccurrences(of: "background", with: "r") + String(str_herTitle.prefix(6)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = LengthReactiveCompatible.userType(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(str_countText))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(str_norValue.replacingOccurrences(of: "edit", with: "nl") + "OneRoomI" + String(str_backgroundData))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension TalkingConversationListener: V2TIMAdvancedMsgListener {
extension TalkingConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(str_infoContent))) || msg.groupID.contains((String(str_femaleTitle.prefix(7)) + String(str_labTitle)))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            UserListBankManagerReactiveCompatible.willSize().appConstraint(msg: msg)
            //: return
            return
        }
        //: if msg.sender == EqualThen.share.loginUserMode.userID { return }
        if msg.sender == EqualThen.share.loginUserMode.userID { return }

        //: let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(str_normalValue.replacingOccurrences(of: "click", with: "e"))] as! [AnyHashable: Any]
        //: let msgModel = DiskModel(dic: extraDic)
        let msgModel = DiskModel(dic: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: kLet_domainEnvironmentTitle, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(str_pathToData.suffix(5)) + str_giveLocationValue.replacingOccurrences(of: "context", with: "e"))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(str_gestureText) + String(str_labName)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_screenValue, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TUIMsgChatManager.checkVoiceMessage(message: msg) {
            if TUIMsgChatManager.checkVoiceMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                //: VoxReactiveCompatible.db_inserVoiceMsgToDB(dic)
                VoxReactiveCompatible.pointView(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.popWith(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SendAnimatTool.shared.receiveOf(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel.msgInfo.messageType == .MessageTypeGift {
        if msgModel.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SendAnimatTool.shared.nearTitle(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(str_makeAddDeviceName.suffix(5)) + "atMent" + String(str_packageData.prefix(4)) + str_wrapValue.replacingOccurrences(of: "message", with: "sg")) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            NameRawThen.share.actionKey(msg: msg, extraInfo: extraDic)
        }

        //: if !msgModel.gift.unlockMsgId.isEmptyString {
        if !msgModel.gift.unlockMsgId.isEmptyString { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: FreeReactiveCompatible.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel.gift.unlockMsgId)
            array.append(msgModel.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: FreeReactiveCompatible.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != MacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != UserMacroDefine.underGet() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.task { $0.count?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            SendNotificationManager.shared.totalimate(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: str_requestValue.map{matchCenter(scale: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: str_toName.map{errorLet(on: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel.msgInfo.isNewUser] as [String: Any]
                                String(bytes: str_frameData.map{$0^92}, encoding: .utf8)!: msgModel.msgInfo.isNewUser] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: kLet_viewName,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: PermitReactiveCompatible.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            PermitReactiveCompatible.moveConstraint(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            FreeReactiveCompatible.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: kLet_senseValue, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
    @objc public func searched(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
        //: let imMsg = msgCellData.innerMessage
        let imMsg = msgCellData.innerMessage

        //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        self.analogDigitalConverterMedium(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(str_sizeViewTitle)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.slateList(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.versation(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_tableText)) + "\(convId)" + (String(str_sexBlockTitle.suffix(6)) + str_textName.replacingOccurrences(of: "by", with: ":")) + "\(code)" + (String(str_dateTextData.prefix(5)) + String(str_contentValue.suffix(2))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func analogDigitalConverterMedium(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = EqualThen.share.loginUserMode.userID
        let senderStr = EqualThen.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                DeepKeyTamperReactiveCompatible.indexView(msg: (str_managerData + "失败，ta" + String(str_senseValue)) + "\(targetId)" + (String(str_sexBlockTitle.suffix(6)) + str_textName.replacingOccurrences(of: "by", with: ":")) + "\(code)" + (String(str_dateTextData.prefix(5)) + String(str_contentValue.suffix(2))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func popWith(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(str_gestureData.lowercased() + "geDire" + String(str_refuseTitle))].boolValue
        if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && EditTextThen.share.addGet()!.isKind(of: SendReactiveCompatible.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: kLet_timeBarName) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: kLet_timeBarName)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension TalkingConversationListener: V2TIMConversationListener {
extension TalkingConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.slateList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.versation(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.slateList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.versation(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: EqualThen.share.unreadMessageNum = Int(totalUnreadCount)
        EqualThen.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.task {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.tally?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func versation(conVersationList: [LengthReactiveCompatible]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if PermitReactiveCompatible.shared().needReqUidsSet != nil {
            if PermitReactiveCompatible.ginseng().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: PermitReactiveCompatible.shared().needReqUidsSet!.formUnion(set)
                PermitReactiveCompatible.ginseng().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.task {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.requestData?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func smearCompletion(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [LengthReactiveCompatible] = []

        // V2TIMConversation->LengthReactiveCompatible
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = LengthReactiveCompatible(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.entitle(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [LengthReactiveCompatible] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = UpInfoTable.starWith(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.callBecome(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension TalkingConversationListener {
extension TalkingConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func voiceBottom(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.smearCompletion(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.versation(conVersationList: arr as? [LengthReactiveCompatible] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_willName) + str_distributionManagerVarName.replacingOccurrences(of: "render", with: "表") + str_listStatusValue + "ode:") + "\(code)" + (String(str_dateTextData.prefix(5)) + String(str_contentValue.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func third(targetId: String) -> LengthReactiveCompatible? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func slateList(convList: [V2TIMConversation]) -> [LengthReactiveCompatible] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [LengthReactiveCompatible] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = LengthReactiveCompatible(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.entitle(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.callBecome(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func callBecome(convModel: LengthReactiveCompatible, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.contentList()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: WithDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - EqualThen.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - EqualThen.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = PermitReactiveCompatible.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = PermitReactiveCompatible.targetAppear(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(str_normalValue.replacingOccurrences(of: "click", with: "e"))][(String(str_modelScaleData))][(String(str_arrayName))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.genreType(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.genreType(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func genreType(convList: [LengthReactiveCompatible], dataType: WithDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func breedType(targetId: String, dataType: WithDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(str_sizeViewTitle)) + "\(targetId)" : (str_maleData.replacingOccurrences(of: "gift", with: "ro") + String(str_valueName)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: PermitReactiveCompatible.db_removeRecord(with: targetId)
            PermitReactiveCompatible.countIn(with: targetId)
            //: VoxReactiveCompatible.db_deleteVoiceMsg(EqualThen.share.loginUserMode.userID, toUid: targetId)
            VoxReactiveCompatible.anvilName(EqualThen.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.migrationTitle()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_shareViewName) + str_itemTouchData + "onvID:") + "\(convID)" + (String(str_sexBlockTitle.suffix(6)) + str_textName.replacingOccurrences(of: "by", with: ":")) + "\(code)" + (String(str_dateTextData.prefix(5)) + String(str_contentValue.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func colorMake(convModel: LengthReactiveCompatible, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            DeepKeyTamperReactiveCompatible.indexView(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.genreType(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.genreType(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension TalkingConversationListener: V2TIMSDKListener {
extension TalkingConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (str_lineName + str_videoName))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(str_namePageData.prefix(4)) + str_makeContent.capitalized + "连\u{63a5}\u{5230}服务器"))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_leadingName.prefix(4)) + "服务器失" + str_popMessageData) + "\(code)" + (str_removeTitle.capitalized + String(str_modelTitle.suffix(7))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: DeepKeyTamperReactiveCompatible.indexView(msg: "IM账号被挤下线。")
        DeepKeyTamperReactiveCompatible.indexView(msg: (String(str_rowUserInputName)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        DeviceSocketDelegate.shared.circumferentOut(userLoginOut: true)
        //: TalkingConversationListener.shared.isConnection = false
        TalkingConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (CellTalkingApplication.shared as! CellTalkingApplication).onArt()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: str_scaleValueData.reversed(), encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = AtAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if SendUpReactiveCompatible.share.interfaceLang == EditTermConvertible.ar.rawValue || SendUpReactiveCompatible.share.interfaceLang == EditTermConvertible.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            SendUpReactiveCompatible.share.interfaceLang == EditTermConvertible.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangWithDiameter(fontSize: 13)
            config.rightFont = UIFont.pingfangWithDiameter(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        MotilityThen.viewConfig()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        MotilityThen.viewConfig(title: (String(str_voiceModelInText) + String(str_detailName)).localized, message: msg, leftBtnTitle: (String(str_imageData.prefix(6))).localized, rightBtnTitle: (String(str_statusTitle)).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            UserPushListener.sexApp().viewLodge()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ViewRoomManager.astatine().fastening()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            LoginRequestTool.atCompletion { t in
                //: ProgressHUD.dismiss()
                ModelReactiveCompatible.dataConverterCan()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_useName, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            LoginRequestTool.info { t in
                //: if t {
                if t {
                    //: TalkingConversationListener.shared.func__LogingIn { _ in
                    TalkingConversationListener.shared.giveAndTakeCompletion { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if UserPushListener.sexApp().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(UserPushListener.sexApp().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    DeviceSocketDelegate.shared.modelSize()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
