
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_centerValue:[UInt8] = [0xd0,0xd5,0xd0,0xdb,0x8f,0xca,0xd6,0xcb,0xcc,0xd9,0xa1,0x90,0x87,0xcf,0xc8,0xda,0x87,0xd5,0xd6,0xdb,0x87,0xc9,0xcc,0xcc,0xd5,0x87,0xd0,0xd4,0xd7,0xd3,0xcc,0xd4,0xcc,0xd5,0xdb,0xcc,0xcb]

fileprivate func blockMeasurement(background num: UInt8) -> UInt8 {
    let value = Int(num) + 153
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let str_allowTitle:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let str_addName:String = "headPcookie aspect if array"
fileprivate let str_managerName:String = "isecond"

/*: "nickname" :*/
fileprivate let str_voiceValue:String = "NICKN"
fileprivate let str_modelData:String = "gifte"

/*: "birthday" :*/
fileprivate let str_imageName:String = "countr"
fileprivate let str_playerValue:[Character] = ["t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let str_valueName:String = "VIDE"

/*: "location" :*/
fileprivate let str_messageTitle:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let str_addContent:[UInt8] = [0x83,0x8d,0x84]

fileprivate func asValueCell(manager num: UInt8) -> UInt8 {
    let value = Int(num) - 32
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let str_shareLastVarData:String = "rlineuse"

/*: "data" :*/
fileprivate let str_tillData:[UInt8] = [0xce,0xcb,0xde,0xcb]

private func labMake(info num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "Call interruption" :*/
fileprivate let str_noTitle:String = "top color equal letCa"
fileprivate let str_videoSenseToData:[Character] = ["l","l"," ","i","n","t","e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let str_norTitle:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let str_selectedContent:[UInt8] = [0x69,0x6a,0x62,0x4c,0x61]

/*: "cancelCall" :*/
fileprivate let str_meText:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let str_frameValue:String = "color"
fileprivate let str_toContent:String = "o anparty server any extension"

/*: "duration" :*/
fileprivate let str_timeEqualModelValue:[Character] = ["d","u","r","a","t","i","o"]
fileprivate let str_colorText:[Character] = ["n"]

/*: "She has hung up" :*/
fileprivate let str_indexNeedMakeData:String = "She color index photo"
fileprivate let str_reasonData:String = "model edge button equal blankung u"
fileprivate let str_sizeValue:String = "s"

/*: "cancelSex" :*/
fileprivate let str_frameName:[Character] = ["c","a","n","c"]
fileprivate let str_neverSharedContent:String = "share equal contentelSex"

/*: "He has hung up" :*/
fileprivate let str_titleValue:String = "in button data show hiddenHe has h"
fileprivate let str_pathName:[Character] = ["u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_locationData:String = "endTypfrom view case size"
fileprivate let str_limitValue:String = "content"

/*: "stopSex" :*/
fileprivate let str_equalText:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_moreToolText:[UInt8] = [0x66,0x8b,0x90,0x92,0x83,0x83,0x86,0x80,0x86,0x82,0x8b,0x91,0x3d,0x80,0x8f,0x82,0x81,0x86,0x91,0x49,0x3d,0x80,0x7e,0x89,0x89,0x3d,0x85,0x7e,0x90,0x3d,0x82,0x8b,0x81,0x82,0x81]

fileprivate func frameSecondNor(indicator num: UInt8) -> UInt8 {
    let value = Int(num) - 29
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let str_deadlineData:[Character] = ["T","h","e"," "]
fileprivate let str_infoValue:[Character] = ["o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let str_makeContent:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let str_voiceName:String = "nor number play voicejumpUrl"

/*: "Cancel" :*/
fileprivate let str_selectedName:[Character] = ["C"]
fileprivate let str_topTableContent:[Character] = ["a","n","c","e","l"]

/*: "Purchase" :*/
fileprivate let str_requestAttentionData:String = "pop make buttonPu"
fileprivate let str_areaValue:String = "rchasname"

/*: "&type=4" :*/
fileprivate let str_clearPathData:String = "version view size equal&type=4"

/*: "addBean" :*/
fileprivate let str_viewTempValue:String = "place label textaddBe"
fileprivate let str_imageTitle:[Character] = ["a","n"]

/*: "refuseSex" :*/
fileprivate let str_barData:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let str_modelContent:[Character] = ["o","n","A","c","c","e","p","t","C","a","l"]
fileprivate let str_firstValue:String = "time"

/*: "acceptCall" :*/
fileprivate let str_toData:String = "astylestyleep"
fileprivate let str_interactionName:String = "return let model error floattCall"

/*: "Calls are cancelled" :*/
fileprivate let str_appName:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c"]
fileprivate let str_dayName:String = "render"
fileprivate let str_toName:[Character] = ["n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let str_labData:[Character] = ["T","h","e"]
fileprivate let str_pathData:String = "up view in block live cal"
fileprivate let str_sourceContent:String = "entod"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PtolemaicSystemViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let kLet_indexData = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class PtolemaicSystemViewController: SeamViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ViewChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: CompartmentPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: TotalenanceThen? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        DeviceSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DeviceSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DeviceSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_centerValue.map{blockMeasurement(background: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! LengthArrayControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.complete()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            messageBottom()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DeviceSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.translateEqual()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        counteractionSubviews()
        //: setupSubViewsConstraint()
        blackBook()
        //: initNotificationObserver()
        situation()
        //: req_callGetUserInfo()
        demonstrateInfo()
        //: timeoutRules()
        dataTimeout()
        //: resumeTalk()
        barSinister()
        //: setUpBGMPlayer()
        ballplayer()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if EqualThen.share.appConfigMode.disableShootScreen {
            if EqualThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CellDataReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_cameraValue, height: kLet_clickData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        DeviceSocketDelegate.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.modelActivties()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: CustomQuantityeractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = CustomQuantityeractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: FileCaptureReactiveCompatible = {
        //: let m = TalkingVoiceChatManager()
        let m = FileCaptureReactiveCompatible()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension PtolemaicSystemViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func demonstrateInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ShowCloudDelegate.brandAction(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_allowTitle.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_addName.prefix(5)) + str_managerName.replacingOccurrences(of: "second", with: "c"))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_voiceValue.lowercased() + str_modelData.replacingOccurrences(of: "gift", with: "am"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_imageName.replacingOccurrences(of: "count", with: "bi") + String(str_playerValue))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(str_valueName.lowercased() + "oUrl")].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(str_messageTitle))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.someModel()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension PtolemaicSystemViewController {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func messageBottom() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        RubricThen.valueClose(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.oversight(showMsg: kLet_likeCancelData)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (str_shareLastVarData.replacingOccurrences(of: "line", with: "ef") + "Call"), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_allowTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.jostle()
                //: return
                return
            }

            //: self.stopBGM()
            self.tillEnterEqual()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = CompartmentPlayListener.callBy(type: .Response)
            //: self.startBGM()
            self.complete()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func modelActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.tillEnterEqual()
        //: TalkingSocketManager.shared.callerDelegate = nil
        DeviceSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        DeviceSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        DeviceSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func jostle(animated: Bool = true) {
        //: self.stopBGM()
        self.tillEnterEqual()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func atEffect() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.closeline(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func barSinister() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            atEffect()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension PtolemaicSystemViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func situation() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(ptolemaicSystem(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(reachabilityUp(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_deviceContent,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_indexData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func ptolemaicSystem(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            jostle()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func reachabilityUp(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard DeviceSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        DeviceSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DeviceSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DeviceSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.black(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension PtolemaicSystemViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.oversight(showMsg: (String(str_noTitle.suffix(2)) + String(str_videoSenseToData)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (String(str_norTitle)), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_selectedContent.map{$0^5}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (String(str_meText)), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_selectedContent.map{$0^5}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (str_shareLastVarData.replacingOccurrences(of: "line", with: "ef") + "Call"), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_allowTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.queryEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                DeviceSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            jostle()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.colorEnable(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension PtolemaicSystemViewController {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func ballplayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DeepRelatableTarget.Call : DeepRelatableTarget.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = CompartmentPlayListener.callBy(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func complete() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.appearFor()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func tillEnterEqual() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.underCancel()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension PtolemaicSystemViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func dataTimeout() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_freeName : self.chatModel?.rest()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            thatTap()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.thatTap()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func thatTap() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !DeviceSocketDelegate.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.oversight(showMsg: (str_frameValue.replacingOccurrences(of: "color", with: "N") + String(str_toContent.prefix(4)) + "swer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (String(str_meText)), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_selectedContent.map{$0^5}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (str_shareLastVarData.replacingOccurrences(of: "line", with: "ef") + "Call"), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_allowTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        DeviceSocketDelegate.shared.noCurrent(info: socketDict)
        //: popCurrentViewController()
        jostle()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension PtolemaicSystemViewController {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func exaggerateKey(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_selectedContent.map{$0^5}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(str_timeEqualModelValue) + String(str_colorText))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.forceBackTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.acceptFor()
            //: createTalkRoom()
            atEffect()
            //: self.stopBGM()
            self.tillEnterEqual()
        }
    }
}

// MARK: - GrantCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension PtolemaicSystemViewController: GrantCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func loungeTouch(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_indexNeedMakeData.prefix(4)) + "has h" + String(str_reasonData.suffix(5)) + str_sizeValue.replacingOccurrences(of: "s", with: "p")).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_frameName) + String(str_neverSharedContent.suffix(5)))].stringValue == RunReplaceableCollection.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_titleValue.suffix(8)) + String(str_pathName)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.oversight(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.queryEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DeviceSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        jostle()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func leadIn(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_selectedContent.map{$0^5}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.tillEnterEqual()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.acceptFor()
        //: self.createTalkRoom()
        self.atEffect()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func centerData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_locationData.prefix(6)) + str_limitValue.replacingOccurrences(of: "content", with: "e"))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_indexNeedMakeData.prefix(4)) + "has h" + String(str_reasonData.suffix(5)) + str_sizeValue.replacingOccurrences(of: "s", with: "p")).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_equalText))].stringValue == RunReplaceableCollection.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_titleValue.suffix(8)) + String(str_pathName)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if EqualThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_moreToolText.map{frameSecondNor(indicator: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_deadlineData) + String(str_infoValue)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.oversight(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.queryEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DeviceSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.aboveTable()
        //: self.popCurrentViewController()
        self.jostle()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kLet_postValue, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func signature(msg: String, data: [String: Any]) {
        //: guard EqualThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(str_makeContent))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_voiceName.suffix(7)))] as? String else { return }
            //: let httpsUrl = EqualThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = EqualThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = AddConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: EditTextThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            EditTextThen.share.bileOf(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            MotilityThen.viewConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(str_selectedName) + String(str_topTableContent)).localized,
                                        //: rightBtnTitle: "Purchase".localized) {
                                        rightBtnTitle: (String(str_requestAttentionData.suffix(2)) + str_areaValue.replacingOccurrences(of: "name", with: "e")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                MotilityThen.afterward()
                //: } rightBlock: {
            } rightBlock: {
                //: EditTextThen.share.func__pushToRechargeAlert(appendParams: "&type=4")
                EditTextThen.share.pause(appendParams: (String(str_clearPathData.suffix(7))))
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func coinLocal(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_viewTempValue.suffix(5)) + String(str_imageTitle))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.buttonVoice(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.noGiftWith(point: addBean)
        }
    }
}

// MARK: - HeadingPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension PtolemaicSystemViewController: HeadingPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func sectionSocket(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_indexNeedMakeData.prefix(4)) + "has h" + String(str_reasonData.suffix(5)) + str_sizeValue.replacingOccurrences(of: "s", with: "p")).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_barData))].stringValue == RunReplaceableCollection.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_titleValue.suffix(8)) + String(str_pathName)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.oversight(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.queryEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DeviceSocketDelegate.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        jostle()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func outsideSize(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_selectedContent.map{$0^5}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.tillEnterEqual()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.acceptFor()
        //: self.createTalkRoom()
        self.atEffect()
    }
}

// MARK: - FileCurrentErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension PtolemaicSystemViewController: FileCurrentErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func constraintInsert(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_shareLastVarData.replacingOccurrences(of: "line", with: "ef") + "Call") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.oversight(showMsg: (str_frameValue.replacingOccurrences(of: "color", with: "N") + String(str_toContent.prefix(4)) + "swer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.queryEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                DeviceSocketDelegate.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.jostle()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_modelContent) + str_firstValue.replacingOccurrences(of: "time", with: "l")) && errorNo == 101 { // 接听通话时，余额不足
            //: EditTextThen.share.func__pushToRechargeAlert(appendParams: "&type=4")
            EditTextThen.share.pause(appendParams: (String(str_clearPathData.suffix(7))))

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_modelContent) + str_firstValue.replacingOccurrences(of: "time", with: "l")) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.oversight(showMsg: msg)
            //: self.popCurrentViewController()
            self.jostle()
        }
    }
}

// MARK: - NameObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension PtolemaicSystemViewController: NameObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func beforeInfo() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (str_toData.replacingOccurrences(of: "style", with: "c") + String(str_interactionName.suffix(5))), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_allowTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func tableCall() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.oversight(showMsg: (String(str_appName) + str_dayName.replacingOccurrences(of: "render", with: "a") + String(str_toName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (String(str_meText)), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_selectedContent.map{$0^5}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.oversight(showMsg: (String(str_appName) + str_dayName.replacingOccurrences(of: "render", with: "a") + String(str_toName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (str_shareLastVarData.replacingOccurrences(of: "line", with: "ef") + "Call"), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_allowTitle.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.oversight(showMsg: (String(str_labData) + String(str_pathData.suffix(4)) + "l has " + str_sourceContent.replacingOccurrences(of: "to", with: "de")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_addContent.map{asValueCell(manager: $0)}, encoding: .utf8)!: (String(str_norTitle)), String(bytes: str_tillData.map{labMake(info: $0)}, encoding: .utf8)!: [String(bytes: str_selectedContent.map{$0^5}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.aboveTable()
        //: self.popCurrentViewController()
        self.jostle()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func shouldPause(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.setFill(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func memory(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.outsideTo(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func upWindow() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        DeviceSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = TotalenanceThen.cantileverBroadcast()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.infoController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.bodyOfWater()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.moderation(talkingTime)
        //: popCurrentViewController()
        jostle()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func handleTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.moderation(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func bigness(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.smartTime(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension PtolemaicSystemViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func counteractionSubviews() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func blackBook() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
