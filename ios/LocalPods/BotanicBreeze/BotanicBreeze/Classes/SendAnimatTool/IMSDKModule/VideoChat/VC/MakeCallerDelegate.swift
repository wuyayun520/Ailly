
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_normalValue:[UInt8] = [0x2f,0x28,0x2f,0x32,0x6e,0x25,0x29,0x22,0x23,0x34,0x7c,0x6f,0x66,0x2e,0x27,0x35,0x66,0x28,0x29,0x32,0x66,0x24,0x23,0x23,0x28,0x66,0x2f,0x2b,0x36,0x2a,0x23,0x2b,0x23,0x28,0x32,0x23,0x22]

private func managerSize(game num: UInt8) -> UInt8 {
    return num ^ 70
}

/*: "uid" :*/
fileprivate let str_imageTitle:[UInt8] = [0xbf,0xa3,0xae]

/*: "headPic" :*/
fileprivate let str_byText:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let str_insideWhiteContent:[Character] = ["n"]
fileprivate let str_valueText:String = "equalkname"

/*: "birthday" :*/
fileprivate let str_userName:String = "bibag"
fileprivate let str_willValue:String = "thdagreen"

/*: "videoUrl" :*/
fileprivate let str_tapData:String = "videoUrlinfo user"

/*: "cover" :*/
fileprivate let str_enableVideoMakeText:String = "meover"

/*: "location" :*/
fileprivate let str_whoName:String = "locaarrayi"
fileprivate let str_appData:String = "ON"

/*: "cmd" :*/
fileprivate let str_kitContent:[UInt8] = [0x9a,0x94,0x9d]

private func atCookie(center num: UInt8) -> UInt8 {
    return num ^ 249
}

/*: "refuseCall" :*/
fileprivate let str_formatText:String = "index self truerefuseC"
fileprivate let str_playValue:String = "aviewview"

/*: "data" :*/
fileprivate let str_videoAttributeValue:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let str_withValue:String = "for else make viewCall i"
fileprivate let str_readTitle:String = "tiowhite"

/*: "stopCall" :*/
fileprivate let str_managerData:String = "stopCafirst for var"
fileprivate let str_fromData:String = "upup"

/*: "logId" :*/
fileprivate let str_modeValue:[UInt8] = [0x1c,0x1f,0x17,0xf9,0x14]

fileprivate func backRow(range num: UInt8) -> UInt8 {
    let value = Int(num) + 80
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let str_layerText:[Character] = ["c","a","n"]
fileprivate let str_tableCenterValue:String = "celCallindex local"

/*: "No answer" :*/
fileprivate let str_requestName:[Character] = ["N","o"," ","a","n"]
fileprivate let str_viewData:String = "ssorter"

/*: "duration" :*/
fileprivate let str_withBlockTitle:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let str_pathTitle:String = "She to share app pic direct"
fileprivate let str_toolName:String = "at model gift frame wraphung up"

/*: "cancelSex" :*/
fileprivate let str_borderText:String = "catextcel"
fileprivate let str_colorTitle:String = "content finish any statusSex"

/*: "He has hung up" :*/
fileprivate let str_timeSectionText:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_callValue:String = "input transaction heightendType"

/*: "stopSex" :*/
fileprivate let str_tableValue:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_selectedData:[UInt8] = [0x64,0x65,0x64,0x6e,0x65,0x20,0x73,0x61,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x2c,0x74,0x69,0x64,0x65,0x72,0x63,0x20,0x74,0x6e,0x65,0x69,0x63,0x69,0x66,0x66,0x75,0x73,0x6e,0x49]

/*: "The other hung up the phone" :*/
fileprivate let str_imageData:String = "The otcan color player view"
fileprivate let str_playerPublishData:String = "manager"
fileprivate let str_transformValue:String = "view data page let up t"

/*: "type" :*/
fileprivate let str_mainContent:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let str_itemCellData:String = "view info if tab itemjumpUr"
fileprivate let str_textValue:String = "L"

/*: "Cancel" :*/
fileprivate let str_shareValue:String = "Cancelreturn image"

/*: "Purchase" :*/
fileprivate let str_numberValue:String = "Purchpath cancel self author"
fileprivate let str_clickContent:[Character] = ["a","s","e"]

/*: "&type=3" :*/
fileprivate let str_succeedTitle:String = "cell app in bottom&type=3"

/*: "addBean" :*/
fileprivate let str_objectData:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let str_targetClearTitle:[Character] = ["r","e","f","u","s","e"]
fileprivate let str_familyData:[Character] = ["S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let str_videoValue:[Character] = ["o","n","A","c","c","e","p","t","C"]
fileprivate let str_ofData:[Character] = ["a","l","l"]

/*: "acceptCall" :*/
fileprivate let str_formatContent:String = "asizesizee"

/*: "Calls are cancelled" :*/
fileprivate let str_roomValue:String = "title card for toCalls"
fileprivate let str_layerData:String = "labelnce"
fileprivate let str_toData:[Character] = ["l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let str_makeSuicideTitle:String = "The true count selected text return"
fileprivate let str_toolNumberValue:String = "has endedlocal resource string show user"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MakeCallerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let kLet_imageData = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class MakeCallerDelegate: SeamViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ViewChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: CompartmentPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: PopView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: RunView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        DeviceSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DeviceSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DeviceSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_normalValue.map{managerSize(game: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! LengthArrayControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.statusDataBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            adjacent()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            barWith(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DeviceSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.sessionWith()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        pathSubviews()
        //: setupSubViewsConstraint()
        roomByConstraint()
        //: initNotificationObserver()
        addRow()
        //: req_callGetUserInfo()
        jab()
        //: timeoutRules()
        rules()
        //: setUpBGMPlayer()
        eventPlayer()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.modelAdd()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if EqualThen.share.appConfigMode.disableShootScreen {
            if EqualThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CellDataReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_cameraValue, height: kLet_clickData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        DeviceSocketDelegate.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.columnActivties()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: TalkingThen = {
        //: let v = TalkingBGVideoView()
        let v = TalkingThen()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.componentShow(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: SaveView = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = SaveView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: ToRecognizerDelegate = {
        //: let v = TalkingVideoWindowView()
        let v = ToRecognizerDelegate()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kLet_cameraValue - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if FileReactiveCompatible.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: kLet_timeSenseValue + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.placeEqual()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ShowCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = ShowCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension MakeCallerDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func jab() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ShowCloudDelegate.brandAction(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_imageTitle.map{$0^202}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_byText))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(str_insideWhiteContent) + str_valueText.replacingOccurrences(of: "equal", with: "ic"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_userName.replacingOccurrences(of: "bag", with: "r") + str_willValue.replacingOccurrences(of: "green", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_tapData.prefix(8)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(str_enableVideoMakeText.replacingOccurrences(of: "me", with: "c"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(str_whoName.replacingOccurrences(of: "array", with: "t") + str_appData.lowercased())].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.addEvent()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.notWindowStatus()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func notWindowStatus() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = AtVideoCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = AtVideoCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.showCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.reopen(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension MakeCallerDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func adjacent() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        RubricThen.andThen { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.oversight(showMsg: kLet_likeCancelData)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_formatText.suffix(7)) + str_playValue.replacingOccurrences(of: "view", with: "l")), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_imageTitle.map{$0^202}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.popDrownLab()
                //: return
                return
            }

            //: self.startPreview()
            self.modelAdd()
            //: self.stopBGM()
            self.atRow()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = CompartmentPlayListener.callBy(type: .Response)
            //: self.startBGM()
            self.statusDataBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func modelAdd() {
        //: self.videoManager.startPreview()
        self.videoManager.stochasticProcessMatch()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func columnActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.atRow()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        TextDanmuManager.resource()
        //: TalkingSocketManager.shared.callerDelegate = nil
        DeviceSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        DeviceSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        DeviceSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func popDrownLab(animated: Bool = true) {
        //: self.stopBGM()
        self.atRow()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func placeEqual() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.contentChild()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.notWindowStatus()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func savePush() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.hidden()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.notWindowStatus()
        // 展示投诉说明飘屏（男性）
//        self.show_videoFloatScreenAnimation()
    }

    /// 展示投诉说明飘屏（男性）
//    private func show_videoFloatScreenAnimation() {
//        guard reportView == nil else { return }
//
//        reportView = RunView()
//        view.addSubview(reportView!)
//        reportView?.show_animation()
//    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension MakeCallerDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func addRow() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(refuseView(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(weight(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kLet_titleValue,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(bolsterNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_deviceContent,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_imageData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func refuseView(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            popDrownLab()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func bolsterNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard DeviceSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        DeviceSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DeviceSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DeviceSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.sumView()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func weight(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.notWindowStatus()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension MakeCallerDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.oversight(showMsg: (String(str_withValue.suffix(6)) + "nterrup" + str_readTitle.replacingOccurrences(of: "white", with: "n")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_managerData.prefix(6)) + str_fromData.replacingOccurrences(of: "up", with: "l")), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_modeValue.map{backRow(range: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_layerText) + String(str_tableCenterValue.prefix(7))), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_modeValue.map{backRow(range: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_formatText.suffix(7)) + str_playValue.replacingOccurrences(of: "view", with: "l")), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_imageTitle.map{$0^202}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.queryEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                DeviceSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            popDrownLab()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension MakeCallerDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func eventPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DeepRelatableTarget.Call : DeepRelatableTarget.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = CompartmentPlayListener.callBy(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func statusDataBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.appearFor()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func atRow() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.underCancel()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension MakeCallerDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func rules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_freeName : self.chatModel?.rest()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            reportCard()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.reportCard()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func reportCard() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !DeviceSocketDelegate.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.oversight(showMsg: (String(str_requestName) + str_viewData.replacingOccurrences(of: "sort", with: "w")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_layerText) + String(str_tableCenterValue.prefix(7))), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_modeValue.map{backRow(range: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_formatText.suffix(7)) + str_playValue.replacingOccurrences(of: "view", with: "l")), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_imageTitle.map{$0^202}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        DeviceSocketDelegate.shared.noCurrent(info: socketDict)
        //: popCurrentViewController()
        popDrownLab()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension MakeCallerDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func pressDownDoing(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_modeValue.map{backRow(range: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(str_withBlockTitle))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.broadcast(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.capacity()
            //: createTalkRoom()
            savePush()
            //: self.stopBGM()
            self.atRow()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func barWith(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.contentChild()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.componentShow(show: false)
        //: self.stopBGM()
        self.atRow()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.capacity()
        //: self.createTalkRoom()
        self.savePush()
    }
}

// MARK: - GrantCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension MakeCallerDelegate: GrantCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func loungeTouch(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_pathTitle.prefix(4)) + "has " + String(str_toolName.suffix(7))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(str_borderText.replacingOccurrences(of: "text", with: "n") + String(str_colorTitle.suffix(3)))].stringValue == RunReplaceableCollection.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_timeSectionText)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.oversight(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.queryEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DeviceSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        popDrownLab()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func leadIn(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        barWith(logId: json[String(bytes: str_modeValue.map{backRow(range: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func centerData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_callValue.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_pathTitle.prefix(4)) + "has " + String(str_toolName.suffix(7))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_tableValue))].stringValue == RunReplaceableCollection.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_timeSectionText)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if EqualThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_selectedData.reversed(), encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_imageData.prefix(6)) + "her hun" + str_playerPublishData.replacingOccurrences(of: "manager", with: "g") + String(str_transformValue.suffix(5)) + "he phone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.oversight(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.queryEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DeviceSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.install()
        //: popCurrentViewController()
        popDrownLab()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func signature(msg: String, data: [String: Any]) {
        //: guard EqualThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard EqualThen.share.appStatus == PhaseOfTheMoonPropertyProtocol.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(str_mainContent))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_itemCellData.suffix(6)) + str_textValue.lowercased())] as? String else { return }
            //: let httpsUrl = EqualThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = EqualThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = AddConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: EditTextThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            EditTextThen.share.bileOf(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            MotilityThen.viewConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(str_shareValue.prefix(6))).localized,
                                        //: rightBtnTitle: "Purchase".localized) {
                                        rightBtnTitle: (String(str_numberValue.prefix(5)) + String(str_clickContent)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                MotilityThen.afterward()
                //: } rightBlock: {
            } rightBlock: {
                //: EditTextThen.share.func__pushToRechargeAlert(appendParams: "&type=3")
                EditTextThen.share.pause(appendParams: (String(str_succeedTitle.suffix(7))))
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func addWithArr(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.showArr(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func coinLocal(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_objectData))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.egoPush(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.stereoscopicPhotograph(point: addBean)
        }
    }
}

// MARK: - HeadingPartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension MakeCallerDelegate: HeadingPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func sectionSocket(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_pathTitle.prefix(4)) + "has " + String(str_toolName.suffix(7))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_targetClearTitle) + String(str_familyData))].stringValue == RunReplaceableCollection.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_timeSectionText)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.oversight(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.queryEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DeviceSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        popDrownLab()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func outsideSize(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        barWith(logId: json[String(bytes: str_modeValue.map{backRow(range: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - FileCurrentErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension MakeCallerDelegate: FileCurrentErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func constraintInsert(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(str_formatText.suffix(7)) + str_playValue.replacingOccurrences(of: "view", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.oversight(showMsg: (String(str_requestName) + str_viewData.replacingOccurrences(of: "sort", with: "w")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.queryEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                DeviceSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            popDrownLab()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_videoValue) + String(str_ofData)) && errorNo == 101 { // 接听通话时，余额不足
            //: EditTextThen.share.func__pushToRechargeAlert(appendParams: "&type=3")
            EditTextThen.share.pause(appendParams: (String(str_succeedTitle.suffix(7))))

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_videoValue) + String(str_ofData)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.oversight(showMsg: msg)
            //: popCurrentViewController()
            popDrownLab()
        }
    }
}

// MARK: - StatusObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension MakeCallerDelegate: StatusObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func sizeCall() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (str_formatContent.replacingOccurrences(of: "size", with: "c") + "ptCall"), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_imageTitle.map{$0^202}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func belowHalf() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.oversight(showMsg: (String(str_roomValue.suffix(5)) + " are c" + str_layerData.replacingOccurrences(of: "label", with: "a") + String(str_toData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_layerText) + String(str_tableCenterValue.prefix(7))), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_modeValue.map{backRow(range: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.oversight(showMsg: (String(str_roomValue.suffix(5)) + " are c" + str_layerData.replacingOccurrences(of: "label", with: "a") + String(str_toData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_formatText.suffix(7)) + str_playValue.replacingOccurrences(of: "view", with: "l")), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_imageTitle.map{$0^202}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.oversight(showMsg: (String(str_makeSuicideTitle.prefix(4)) + "call " + String(str_toolNumberValue.prefix(9))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            DeviceSocketDelegate.shared.noCurrent(info: [String(bytes: str_kitContent.map{atCookie(center: $0)}, encoding: .utf8)!: (String(str_managerData.prefix(6)) + str_fromData.replacingOccurrences(of: "up", with: "l")), String(bytes: str_videoAttributeValue.reversed(), encoding: .utf8)!: [String(bytes: str_modeValue.map{backRow(range: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        popDrownLab()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func nameFront(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.trademarkColor(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func marque(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.componentShow(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.employ(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.notWindowStatus()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func miniMake() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        DeviceSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = PopView.titleError()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.complexionMode()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.someMakeMode()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.infoController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        popDrownLab()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func doingApp() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension MakeCallerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func pathSubviews() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.stageView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func roomByConstraint() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
