
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_statusName:[UInt8] = [0x68,0x6f,0x68,0x75,0x29,0x62,0x6e,0x65,0x64,0x73,0x3b,0x28,0x21,0x69,0x60,0x72,0x21,0x6f,0x6e,0x75,0x21,0x63,0x64,0x64,0x6f,0x21,0x68,0x6c,0x71,0x6d,0x64,0x6c,0x64,0x6f,0x75,0x64,0x65]

private func orderedSeries(player num: UInt8) -> UInt8 {
    return num ^ 1
}

/*: "Text greeting" :*/
fileprivate let str_giftPathData:String = "clear equalText"

/*: "Add" :*/
fileprivate let str_modeTitle:String = "enter self clear inAdd"

/*: "type" :*/
fileprivate let str_equalContent:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let str_sizeAddTitle:String = "backgroundd"
fileprivate let str_byName:String = "first let shared lab systemdStatus"

/*: "list" :*/
fileprivate let str_viewValue:String = "lmanagert"

/*: "Are you sure to delete this message？" :*/
fileprivate let str_sizeData:[UInt8] = [0x99,0xca,0xbd,0x78,0xd1,0xc7,0xcd,0x78,0xcb,0xcd,0xca,0xbd,0x78,0xcc,0xc7,0x78,0xbc,0xbd,0xc4,0xbd,0xcc,0xbd,0x78,0xcc,0xc0,0xc1,0xcb,0x78,0xc5,0xbd,0xcb,0xcb,0xb9,0xbf,0xbd,0x47,0x14,0xf7]

fileprivate func memoryImage(across num: UInt8) -> UInt8 {
    let value = Int(num) - 88
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let str_withName:String = "Cancelview super return"

/*: "Delete" :*/
fileprivate let str_titleLabText:String = "up data mediumDelete"

/*: "id" :*/
fileprivate let str_succeedData:String = "iconversation"

/*: "The upper limit has been reached" :*/
fileprivate let str_indicatorLayerTitle:[UInt8] = [0xd8,0xec,0xe9,0xa4,0xf9,0xf4,0xf4,0xe9,0xf6,0xa4,0xf0,0xed,0xf1,0xed,0xf8,0xa4,0xec,0xe5,0xf7,0xa4,0xe6,0xe9,0xe9,0xf2,0xa4,0xf6,0xe9,0xe5,0xe7,0xec,0xe9,0xe8]

fileprivate func makeInfo(size num: UInt8) -> UInt8 {
    let value = Int(num) + 124
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "text" :*/
fileprivate let str_systemText:String = "TEXT"

/*: "Successfully added, please wait for approval" :*/
fileprivate let str_appValue:[UInt8] = [0x51,0x77,0x61,0x61,0x67,0x71,0x71,0x64,0x77,0x6e,0x6e,0x7b,0x22,0x63,0x66,0x66,0x67,0x66,0x2e,0x22,0x72,0x6e,0x67,0x63,0x71,0x67,0x22,0x75,0x63,0x6b,0x76,0x22,0x64,0x6d,0x70,0x22,0x63,0x72,0x72,0x70,0x6d,0x74,0x63,0x6e]

private func collectionPath(text num: UInt8) -> UInt8 {
    return num ^ 2
}

/*: "Any-Hex/Java" :*/
fileprivate let str_detailData:String = "Any-Hexkey inside let status data"
fileprivate let str_sText:String = "/Javaleft view index"

/*: "ListViewCell" :*/
fileprivate let str_purchaseValue:[Character] = ["T","a","l","k","i","n","g","G","r"]
fileprivate let str_languageName:String = "to"
fileprivate let str_textData:String = "etTextvalue self beauty view let"

/*: "#FF506D" :*/
fileprivate let str_labTitle:String = "frame left data quality count#FF506"
fileprivate let str_packName:[Character] = ["D"]

/*: "btn_message_delete_nor" :*/
fileprivate let str_sendName:String = "btn_meevery true view tap"
fileprivate let str_addData:String = "moment presentationdelete_n"
fileprivate let str_tapValue:String = "ping"

/*: "Click the button below to add\ncontent" :*/
fileprivate let str_imageText:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63,0xa,0x64,0x64,0x61,0x20,0x6f,0x74,0x20,0x77,0x6f,0x6c,0x65,0x62,0x20,0x6e,0x6f,0x74,0x74,0x75,0x62,0x20,0x65,0x68,0x74,0x20,0x6b,0x63,0x69,0x6c,0x43]

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let str_sizeContent:[UInt8] = [0xc5,0xfa,0xf7,0xfc,0xb2,0xf3,0xf6,0xf6,0xfb,0xfc,0xf5,0xb2,0xff,0xe7,0xfe,0xe6,0xfb,0xe2,0xfe,0xf7,0xb2,0xe6,0xf7,0xea,0xe6,0xb2,0xff,0xf7,0xe1,0xe1,0xf3,0xf5,0xf7,0xbe,0xb2,0xe6,0xfa,0xf7,0xb2,0xe1,0xeb,0xe1,0xe6,0xb2,0xf7,0xff,0xb2,0xe5,0xfb,0xfe,0xfe,0xb2,0xe1,0xf7,0xfc,0xf6,0xb2,0xe6,0xfa,0xf7,0xff,0xb2,0xf0,0xeb,0xb2,0xe6,0xe7,0xe0,0xfc,0xe1,0xa9,0xe1,0xfe,0xfb,0xf6,0xf7,0xb2,0xe6,0xfd,0xb2,0xe6,0xfa,0xf7,0xb2,0xff,0xf7,0xe1,0xe1,0xf3,0xf5,0xf7,0xb2,0xe6,0xfd,0xb2,0xf6,0xf7,0xfe,0xf7,0xe6,0xf7]

private func objectMain(frame num: UInt8) -> UInt8 {
    return num ^ 146
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PopViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class PopViewController: SeamViewController {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: WithGestureView?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_statusName.map{orderedSeries(player: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(str_giftPathData.suffix(4)) + " greeting").localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.imageColor()
        //: designView()
        record()
        //: getAudioListData()
        time()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [OnHandyJSON] = {
        //: var array = [TalkingGreetModel]()
        var array = [OnHandyJSON]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_cameraValue, height: kLet_clickData - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(str_modeTitle.suffix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.pingfang(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.makeBackground(colors: UIColor.appToVoice(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(hemlineFixingClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension PopViewController {
    //: func getAudioListData() {
    func time() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(String(str_equalContent))] = 2
        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        CameraThen.fall(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            ModelReactiveCompatible.dataConverterCan()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.halt()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(str_sizeAddTitle.replacingOccurrences(of: "background", with: "a") + String(str_byName.suffix(7)))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(str_viewValue.replacingOccurrences(of: "manager", with: "is"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [OnHandyJSON] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<OnHandyJSON>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func will(index: Int) {
        //: let config = ShowAlertConfig()
        let config = AtAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.pingfang(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        MotilityThen.customLabel(message: String(bytes: str_sizeData.map{memoryImage(across: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(str_withName.prefix(6))).localized, rightBtnTitle: (String(str_titleLabText.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            MotilityThen.afterward()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            MotilityThen.afterward()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: OnHandyJSON = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            ModelReactiveCompatible.fromTarget()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            CameraThen.live(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ModelReactiveCompatible.dataConverterCan()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.time()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension PopViewController {
    //: @objc func addButtonClick() {
    @objc func hemlineFixingClick() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.oversight(showMsg: String(bytes: str_indicatorLayerTitle.map{makeInfo(size: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = WithGestureView(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.singingVoiceShow()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.cuttingEdge(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(String(str_equalContent))] = 2
            //: dict["text"] = tempStr
            dict[(str_systemText.lowercased())] = tempStr
            //: ProgressHUD.show()
            ModelReactiveCompatible.fromTarget()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            CameraThen.dataConverterWith(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ModelReactiveCompatible.dataConverterCan()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.fileFirst(showMsg: String(bytes: str_appValue.map{collectionPath(text: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.time()
                //: self.recordView?.dismiss()
                self.recordView?.addView()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func cuttingEdge(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(str_detailData.prefix(7)) + String(str_sText.prefix(5))) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension PopViewController: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: kLet_cameraValue - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfang(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetTextCell"
        let identifier = (String(str_purchaseValue) + str_languageName.replacingOccurrences(of: "to", with: "e") + String(str_textData.prefix(6)) + "Cell")
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: ListViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! ListViewCell

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.viewInDeadline(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(str_titleLabText.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.will(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(str_labTitle.suffix(6)) + String(str_packName)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.will(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.eponymUp(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.eponymUp(name: (String(str_sendName.prefix(6)) + "ssage_" + String(str_addData.suffix(8)) + str_tapValue.replacingOccurrences(of: "ping", with: "or"))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = ImageReactiveCompatible(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension PopViewController {
    //: private func setupEmptyDataView() {
    private func halt() {
        //: var style = EmptyStyle()
        var style = ShadowViewEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: str_imageText.reversed(), encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .imageColor()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func record() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: "TalkingGreetTextCell")
        MainTable.register(ListViewCell.self, forCellReuseIdentifier: (String(str_purchaseValue) + str_languageName.replacingOccurrences(of: "to", with: "e") + String(str_textData.prefix(6)) + "Cell"))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: kLet_cameraValue, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.publishView()
        label.textColor = UIColor.publishView()
        //: label.font = UIFont.outsideAllSize(fontSize: 15)
        label.font = UIFont.outsideAllSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: str_sizeContent.map{objectMain(frame: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
