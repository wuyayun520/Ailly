
//: Declare String Begin

/*: "nil" :*/
fileprivate let str_selectedName:String = "nlayer"

/*: "Edit profiles" :*/
fileprivate let str_voiceData:String = "empty view return make heightEdi"
fileprivate let str_whiteData:String = "t prosize if if size manager"

/*: "Done" :*/
fileprivate let str_sharedText:[Character] = ["D","o","n","e"]

/*: "#999999" :*/
fileprivate let str_sureTitle:String = "#sessionsessionsessionsessionsessionsession"

/*: "uid" :*/
fileprivate let str_keyData:String = "uinormal"

/*: "AtOnViewCell" :*/
fileprivate let str_textTitle:String = "Talkingarray return make"
fileprivate let str_aData:String = "normal female statusEdit"

/*: "HeadTitleThen" :*/
fileprivate let str_countData:String = "return let itemTalki"
fileprivate let str_equalData:String = "tSignCline area add"
fileprivate let str_modelData:[Character] = ["e","l","l"]

/*: "Save the change?" :*/
fileprivate let str_nextTitle:String = "text self text bottomSave "
fileprivate let str_toText:String = "lab card manager table targetthe "

/*: "Cancel" :*/
fileprivate let str_videoTitle:String = "c"
fileprivate let str_withTitle:[Character] = ["a","n","c","e","l"]

/*: "Save" :*/
fileprivate let str_lastValue:[Character] = ["S","a","v","e"]

/*: "nickname" :*/
fileprivate let str_dataName:String = "normalick"

/*: "birthday" :*/
fileprivate let str_labValue:String = "changeirthday"

/*: "desc" :*/
fileprivate let str_premiumData:String = "deslabel"

/*: "Modify the success" :*/
fileprivate let str_pointRefreshName:[Character] = ["M","o","d","i","f","y"," ","t"]
fileprivate let str_frameText:String = "he suview self"
fileprivate let str_willData:String = "ccetop"

/*: "pic" :*/
fileprivate let str_tableName:String = "pmodel"

/*: "[ :*/
fileprivate let str_sizeTargetTitle:String = "live"

/*: ]" :*/
fileprivate let str_timeKeyData:String = "]"

/*: "pids" :*/
fileprivate let str_postData:String = "pidpage"

/*: "aboutMe" :*/
fileprivate let str_kindName:String = "ablackut"
fileprivate let str_backgroundName:String = "Medetail attribute value"

/*: "interest" :*/
fileprivate let str_cellMainValue:[Character] = ["i","n","t"]
fileprivate let str_indicatorGreenTextValue:[Character] = ["e","r","e","s","t"]

/*: "tagIds" :*/
fileprivate let str_userValue:String = "tagIdi range on"
fileprivate let str_componentTitle:String = "succeed"

/*: "category" :*/
fileprivate let str_backName:[Character] = ["c","a","t","e","g","o","r"]
fileprivate let str_titleCellContent:[Character] = ["y"]

/*: "An error occurred, please try again" :*/
fileprivate let str_contentData:[UInt8] = [0x59,0x86,0x38,0x7d,0x8a,0x8a,0x87,0x8a,0x38,0x87,0x7b,0x7b,0x8d,0x8a,0x8a,0x7d,0x7c,0x44,0x38,0x88,0x84,0x7d,0x79,0x8b,0x7d,0x38,0x8c,0x8a,0x91,0x38,0x79,0x7f,0x79,0x81,0x86]

fileprivate func changePhoto(empty num: UInt8) -> UInt8 {
    let value = Int(num) - 24
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "imgUrl" :*/
fileprivate let str_selectedValue:[Character] = ["i","m","g","U","r","l"]

/*: "videoUrl" :*/
fileprivate let str_recordingContent:String = "imaged"
fileprivate let str_centerValue:String = "dismiss view application table usereoUrl"

/*: "Video cover submitted successfully" :*/
fileprivate let str_toTitle:[UInt8] = [0x4f,0x62,0x5d,0x5e,0x68,0x19,0x5c,0x68,0x6f,0x5e,0x6b,0x19,0x6c,0x6e,0x5b,0x66,0x62,0x6d,0x6d,0x5e,0x5d,0x19,0x6c,0x6e,0x5c,0x5c,0x5e,0x6c,0x6c,0x5f,0x6e,0x65,0x65,0x72]

fileprivate func areaDirection(total num: UInt8) -> UInt8 {
    let value = Int(num) + 7
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "EventWithVideoCell" :*/
fileprivate let str_errValue:[Character] = ["T","a","l","k","i","n","g","E","d","i","t"]
fileprivate let str_makeName:[Character] = ["V","i","d","e","o","C","e","l","l"]

/*: "video_url" :*/
fileprivate let str_scheduleTitle:String = "transformide"
fileprivate let str_sizeName:String = "view"

/*: "img_url" :*/
fileprivate let str_signValue:[Character] = ["i","m","g","_","u","r","l"]

/*: "status" :*/
fileprivate let str_effectData:String = "sroomtus"

/*: "PointOfDepartureViewCell" :*/
fileprivate let str_lessAddReturnData:String = "comment gift class trueTalkingE"
fileprivate let str_imageName:[Character] = ["d","i","t","P","h","o","t","o","C","e","l","l"]

/*: "MakeThen" :*/
fileprivate let str_blockValue:String = "Talkintext time self make"
fileprivate let str_cloudData:String = "push modelgEditB"
fileprivate let str_ofValue:[Character] = ["i","r","t","h","d","a","y","C","e","l","l"]

/*: "FreeCopyreadOrSoMeCompartmentView" :*/
fileprivate let str_automaticallyToData:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","A","b","o","u","t","M","e","C","e"]
fileprivate let str_modelTableData:[Character] = ["l","l"]

/*: "UITableViewCell" :*/
fileprivate let str_kitText:[Character] = ["U","I","T","a","b"]
fileprivate let str_tripValue:[Character] = ["l","e","V","i","e","w","C","e","l","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CurrentReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class CurrentReactiveCompatible: SeamViewController {
    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [OfHandyJSON] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [OfHandyJSON] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [TargetTitleHandyJSON] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (str_selectedName.replacingOccurrences(of: "layer", with: "il"))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.imageColor()
        //: self.title = "Edit profiles".localized
        self.title = (String(str_voiceData.suffix(3)) + String(str_whiteData.prefix(5)) + "files").localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(str_sharedText)).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (str_sureTitle.replacingOccurrences(of: "session", with: "9"))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.pingfang(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(supplement), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = EqualThen.share.loginUserMode.aboutMe!
        seleteAboutme = EqualThen.share.loginUserMode.aboutMe!
        //: seleteInters = EqualThen.share.loginUserMode.interest!
        seleteInters = EqualThen.share.loginUserMode.interest!
        //: designView()
        petTitle()
        //: GetGallery()
        ofGallery()
        //: getVideoInfo()
        coloredModel()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(sunna(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        ModelReactiveCompatible.dataConverterCan()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_cameraValue, height: kLet_clickData), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension CurrentReactiveCompatible {
    //: func GetGallery() {
    func ofGallery() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = EqualThen.share.loginUserMode.userID
        dict[(str_keyData.replacingOccurrences(of: "normal", with: "d"))] = EqualThen.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        CameraThen.baptise(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<TargetTitleHandyJSON>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [TargetTitleHandyJSON])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func white() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_textTitle.prefix(7)) + String(str_aData.suffix(4)) + "NameCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AtOnViewCell = i as! AtOnViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_countData.suffix(5)) + "ngEdi" + String(str_equalData.prefix(6)) + String(str_modelData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: HeadTitleThen = i as! HeadTitleThen
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&EqualThen.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&EqualThen.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && EqualThen.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && EqualThen.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && EqualThen.share.loginUserMode.birthday != self.birthday) || (signStr != (str_selectedName.replacingOccurrences(of: "layer", with: "il")) && EqualThen.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = AtAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            MotilityThen.customLabel(message: (String(str_nextTitle.suffix(5)) + String(str_toText.suffix(4)) + "change?").localized, leftBtnTitle: (str_videoTitle.uppercased() + String(str_withTitle)).localized, rightBtnTitle: (String(str_lastValue)).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                MotilityThen.afterward()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                MotilityThen.afterward()
                //: self.uploadTool(isBack: true)
                self.rounder(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func supplement() {
        //: uploadTool(isBack: false)
        rounder(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func rounder(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_textTitle.prefix(7)) + String(str_aData.suffix(4)) + "NameCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AtOnViewCell = i as! AtOnViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_countData.suffix(5)) + "ngEdi" + String(str_equalData.prefix(6)) + String(str_modelData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: HeadTitleThen = i as! HeadTitleThen
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && EqualThen.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, EqualThen.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(str_dataName.replacingOccurrences(of: "normal", with: "n") + "name")] = nameStr
        }
        //: if birthday.count > 0 && EqualThen.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, EqualThen.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(str_labValue.replacingOccurrences(of: "change", with: "b"))] = birthday
        }
        //: if signStr != "nil" && EqualThen.share.loginUserMode.signature != signStr {
        if signStr != (str_selectedName.replacingOccurrences(of: "layer", with: "il")), EqualThen.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(str_premiumData.replacingOccurrences(of: "label", with: "c"))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        aboveTouch(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && EqualThen.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, EqualThen.share.loginUserMode.sex == RunReplaceableCollection.female.rawValue {
            //: uploadVideo()
            of()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        CameraThen.underbelly(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            ModelReactiveCompatible.dataConverterCan()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.fileFirst(showMsg: (String(str_pointRefreshName) + String(str_frameText.prefix(5)) + str_willData.replacingOccurrences(of: "top", with: "ss")).localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: EqualThen.share.loginUserMode.nickname = self.nameStr
                    EqualThen.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: EqualThen.share.loginUserMode.birthday = self.birthday
                    EqualThen.share.loginUserMode.birthday = self.birthday
                }
                //: EqualThen.share.loginUserMode.signature = self.signStr
                EqualThen.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.oversight(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func aboveTouch(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            sinceShared()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                ignitionKey()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            sub(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            sub(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func ignitionKey() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].key()! as NSData
            //: dict["pic"] = resultData
            dict[(str_tableName.replacingOccurrences(of: "model", with: "ic"))] = resultData
            //: ProgressHUD.show()
            ModelReactiveCompatible.fromTarget()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            CameraThen.modelCompletion(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                ModelReactiveCompatible.dataConverterCan()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.fileFirst(showMsg: (String(str_pointRefreshName) + String(str_frameText.prefix(5)) + str_willData.replacingOccurrences(of: "top", with: "ss")).localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.ofGallery()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func sinceShared() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(str_postData.replacingOccurrences(of: "page", with: "s"))] = str
        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        CameraThen.fact(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.ignitionKey()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                ModelReactiveCompatible.dataConverterCan()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.fileFirst(showMsg: (String(str_pointRefreshName) + String(str_frameText.prefix(5)) + str_willData.replacingOccurrences(of: "top", with: "ss")).localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.ofGallery()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func sub(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [OfHandyJSON] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (str_kindName.replacingOccurrences(of: "black", with: "bo") + String(str_backgroundName.prefix(2)))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (String(str_cellMainValue) + String(str_indicatorGreenTextValue))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(str_userValue.prefix(5)) + str_componentTitle.replacingOccurrences(of: "succeed", with: "s"))] = str
        //: dict["category"] = category
        dict[(String(str_backName) + String(str_titleCellContent))] = category
        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        CameraThen.mentalPicture(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            ModelReactiveCompatible.dataConverterCan()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.fileFirst(showMsg: (String(str_pointRefreshName) + String(str_frameText.prefix(5)) + str_willData.replacingOccurrences(of: "top", with: "ss")).localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: EqualThen.share.loginUserMode.aboutMe = self.seleteAboutme
                    EqualThen.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: EqualThen.share.loginUserMode.interest = self.seleteInters
                    EqualThen.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func of() {
        //: ProgressHUD.show()
        ModelReactiveCompatible.fromTarget()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        ListReactiveCompatible.shared.nextIndex(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                ModelReactiveCompatible.dataConverterCan()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.oversight(showMsg: String(bytes: str_contentData.map{changePhoto(empty: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(str_selectedValue))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(str_recordingContent.replacingOccurrences(of: "image", with: "vi") + String(str_centerValue.suffix(5)))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            CameraThen.somebody(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                ModelReactiveCompatible.dataConverterCan()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.oversight(showMsg: String(bytes: str_contentData.map{changePhoto(empty: $0)}, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.fileFirst(showMsg: String(bytes: str_toTitle.map{areaDirection(total: $0)}, encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(str_errValue) + String(str_makeName)) {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: EventWithVideoCell = i as! EventWithVideoCell
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.look(videoPlayPath: dict[(str_recordingContent.replacingOccurrences(of: "image", with: "vi") + String(str_centerValue.suffix(5)))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.componentAcross(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func coloredModel() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        CameraThen.fee { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(str_scheduleTitle.replacingOccurrences(of: "transform", with: "v") + "o_ur" + str_sizeName.replacingOccurrences(of: "view", with: "l"))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(str_signValue))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(str_effectData.replacingOccurrences(of: "room", with: "ta"))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension CurrentReactiveCompatible {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func sunna(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_textTitle.prefix(7)) + String(str_aData.suffix(4)) + "NameCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AtOnViewCell = i as! AtOnViewCell
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_countData.suffix(5)) + "ngEdi" + String(str_equalData.prefix(6)) + String(str_modelData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: HeadTitleThen = i as! HeadTitleThen
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension CurrentReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if EqualThen.share.loginUserMode.sex == Gender.female.rawValue {
            if EqualThen.share.loginUserMode.sex == RunReplaceableCollection.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((kLet_cameraValue - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.beforeEqual(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.beforeEqual(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(str_errValue) + String(str_makeName))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? EventWithVideoCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = EventWithVideoCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.componentAcross(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.toPath(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.look(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(str_lessAddReturnData.suffix(8)) + String(str_imageName))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? PointOfDepartureViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = PointOfDepartureViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.photomosaicMore(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.makeTo()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(str_textTitle.prefix(7)) + String(str_aData.suffix(4)) + "NameCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AtOnViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = AtOnViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(str_blockValue.prefix(6)) + String(str_cloudData.suffix(6)) + String(str_ofValue))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? MakeThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = MakeThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(str_countData.suffix(5)) + "ngEdi" + String(str_equalData.prefix(6)) + String(str_modelData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? HeadTitleThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = HeadTitleThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(str_automaticallyToData) + String(str_modelTableData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? FreeCopyreadOrSoMeCompartmentView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = FreeCopyreadOrSoMeCompartmentView(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.outsideName()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.imbricateUpwardly(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.imbricateUpwardly(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = UserViewDelegate()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.tagObject(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.tagObject(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.tagObject(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.tagObject(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(str_kitText) + String(str_tripValue))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func beforeEqual(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [OfHandyJSON]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if SendUpReactiveCompatible.share.interfaceLang == EditTermConvertible.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if SendUpReactiveCompatible.share.interfaceLang == EditTermConvertible.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if SendUpReactiveCompatible.share.interfaceLang == EditTermConvertible.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfang(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > kLet_cameraValue - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - TempleTagDelegate

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension CurrentReactiveCompatible: TempleTagDelegate {
    //: func freshSeleteTag() {
    func unspoiltColorTag() {
        //: seleteAboutme =  EqualThen.share.loginUserMode.aboutMe!
        seleteAboutme = EqualThen.share.loginUserMode.aboutMe!
        //: seleteInters =  EqualThen.share.loginUserMode.interest!
        seleteInters = EqualThen.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - PhotoReactiveCompatible

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension CurrentReactiveCompatible: PhotoReactiveCompatible {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func framePhoto(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = TargetTitleHandyJSON()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func world(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func dataConverterGift(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension CurrentReactiveCompatible {
    //: private func designView() {
    private func petTitle() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(EventWithVideoCell.self, forCellReuseIdentifier: (String(str_errValue) + String(str_makeName)))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(PointOfDepartureViewCell.self, forCellReuseIdentifier: (String(str_lessAddReturnData.suffix(8)) + String(str_imageName)))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(AtOnViewCell.self, forCellReuseIdentifier: (String(str_textTitle.prefix(7)) + String(str_aData.suffix(4)) + "NameCell"))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(MakeThen.self, forCellReuseIdentifier: (String(str_blockValue.prefix(6)) + String(str_cloudData.suffix(6)) + String(str_ofValue)))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(HeadTitleThen.self, forCellReuseIdentifier: (String(str_countData.suffix(5)) + "ngEdi" + String(str_equalData.prefix(6)) + String(str_modelData)))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(FreeCopyreadOrSoMeCompartmentView.self, forCellReuseIdentifier: (String(str_automaticallyToData) + String(str_modelTableData)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
