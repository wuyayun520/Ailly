
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_requestName:[UInt8] = [0xff,0x4,0xff,0xa,0xbe,0xf9,0x5,0xfa,0xfb,0x8,0xd0,0xbf,0xb6,0xfe,0xf7,0x9,0xb6,0x4,0x5,0xa,0xb6,0xf8,0xfb,0xfb,0x4,0xb6,0xff,0x3,0x6,0x2,0xfb,0x3,0xfb,0x4,0xa,0xfb,0xfa]

fileprivate func barScale(equal num: UInt8) -> UInt8 {
    let value = Int(num) + 106
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let str_recordingData:[Character] = ["C","a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let str_likeData:String = "lab return stackSubmit"

/*: "Please tell us your problems                   " :*/
fileprivate let str_fromName:[UInt8] = [0xc0,0xfc,0xf5,0xf1,0xe3,0xf5,0xb0,0xe4,0xf5,0xfc,0xfc,0xb0,0xe5,0xe3,0xb0,0xe9,0xff,0xe5,0xe2,0xb0,0xe0,0xe2,0xff,0xf2,0xfc,0xf5,0xfd,0xe3,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0,0xb0]

/*: "You need to choose at least one." :*/
fileprivate let str_textTitle:[UInt8] = [0x48,0x5e,0x64,0xf,0x5d,0x54,0x54,0x53,0xf,0x63,0x5e,0xf,0x52,0x57,0x5e,0x5e,0x62,0x54,0xf,0x50,0x63,0xf,0x5b,0x54,0x50,0x62,0x63,0xf,0x5e,0x5d,0x54,0x1d]

fileprivate func momentImage(to num: UInt8) -> UInt8 {
    let value = Int(num) + 17
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Please tell us your problems." :*/
fileprivate let str_directionData:String = "register asPleas"
fileprivate let str_modelPhotoName:[Character] = [" ","u","s"," ","y","o","u","r"," ","p","r","o","b","l","e","m","s","."]

/*: "app/userReport" :*/
fileprivate let str_equalTitle:String = "allow scale info hidden balanceapp/"
fileprivate let str_cloudAddTitle:String = "norport"

/*: "id" :*/
fileprivate let str_makeValue:[UInt8] = [0x95,0x98]

/*: "reason" :*/
fileprivate let str_buttonTargetData:String = "REASON"

/*: "desc" :*/
fileprivate let str_viewData:[UInt8] = [0x6d,0x6e,0x7c,0x6c]

fileprivate func greetModel(raw num: UInt8) -> UInt8 {
    let value = Int(num) - 9
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/report" :*/
fileprivate let str_genderData:String = "source equalmf/mom"
fileprivate let str_fileName:String = "equalport"

/*: "momentId" :*/
fileprivate let str_topCenterLetTitle:[UInt8] = [0x6f,0x6d,0x6f,0x67,0x6c,0x76,0x4b,0x66]

/*: "mf/momentReply/report" :*/
fileprivate let str_groupText:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p"]
fileprivate let str_originText:[Character] = ["o","r","t"]

/*: "app/videoReport" :*/
fileprivate let str_insideName:String = "app/vitime other let push"
fileprivate let str_changeData:String = "model"

/*: "logId" :*/
fileprivate let str_arrayName:[UInt8] = [0xeb,0xe8,0xe0,0xce,0xe3]

/*: "Submitted successfully" :*/
fileprivate let str_contactViewGroupText:String = "let live section top letSubmit"
fileprivate let str_hiddenData:[Character] = ["t","e","d"," ","s","u","c","c"]
fileprivate let str_modelRequestName:String = "elivelivefully"

/*: "Nudity" :*/
fileprivate let str_indexManagerData:String = "Nuditybar if true"

/*: "Violence" :*/
fileprivate let str_senseName:String = "view equal view appViolence"

/*: "Harassment" :*/
fileprivate let str_priceTitle:String = "in roomHar"
fileprivate let str_controlValue:String = "ASSMENT"

/*: "Suicide or Self-injury" :*/
fileprivate let str_modelData:String = "sex make effectSuicid"
fileprivate let str_modeContent:String = "let true Sel"

/*: "False Information" :*/
fileprivate let str_contentData:String = "Falsenormal part bottom page height"
fileprivate let str_matchValue:String = "MATION"

/*: "Spam" :*/
fileprivate let str_frameContent:[Character] = ["S","p","a","m"]

/*: "Hate Speech" :*/
fileprivate let str_replyTitle:String = "h"
fileprivate let str_gestureRefreshValue:String = "ate send mode play data image"

/*: "Terrorism" :*/
fileprivate let str_infoTitle:String = "Terrself return generate data"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let str_reasonName:[UInt8] = [0x59,0x67,0x72,0x6b,0x26,0x75,0x6c,0x26,0x4f,0x72,0x72,0x6b,0x6d,0x67,0x72,0x26,0x75,0x78,0x26,0x58,0x6b,0x6d,0x7b,0x72,0x67,0x7a,0x6b,0x6a,0x26,0x4d,0x75,0x75,0x6a,0x79]

fileprivate func mainSize(view num: UInt8) -> UInt8 {
    let value = Int(num) + 250
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let str_labColorData:String = "index bag upSendi"
fileprivate let str_showValue:String = "ng dvoice manager view"
fileprivate let str_valueText:String = "stview"
fileprivate let str_upTitle:String = "sagbottoms"

/*: "Using photos of nudity or drugs" :*/
fileprivate let str_colorData:[UInt8] = [0x44,0x62,0x78,0x7f,0x76,0x31,0x61,0x79,0x7e,0x65,0x7e,0x62,0x31,0x7e,0x77,0x31,0x7f,0x64,0x75,0x78,0x65,0x68,0x31,0x7e,0x63,0x31,0x75,0x63,0x64,0x76,0x62]

private func sizeEqual(cell num: UInt8) -> UInt8 {
    return num ^ 17
}

/*: "Harassing or sending ads" :*/
fileprivate let str_infoTextAsValue:String = "cell lab interaction labHara"
fileprivate let str_itemText:String = " or stext shadow time"
fileprivate let str_toValue:String = "point content replyg ads"

/*: "Defraud or using fake info" :*/
fileprivate let str_makeActualData:String = "Defrain user time up render"
fileprivate let str_afterValue:String = "USING"
fileprivate let str_toTitle:String = "value value info"

/*: "Others" :*/
fileprivate let str_colorUserValue:String = "o"
fileprivate let str_appData:String = "THERS"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WithSkinViewDelegate.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import UIKit
import UIKit

//: import Then
import Then

//: enum TalkingReportAlertType: CaseIterable {
enum BreedCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class WithSkinViewDelegate: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: BreedCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [UserReactiveCompatible] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = UserReactiveCompatible()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: BreedCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        searchBottomAnalogDigitalConverter()
        //: setupSubViewsConstraint()
        equalDate()
        //: setupReasons()
        past()
        //: bindInteraction()
        card()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_requestName.map{barScale(equal: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(str_recordingData)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.plaster(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.pingfang(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.plaster().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(nest), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(str_likeData.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.pingfang(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.makeBackground(colors: UIColor.appToVoice(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(pic), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: kLet_cameraValue - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.showSub(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.publishView(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = DraftCopyReactiveCompatible(placeholder: String(bytes: str_fromName.map{$0^144}, encoding: .utf8)!.localized, placeholderColor: UIColor.publishView(), frame: CGRect(x: 10, y: 10, width: kLet_cameraValue - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .pingfang(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.executeTo()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .pingfang(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.showSub(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(KeyThen.self, forCellReuseIdentifier: KeyThen.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension WithSkinViewDelegate {
    //: @objc func func__TapGestureRecognizer() {
    @objc func buttonRecognizer() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func withPair(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = MacroDefine.getWindow()
            let window = UserMacroDefine.toUp()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func brushDoingApp(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func nest() {
        //: self.dismissReportView(succeed: false)
        self.brushDoingApp(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func pic() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.oversight(showMsg: String(bytes: str_textTitle.map{momentImage(to: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.oversight(showMsg: (String(str_directionData.suffix(5)) + "e tell" + String(str_modelPhotoName)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(str_equalTitle.suffix(4)) + "userR" + str_cloudAddTitle.replacingOccurrences(of: "nor", with: "e"))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (str_buttonTargetData.lowercased()): selectedModel.type, String(bytes: str_viewData.map{greetModel(raw: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(str_genderData.suffix(6)) + "ent/" + str_fileName.replacingOccurrences(of: "equal", with: "re"))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: str_topCenterLetTitle.map{$0^2}, encoding: .utf8)!: tempId, (str_buttonTargetData.lowercased()): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(str_groupText) + String(str_originText))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (str_buttonTargetData.lowercased()): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(str_insideName.prefix(6)) + "deoRepor" + str_changeData.replacingOccurrences(of: "model", with: "t"))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: str_arrayName.map{$0^135}, encoding: .utf8)!: tempId, (str_buttonTargetData.lowercased()): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        TitleEqualRequestManager.upEnable(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.fileFirst(showMsg: (String(str_contactViewGroupText.suffix(6)) + String(str_hiddenData) + str_modelRequestName.replacingOccurrences(of: "live", with: "s")).localized)
                //: self.dismissReportView(succeed: true)
                self.brushDoingApp(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension WithSkinViewDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: KeyThen.className()) as! KeyThen
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.theme(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension WithSkinViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func searchBottomAnalogDigitalConverter() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func equalDate() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func past() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(str_indexManagerData.prefix(6))).localized,
                           //: "Violence".localized,
                           (String(str_senseName.suffix(8))).localized,
                           //: "Harassment".localized,
                           (String(str_priceTitle.suffix(3)) + str_controlValue.lowercased()).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(str_modelData.suffix(6)) + "e or" + String(str_modeContent.suffix(4)) + "f-injury").localized,
                           //: "False Information".localized,
                           (String(str_contentData.prefix(5)) + " Infor" + str_matchValue.lowercased()).localized,
                           //: "Spam".localized,
                           (String(str_frameContent)).localized,
                           //: "Hate Speech".localized,
                           (str_replyTitle.uppercased() + String(str_gestureRefreshValue.prefix(4)) + "Speech").localized,
                           //: "Terrorism".localized,
                           (String(str_infoTitle.prefix(4)) + "orism").localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: str_reasonName.map{mainSize(view: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(str_labColorData.suffix(5)) + String(str_showValue.prefix(4)) + "isgu" + str_valueText.replacingOccurrences(of: "view", with: "in") + "g mes" + str_upTitle.replacingOccurrences(of: "bottom", with: "e")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: str_colorData.map{sizeEqual(cell: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(str_infoTextAsValue.suffix(4)) + "ssing" + String(str_itemText.prefix(5)) + "endin" + String(str_toValue.suffix(5))).localized,
                           //: "Defraud or using fake info".localized,
                           (String(str_makeActualData.prefix(5)) + "ud or " + str_afterValue.lowercased() + " fake" + String(str_toTitle.suffix(5))).localized,
                           //: "Others".localized]
                           (str_colorUserValue.uppercased() + str_appData.lowercased()).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = EqualThen.share.appConfigMode.videoReport["reason"] {
            if let reason = EqualThen.share.appConfigMode.videoReport[(str_buttonTargetData.lowercased())] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = UserReactiveCompatible()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func card() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
