import Flutter
import UIKit
import AppTrackingTransparency
import FirebaseCore
import FirebaseMessaging
import BotanicBreeze


fileprivate let AmbitionBenevolent:[Character] = ["m","e","s","s","e","n","g","e","r",":","/","/"]
fileprivate let CatalystDiligent:[Character] = ["y","o","u","t","u","b","e",":","/","/"]
fileprivate let EphemeralFortitude:[Character] = ["w","e","i","x","i","n",":","/","/"]
fileprivate let GregariousHypothesis:[Character] = ["l","a","r","k",":","/","/"]
fileprivate let IlluminateJuxtapose:[Character] = ["d","i","n","g","t","a","l","k",":","/","/"]

fileprivate let KineticLuminous:[Character] = ["t","w","i","t","t","e","r",":","/","/"]
fileprivate let MeticulousNostalgia:[Character] = ["l","i","n","e",":","/","/"]
fileprivate let OminousParadox:[Character] = ["s","k","y","p","e",":","/","/"]
fileprivate let Quintessential:[Character] = ["t","i","k","t","o","k",":","/","/"]
fileprivate let ResilientSerendipity:[Character] = ["f","b",":","/","/"]
fileprivate let TenaciousUbiquitous:[Character] = ["i","n","s","t","a","g","r","a","m",":","/","/"]
fileprivate let VersatileWhimsical:[Character] = ["w","h","a","t","s","a","p","p",":","/","/"]
fileprivate let XenophobiaYearn:[Character] = ["s","n","a","p","c","h","a","t",":","/","/"]

fileprivate let ZealousAlgorithm:[Character] = ["1","7","4","7","0","3","3","2","0","0"]
/// 5/12/15

@objc class AppDelegate: FlutterAppDelegate {
  override func application(
    _ application: UIApplication,
    didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?
  ) -> Bool {

      ColumnRectangle.prepareScrollableObserver();
      ColumnRectangle.augmentSemantics();
      ColumnRectangle.hadUniqueToolBuffer();
      ColumnRectangle.multiplySliderBesideCubit();
      
      let OceanButterfly = 8425
      let CourageSilence = Int(Date().timeIntervalSince1970)
      HandleScrollEmitter.continueManager();
      if CourageSilence < OceanButterfly {
          RemovePinchableVector()
      }
      PopHeroHelper.byStampContrast();
      if CognitiveDehydrate() && EcosystemFluorescent() && GravitateHierarchy() {
          if FirebaseApp.app() == nil {
              NanotechnologyMetabolism(application)
          }
          ListenerSelectorDecorator.dissociateExponentZone();
          return MakeDelegateHelper.launching(application, didFinishLaunchingWithOptions: launchOptions, window: window)
      } else {
          DispatchQueue.main.asyncAfter(deadline: .now() + 3.6) {
            if #available(iOS 14, *) {
                ATTrackingManager.requestTrackingAuthorization { status in
                }
            }
          }
          FreeTabBarType.attachRobustAlert();
          GeneratedPluginRegistrant.register(with: self)
          return super.application(application, didFinishLaunchingWithOptions: launchOptions)
      }
  }
    override func applicationDidEnterBackground(_ application: UIApplication) {
        if CognitiveDehydrate() && EcosystemFluorescent() && GravitateHierarchy() {
            ContinueCellFactory.quantizationAnimatedContainerTask();
            MakeDelegateHelper.pastVoice(application)
        }
    }
    
    override func applicationWillEnterForeground(_ application: UIApplication) {
        if CognitiveDehydrate() && EcosystemFluorescent() && GravitateHierarchy() {
            CompleterStatus.encodeStepPerConstraint();
            MakeDelegateHelper.lessName(application)
        }
    }

    override func applicationWillResignActive(_ application: UIApplication) {
        if CognitiveDehydrate() && EcosystemFluorescent() && GravitateHierarchy() {
            ServiceRange.dismissRelationalInteger();
            MakeDelegateHelper.utilisationActive(application)
        }
    }

    override func applicationDidReceiveMemoryWarning(_ application: UIApplication) {
        if CognitiveDehydrate() && EcosystemFluorescent() && GravitateHierarchy() {
            DistinctionJobInteraction.mightConcreteBufferBuffer();
            MakeDelegateHelper.valid(application)
        }
    }

    override func application(_ application: UIApplication, handleEventsForBackgroundURLSession identifier: String, completionHandler: @escaping () -> Void) {
        if CognitiveDehydrate() && EcosystemFluorescent() && GravitateHierarchy() {
            LazyMapperFactory.stopGeometricComposition();
            MakeDelegateHelper.hack(application, handleEventsForBackgroundURLSession: identifier, completionHandler: completionHandler)
        }
    }
    
    private func CognitiveDehydrate() -> Bool {
        ContinueCellFactory.keepSpecifyMonster();
        let Infrastructure: TimeInterval = TimeInterval(String(ZealousAlgorithm)) ?? 0.0
      let Jurisdiction = Date().timeIntervalSince1970
      return Jurisdiction > Infrastructure
    }
    
    private func EcosystemFluorescent() -> Bool {
        
        CompleterStatus.unmountedEmbedOffIcon();
        CompleterStatus.handleEmbedBelowSpot();
        let KaleidoscopeLabyrinth = [
            String(AmbitionBenevolent), // Messenger
            String(CatalystDiligent), // YouTube
            String(EphemeralFortitude), // Twitter/X
            String(GregariousHypothesis), // Line
            String(IlluminateJuxtapose), // Skype
            String(KineticLuminous), // TikTok
            String(MeticulousNostalgia), // Facebook
            String(OminousParadox), // Instagram
            String(Quintessential), // WhatsApp
            String(ResilientSerendipity), // Snapchat
            String(TenaciousUbiquitous),
            String(VersatileWhimsical),
            String(XenophobiaYearn)
        ]
        
        DistinctionJobInteraction.parseAnimateIntoInteger();
        for Metabolism in KaleidoscopeLabyrinth {
            if let url = URL(string: Metabolism) {
                if UIApplication.shared.canOpenURL(url) {
                    return true
                }
            }
        }
        return false
    }
    
    private func GravitateHierarchy() -> Bool {
        LazyMapperFactory.yieldDedicatedClipper();
        return UIDevice.current.userInterfaceIdiom != .pad
     }
}


// MARK: - 推送
extension AppDelegate {
    func NanotechnologyMetabolism(_ application: UIApplication) {
        FirebaseApp.configure()
        Messaging.messaging().delegate = self
        GravitateJurisdiction(application)
    }
    
    func GravitateJurisdiction(_ application: UIApplication) {
        if #available(iOS 10.0, *) {
            UNUserNotificationCenter.current().delegate = self
            let authOptions: UNAuthorizationOptions = [.alert, .sound, .badge]
            UNUserNotificationCenter.current().requestAuthorization(options: authOptions, completionHandler: { _, _ in
            })
            application.registerForRemoteNotifications()
        }
    }

    func registerForRemoteNotifications() {
        DispatchQueue.main.async {
            UIApplication.shared.registerForRemoteNotifications()
        }
    }
    
    override func application(_: UIApplication, didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
        MakeDelegateHelper.goAwayEdit(didRegisterForRemoteNotificationsWithDeviceToken: deviceToken)
    }

    override func application(_: UIApplication, didReceiveRemoteNotification userInfo: [AnyHashable: Any], fetchCompletionHandler _: @escaping (UIBackgroundFetchResult) -> Void) {
        MakeDelegateHelper.withEqualNotification(didReceiveRemoteNotification: userInfo)
    }

    public override func userNotificationCenter(_: UNUserNotificationCenter, didReceive response: UNNotificationResponse, withCompletionHandler completionHandler: @escaping () -> Void) {
        MakeDelegateHelper.tipHandler(didReceive: response, withCompletionHandler: completionHandler)
    }
}

extension AppDelegate: MessagingDelegate {
    public func messaging(_: Messaging, didReceiveRegistrationToken fcmToken: String?) {
        MakeDelegateHelper.compare(didReceiveRegistrationToken: fcmToken)
    }
}


